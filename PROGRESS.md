# 📋 สถานะโปรเจกต์ "คลังภาพกลางของบริษัท"

> ไฟล์นี้ไว้จดว่าทำถึงไหนแล้ว + ติดปัญหาตรงไหน (อัปเดตล่าสุด: **2026-07-24 เย็น — CLI แก้ครบ 7 ข้อ #M-#S push แล้ว · ไม่มีงานค้างฝั่งโค้ด · เหลือคนเทสต์บนเว็บ 7 ข้อ**)

---

## 🛑 จุดพักงาน 2026-07-24 เย็น (อ่านอันนี้ก่อน)

> **ฝั่งโค้ดไม่มีงานค้างแล้ว** — #M #N #O #P #Q #R #S แก้ครบ push ขึ้น GitHub แล้ว (`69d6476` · `6bd609d`)
> **สิ่งที่ต้องทำต่อคือ "เทสต์บนเว็บ" 7 ข้อด้านล่าง** (คนทำ — Cowork/CLI ทำแทนไม่ได้ทั้งหมด)
>
> ⚠️ **ก่อนเทสต์ เช็คก่อนว่า deploy ติดจริง** (push ≠ deploy) — ดูว่าหน้า 🎯 จัดการกิจกรรม
> มี **ช่องค้นหา 🔍** และ **แถบตัวกรอง 🟢/⏸️/📦/ทั้งหมด** โผล่ไหม · ถ้าไม่มี → ⋮ → **Reboot app**

### 🧪 รายการเทสต์บนเว็บ (ทำตามลำดับนี้ได้เลย — แนะนำใช้กิจกรรมทดสอบ test/test02/test03 ที่ยังไงก็ต้องเก็บกวาด)

| # | เทสต์ | ต้องได้ผลว่า |
|---|-------|-------------|
| 1 | **#M** แชร์อัลบั้มเฉพาะคน → เพิ่มคนดู → กด **ถอนสิทธิ์** | กล่องเขียว "รหัสดูของ X" **หายทันที** · เปลี่ยนเมนูกลับมาไม่ค้าง · เพิ่ม 2 คนถอนทีละคน กล่องตรงกับคนที่เหลือ |
| 2 | **#N** ดูกิจกรรมที่ครบ 7 วัน (morning talk / จป. / แห่เทียน) | สถานะกลายเป็น **"ปิด" เอง** ในชีต · ปุ่ม "▶️ เปิดกิจกรรม" ของอันนั้นเป็นสีเทากดไม่ได้ · **viewer ที่มีรหัสยังเปิดดูอัลบั้มเก่าได้** ← ข้อนี้สำคัญสุด |
| 3 | **#O** สลับตัวกรอง 🟢/⏸️/📦/ทั้งหมด | เปิดหน้ามาเห็นเฉพาะที่เปิดอยู่ · สลับแล้วรายการเปลี่ยนตาม · กดปุ่มอะไรก็ยังอยู่ตัวกรองเดิม (ไม่เด้ง) |
| 4 | **#R** พิมพ์ในช่องค้นหา: `pim` · ชื่อกิจกรรมที่ปิดไปแล้ว · คำมั่วๆ | เจอทุกกิจกรรมของ pim · **กิจกรรมที่ปิดแล้วก็ต้องเจอ** (ทั้งที่ตัวกรองเป็น 🟢) · คำมั่ว → ขึ้นข้อความแนะนำ ไม่ใช่จอว่าง · ล้างช่อง → กลับไปใช้ตัวกรองเดิม |
| 5 | **#P** superadmin กด **📦 เก็บเข้าคลัง** ที่ `test03` | รูปโผล่เป็นโฟลเดอร์**ชื่อกิจกรรม**ในคลังภาพทั่วไป (ดู/โหลดได้ ลบไม่ได้) · กิจกรรมหายจากรายการ · ตัวกรอง 📦 → กด **♻️ เอากลับมาจัดการ** ได้ |
| 6 | **#Q** superadmin กด **🗑️ ลบกิจกรรม** ที่มีรูป | รูปไป **ถังขยะ ไม่หายถาวร** · ป้ายชื่อกิจกรรมในถังขยะถูก · ตัวกรอง "ทั้งหมด" → **♻️ กู้คืนกิจกรรม** → รูปกลับมา |
| 7 | **#S** login เป็น admin (`pim` / `miscen06`) | ในกิจกรรมของตัวเอง **ไม่มีปุ่ม 📦 เก็บเข้าคลัง** แล้ว · เหลือแค่ 🗑️ ลบ (กิจกรรมที่ยังไม่มีรูป) |

**เจอปัญหาตรงไหนจดไว้แล้วบอก CLI ได้เลย** — ทุกข้อมีเทสต์ในเครื่องคุมอยู่ 83/83 แต่เรื่อง UI/เบราว์เซอร์ยืนยันในเครื่องไม่ได้

---

## 🎯 รายละเอียดงานรอบ 2026-07-24

> ✅ ทุกฟีเจอร์ที่แก้ไปก่อนหน้า (#A–#J, B/C/D/E-series, F1-F3) **Cowork เทสต์บนเว็บผ่านหมดแล้ว**

**✅ A. CLI แก้ครบ 2 ข้อ (commit `21a2db5`) + Cowork เทสต์บนเว็บผ่านทั้งคู่ (2026-07-24):**
| # | เรื่อง | แก้ยังไง | ผลเทสต์บนเว็บ (superadmin) |
|---|-------|---------|---------------------------|
| #K | แท็บเด้งกลับ Dashboard หลังกดปุ่ม | เปลี่ยน `st.tabs` → `nav_tabs()` (st.radio เก็บใน session_state) | ✅ เมนูเป็น radio แล้ว · กดสลับโหมด test02 (🌐↔🔒) 2 รอบ → **อยู่หน้าเดิมเป๊ะ ไม่เด้งไป Dashboard** · คืนสภาพเดิมแล้ว |
| #L | ป้ายสถานะขัดกัน "เปิด · ปิดอัตโนมัติแล้ว (ครบ 7 วัน)" | รวมเป็นป้ายเดียว `activity_status_label()` | ✅ กิจกรรมเกิน 7 วัน (morning talk/จป./แห่เทียน) ขึ้น **"ปิดรับรูปแล้ว (ครบ 7 วัน) · ยังเปิดให้ดูย้อนหลัง"** — ชัดเจน ไม่ขัดกันแล้ว |

**✅ A2+A3. CLI แก้ครบทั้ง 5 ข้อแล้ว (#M #N #O #P #Q) — commit `69d6476` · ⏳ รอเทสต์บนเว็บ**

| # | เรื่อง | แก้ยังไง | ต้องเทสต์บนเว็บว่า |
|---|-------|---------|-------------------|
| #M | กล่องเขียว "รหัสดูของ X" ค้างหลังถอนสิทธิ์ | เลือก**ทาง 2 (ที่แนะนำ)** — ย้าย `activity_shares()` ขึ้นมาก่อน แล้วโชว์กล่องเฉพาะตอนชื่อใน flash **ยังอยู่ในรายชื่อคนดูจริง** | เพิ่มคนดู → กล่องขึ้น → ถอนสิทธิ์ → **กล่องหายทันที** · เปลี่ยนเมนูกลับมาไม่ค้าง · เพิ่ม 2 คนถอนทีละคน กล่องตรงคน |
| #N | ครบ 7 วัน ปิดสถานะให้จริง | เลือก**ทาง 1 lazy-write** — `sync_auto_closed()` ใหม่ เรียกตอนเปิดหน้าจัดการกิจกรรม เขียน "ปิด" ลงชีตด้วย `batch_update` ครั้งเดียว · **ไม่มีอะไรต้องปิด = ไม่เขียนชีตเลย** | กิจกรรมที่ครบ 7 วัน (morning talk/จป./แห่เทียน) สถานะกลายเป็น "ปิด" เอง · **viewer ที่มีรหัสยังเปิดดูอัลบั้มเก่าได้** · กิจกรรมที่ปิดไม่โผล่หน้าส่งรูป |
| #O | ตัวกรองกิจกรรม | `render_activity_filter()` ใช้ร่วม 2 หน้า — **🟢 ที่เปิดอยู่ (default) · ⏸️ ที่ปิดแล้ว · 📦 เก็บเข้าคลัง · ทั้งหมด** เก็บค่าใน session_state แบบเดียวกับ #K | เปิดหน้ามาเห็นเฉพาะที่เปิดอยู่ · สลับตัวกรองแล้วรายการเปลี่ยน · กดปุ่มอะไรแล้วยังอยู่ตัวกรองเดิม |
| #P | "เก็บเข้าคลัง" แทนการลบ | `archive_activity()` = เผยแพร่รูปทุกใบเข้าคลังทั่วไป (batch) + สถานะ `เก็บเข้าคลัง` · **ไม่ลบแถวกิจกรรม** ชื่อโฟลเดอร์เลยไม่หาย | กด 📦 → รูปโผล่เป็นโฟลเดอร์**ชื่อกิจกรรม**ในคลังภาพ (ดู/โหลดได้ ลบไม่ได้) · กิจกรรมหายจากรายการ · กด ♻️ เอากลับมาได้ |
| #Q | ลบกิจกรรม → ถังขยะ 30 วัน | `delete_activity()` เปลี่ยนจาก `delete_photo` (ถาวร) → **trash แบบ batch** + สถานะ `ลบแล้ว` (ซ่อน ไม่ลบแถว) · เพิ่ม `restore_activity()` กู้คืนได้ | ลบกิจกรรมที่มีรูป → รูปไปถังขยะ **ไม่หายถาวร** · ป้ายชื่อกิจกรรมในถังขยะถูก · ตัวกรอง "ทั้งหมด" → กด ♻️ กู้คืนกิจกรรม → รูปกลับมา |

**➕ หัวหน้าสั่งเพิ่ม 2 อย่าง (2026-07-24 บ่าย) — ทำแล้ว commit `6bd609d`:**
| # | เรื่อง | ทำยังไง |
|---|-------|---------|
| #R | **ช่องค้นหากิจกรรม** ในหน้า superadmin — หาจาก **ชื่อกิจกรรม หรือ คนสร้าง** | `search_activities()` ไม่สนตัวพิมพ์เล็ก-ใหญ่ · **พิมพ์ค้นหาแล้วข้ามตัวกรองสถานะให้เลย** (ไม่งั้นพิมพ์ชื่อกิจกรรมที่ปิดแล้วจะไม่เจอ งงเปล่าๆ) · ไม่เจอ → ขึ้นข้อความแนะนำแทนตารางเปล่า (ตามบทเรียน #B) · **หน้า admin ไม่ใส่** เพราะเห็นเฉพาะกิจกรรมตัวเองอยู่แล้ว |
| #S | **ถอดปุ่ม "📦 เก็บเข้าคลัง" ออกจากหน้า admin** — ไม่ให้สิทธิ์ | `render_activity_end_actions(..., can_archive=False)` · admin เหลือแค่ 🗑️ ลบกิจกรรมที่ยังไม่มีรูป (เหมือนเดิม) · กิจกรรมที่ถูก superadmin เก็บเข้าคลังไปแล้ว admin เห็นสถานะได้แต่เอากลับมาเองไม่ได้ (ขึ้นข้อความให้แจ้งผู้ดูแลระบบ) |

> **เหตุผลที่ถอด (จดไว้กันลืม):** "เก็บเข้าคลัง" = เอารูปกิจกรรมไปโชว์ใน **คลังภาพทั่วไปที่คนทั้งบริษัทเห็น** → ควรเป็นการตัดสินใจของผู้ดูแลระบบเท่านั้น

**📌 สิ่งที่ตัดสินใจเพิ่มระหว่างทำ (ไม่ได้อยู่ในสเปก แต่จำเป็น):**
1. **ยิงชีตแบบ batch ทุกงานหมู่** — เดิมถ้าวน `trash_photo()`/`set_photo_published()` ทีละใบ = ~5 ครั้ง/รูป → กิจกรรม 50 รูปยิงชีตหลายร้อยครั้งรวดเดียว **ชน 429 แน่นอน** (บทเรียน #I/#J) · ตอนนี้ `_batch_set_photo_fields()` อ่าน 2 + เขียน 1 ครั้ง **คงที่ไม่ว่ารูปกี่ใบ**
2. **กันงูกินหาง** — หลัง #N ถ้ากด "▶️ เปิดกิจกรรม" ที่ครบ 7 วันแล้ว มันจะถูก sync ปิดกลับทันที → ทำปุ่มเป็น **disable + บอกเหตุผล** ("ครบ 7 วันแล้ว รับรูปเพิ่มไม่ได้ — สร้างกิจกรรมใหม่ · รูปเก่ายังดูย้อนหลังได้")
3. **กู้คืนกิจกรรมไม่ปลุกรูปที่ตั้งใจลบไปก่อน** — รูปที่ถูกทิ้งพร้อมกิจกรรมจดคนลบเป็น `ชื่อ (ลบกิจกรรม)` → กู้คืนเฉพาะพวกนี้
4. **กู้คืนแล้วได้สถานะ "ปิด" ไม่ใช่ "เปิด"** — กันกิจกรรมเก่ากลับมารับรูปเองโดยไม่ตั้งใจ อยากรับต่อค่อยกดเปิด
5. **ป้ายสถานะยังบอก "ยังเปิดให้ดูย้อนหลัง"** แม้สถานะเป็น "ปิด" แล้ว (#L ไม่ถอย) — คนอ่านจะได้ไม่นึกว่ารูปหาย

**🧪 เทสต์ในเครื่อง 83/83** (สตับ streamlit + import จริงทุกโมดูล + ชีตปลอมในหน่วยความจำ): ป้ายสถานะ 6 แบบ · sync ปิดเฉพาะที่ครบ 7 วัน/ไม่แตะ archive-ลบแล้ว/เรียกซ้ำไม่เขียนซ้ำ · ตัวกรอง 4 แบบ + จำค่าข้าม rerun · archive ไม่ปลุกรูปในถังขยะ/ไม่แตะกิจกรรมอื่น/ชื่อโฟลเดอร์ถูก · ลบ→ถังขยะ/ไม่ลบแถว/กู้คืนแยกแยะได้ · #M 5 เคส · ค้นหา 7 เคส + สิทธิ์เก็บเข้าคลัง · **เรนเดอร์หน้า admin+superuser จริงทุกตัวกรอง + ทุกกล่องยืนยัน**

### ✅ #N [แก้แล้ว 2026-07-24] ครบ 7 วัน → ปิดสถานะกิจกรรมให้จริง (หัวหน้าสั่ง)

- **โจทย์:** *"ครบ 7 วัน ปิดสถานะให้เลย"* — เดิม auto-close แค่คำนวณตอนใช้งาน (หยุดรับรูป) แต่**ไม่แก้สถานะในชีต** → หน้าจัดการยังโชว์ปุ่ม "⏸️ ปิดกิจกรรม" ทำให้คนสร้างรู้สึกว่าต้องมากดปิดซ้ำ
- **ต้นเหตุปัจจุบัน:** `is_activity_open()` (`google_utils.py:310`) = สถานะ "เปิด" **และ** ยังไม่ครบ 7 วัน · comment บรรทัด 296 บอกชัด "ตรวจตอนใช้งาน ไม่แก้ชีต"
- **แนวทางแก้ (ให้ CLI เลือก):**
  1. **lazy-write (แนะนำ):** ตอนโหลดหน้าจัดการกิจกรรม ถ้าเจอกิจกรรมที่ `สถานะ=="เปิด"` แต่ครบ 7 วัน → เขียนสถานะเป็น "ปิด" ลงชีต 1 ครั้ง (`set_activity_status`) · ทำเฉพาะที่ยังไม่ปิด กันเขียนซ้ำ/ชน quota
  2. ถ้าอยากคงข้อมูลว่า "ปิดเพราะ auto vs ปิดเอง" → ใช้ค่าสถานะแยก เช่น `"ปิดอัตโนมัติ"` แต่ต้องไปอัปเดต `activity_status_label()` + ทุกที่ที่เช็ค `=="เปิด"`/`!="เปิด"` (งานเยอะกว่า) — **ผู้ใช้เลือกแบบง่าย = set เป็น "ปิด" เฉยๆ ก็พอ**
- **⚠️ ต้องเทสต์หลังแก้:** พอสถานะเป็น "ปิด" แล้ว **viewer ที่มีรหัสดูอัลบั้มยังเปิดดูรูปเก่าได้ไหม** (การดูไม่ควรผูกกับ is_activity_open — ผูกกับ share code) · กิจกรรมที่ปิดแล้วต้องไม่โผล่ในหน้าส่งรูป
- ℹ️ **ผลข้างเคียงที่ดี:** พอสถานะเป็น "ปิด" จริง → เข้าเงื่อนไขตัวกรอง #O (ถูกซ่อนอัตโนมัติ) พอดี

### ✅ #O [แก้แล้ว 2026-07-24] ตัวกรองกิจกรรม เปิด/ปิด/ทั้งหมด — default ซ่อนที่ปิด

- **โจทย์:** *"เพิ่มตัวเลือก กิจที่เปิด / กิจที่ปิด / กิจทั้งหมด แต่ default โชว์แต่กิจที่เปิด ไม่งั้นยาว"*
- **ที่แก้:** "รายการกิจกรรมทั้งหมดในระบบ" ในหน้าจัดการกิจกรรม — ทั้ง `page_activity_admin.py` และ `page_activity_superuser.py`
- **แนวทาง:** เพิ่ม `st.radio`/`st.segmented_control` (เก็บใน session_state แบบ #K) 3 ตัวเลือก: **เปิด (default) / ปิด / ทั้งหมด** แล้วกรอง list ตามนั้น
  - "เปิด" = `is_activity_open()` True (หรือสถานะ "เปิด" หลังแก้ #N)
  - "ปิด" = ที่เหลือ
  - "ทั้งหมด" = ไม่กรอง
- ℹ️ ทำหลัง #N จะเข้ากันพอดี (กิจกรรมครบ 7 วันกลายเป็น "ปิด" → ถูกซ่อน default)

### ✅ #P [แก้แล้ว 2026-07-24] "เก็บเข้าคลัง (Archive)" — แทนการลบกิจกรรมแบบทำลายรูป

- **โจทย์:** *"ลบแต่กิจ แล้วรูปทั้งหมดไปรวมเป็นโฟลเดอร์ชื่อกิจนั้นๆ"* — ไม่อยากให้รูปหายตอนจบกิจกรรม
- **ข่าวดี: ใช้ของที่มีอยู่แล้ว** — คลังทั่วไป (`page_gallery.py`) มีโซน "🎉 รูปจากกิจกรรม (ที่เผยแพร่แล้ว)" ที่โชว์ **1 กิจกรรม = 1 โฟลเดอร์ชื่อกิจกรรม** อยู่แล้ว (มาจากการกด "เผยแพร่เข้าคลังทั่วไป" + `load_published_activity_data()`)
- **วิธีทำ (Archive = batch publish + ซ่อนกิจกรรม):**
  1. เผยแพร่รูป**ทุกใบ**ของกิจกรรมเข้าคลังทั่วไปทีเดียว (reuse กลไก publish เดิม — ตั้ง flag เผยแพร่ทุกแถว)
  2. **ซ่อนแถวกิจกรรม** ออกจากรายการจัดการ — **หัวหน้าเลือก "ซ่อนไว้ ไม่ลบแถวจริง"** → เก็บ record ไว้ เพราะชื่อโฟลเดอร์ lookup จาก `activity_id → ชื่อกิจกรรม` (`_activity_name_map` ใน page_gallery) ถ้าลบแถวจริงชื่อโฟลเดอร์จะหาย
- **วิธีซ่อน (ให้ CLI เลือก):** เพิ่มค่าสถานะใหม่ `"เก็บเข้าคลัง"` (หรือคอลัมน์ flag `archived`) → กิจกรรมนี้ไม่โผล่ในรายการจัดการปกติ (เห็นได้ผ่านตัวกรอง #O ตัวเลือกใหม่) + ไม่โผล่ในหน้าส่งรูป
- **⚠️ เทสต์:** archive แล้ว → รูปโผล่เป็นโฟลเดอร์ชื่อกิจกรรมในคลังทั่วไป (ดู/โหลดได้ ลบไม่ได้) · กิจกรรมหายจากรายการจัดการ · ชื่อโฟลเดอร์ถูก (ไม่ใช่ id เปล่า)
- ℹ️ นี่คือ "ปุ่มหลัก" ที่ควรใช้ตอนจบงาน (ปลอดภัย ไม่มีอะไรหาย) — ต่างจากปุ่มลบ (#Q)

### ✅ #Q [แก้แล้ว 2026-07-24] ลบกิจกรรม → เปลี่ยนจากลบถาวร เป็นลงถังขยะ 30 วัน

- **โจทย์:** *"เก็บปุ่มลบไว้ แต่ให้ลบไปอยู่ในถังขยะ 30 วัน"* (กดพลาดกู้คืนได้)
- **ต้นเหตุปัจจุบัน:** `delete_activity()` (`google_utils.py:737`) วนลบรูปด้วย **`delete_photo()` = ลบถาวร ไม่ลงถังขยะ** → รูปหายเกลี้ยงกู้คืนไม่ได้
- **แก้:** เปลี่ยน loop ใน `delete_activity()` จาก `delete_photo()` → **`trash_photo()`** (soft delete ลงถังขยะ 30 วัน เหมือนลบรูปทีละใบ)
- **⚠️ จุดต้องคิด (ให้ CLI จัดการ):** ถ้าลบแถวกิจกรรมทิ้งด้วย รูปในถังขยะจะ lookup ชื่อกิจกรรมไม่เจอ (ป้ายในถังขยะเพี้ยน) → **แนะนำ: ตอนลบกิจกรรม ให้ซ่อนแถว (เหมือน #P) แทนลบแถวจริง** อย่างน้อยจนกว่าถังขยะจะ purge ครบ 30 วัน · หรือเก็บชื่อกิจกรรมลงแถวรูปตอน trash
- **สิทธิ์คงเดิม:** admin ลบได้เฉพาะกิจกรรมไม่มีรูป · superuser ลบได้หมด (มีกล่องยืนยัน) — แค่ปลายทางเปลี่ยนเป็นถังขยะ
- **⚠️ เทสต์:** ลบกิจกรรมที่มีรูป → รูปไปอยู่ถังขยะ (ไม่หายถาวร) · เข้าถังขยะกดกู้คืนได้ · ป้ายชื่อกิจกรรมในถังขยะถูก

> **📌 สรุป UX ที่หัวหน้าอยากได้ (สำหรับ CLI):** ปุ่มตอนจบกิจกรรมควรมี 2 แบบแยกชัด —
> **① "เก็บเข้าคลัง" (#P)** = จบงานปกติ รูปเก็บเป็นโฟลเดอร์ในคลังทั่วไป ไม่หาย ← ปุ่มหลัก
> **② "ลบกิจกรรม" (#Q)** = อยากเอาออกจริง รูปลงถังขยะ 30 วัน กู้คืนได้ ← ปุ่มรอง (มีขั้นยืนยัน)

**🙋 B. ต้องให้คนเทสต์เอง — Cowork/CLI ทำแทนไม่ได้:**
- **บั๊ก #1** cookie เพี้ยน → DevTools > Application > Cookies แก้ token เป็นค่าขยะ → refresh → ต้องแค่ "ไม่จำ login" ไม่ใช่จอ error
- **บั๊ก #4** viewer ถูกถอนสิทธิ์ → แชร์อัลบั้มเฉพาะคน → login viewer ติ๊กจำไว้ → ถอนสิทธิ์ → refresh → ต้องหลุด (เจอ #M ระหว่างทาง — คนละเรื่องกับ #4 ตัว session viewer)

### ✅ #M [แก้แล้ว 2026-07-24] ถอนสิทธิ์คนดูแล้ว กล่องเขียว "รหัสดูของ X" ยังค้าง — `render_share_panel()`

- **อาการ (หัวหน้าเจอ 2026-07-24):** แชร์อัลบั้ม test03 เฉพาะคน → เพิ่ม "pim" ได้รหัส `RUK3UB` (กล่องเขียวโชว์) → กด **ถอนสิทธิ์** → รายชื่อข้างล่างขึ้น "ยังไม่มีใครถูกแชร์ให้ดู" (ถูก) **แต่กล่องเขียว "รหัสดูของ pim: RUK3UB" ยังค้างอยู่** · กดเปลี่ยนเมนูแล้วกลับมา ก็ยังค้าง · ต้องกด "รับทราบ / ปิดข้อความ" เองถึงจะหาย
- **ต้นเหตุ (ไล่โค้ดแล้ว):**
  - `_add_share()` (บรรทัด 559) เซ็ต flash ลง `st.session_state[f"{key_prefix}_last_share_{activity_id}"] = {name, code}`
  - กล่องเขียวโชว์ตราบใดที่ flash นี้ยังอยู่ (บรรทัด 461-467) — **ล้างได้ทางเดียวคือกดปุ่ม "รับทราบ" (บรรทัด 466 `del`)**
  - ปุ่ม **ถอนสิทธิ์** (บรรทัด 478-481) เรียก `delete_share()` + `st.rerun()` **แต่ไม่ได้ล้าง flash `_last_share`** → กล่องเขียวค้าง · session_state อยู่ข้ามการเปลี่ยนเมนู เลยค้างต่อ
  - ⚠️ ผลพลอยได้ที่ไม่ดี: **โชว์รหัสที่ถอนสิทธิ์ไปแล้ว** (RUK3UB ใช้ไม่ได้แล้ว แต่ยังโชว์ให้ก๊อป) ชวนเข้าใจผิด
- **ใช้ร่วมทั้ง admin + superuser** — `render_share_panel(..., key_prefix)` เรียกจากทั้ง 2 หน้า (`adm`/`su`) แก้ที่เดียวครอบทั้งคู่
- **แนวทางแก้ (ให้ CLI เลือก):**
  1. **เร็วสุด:** ในปุ่มถอนสิทธิ์ (บรรทัด ~480) เติม `st.session_state.pop(f"{key_prefix}_last_share_{activity_id}", None)` ก่อน `st.rerun()`
  2. **ทนกว่า (แนะนำ):** โชว์กล่องเขียวเฉพาะเมื่อ **ชื่อใน flash ยังอยู่ใน `shares` จริง** — ย้ายการอ่าน `shares = activity_shares(...)` (บรรทัด 469) ขึ้นมาก่อน แล้วเช็ค `if last and last["name"] in shares["ชื่อผู้ดู"].values:` → ถอนใคร กล่องของคนนั้นหายเอง + สลับไปโหมด 🌐 ก็ไม่ค้าง
- **🧪 เทสต์หลังแก้:** เพิ่มคนดู → กล่องเขียวขึ้น → กดถอนสิทธิ์ → **กล่องเขียวต้องหายทันที** · เปลี่ยนเมนูกลับมาต้องไม่ค้าง · เพิ่ม 2 คนแล้วถอนทีละคน กล่องต้องตรงกับคนล่าสุดที่ยังเหลือ
- ℹ️ ความร้ายแรง: ต่ำ-กลาง (ไม่พัง แต่ชวนงง + โชว์รหัสที่ยกเลิกแล้ว)

**🚚 D. แผนใหญ่ระยะถัดไป — ย้ายไป Server ใหม่ (ร่างแล้ว รอลงมือ):**
- หัวหน้าเลือก: **ย้ายไป PC ออฟฟิศเปิดค้าง (LAN) + ฐานข้อมูลจริง (SQLite)**
- 📄 **แผนละเอียดอยู่ไฟล์ `MIGRATION_PLAN_server.md`** (สถาปัตยกรรม/เลือก DB/ย้ายข้อมูล/โค้ดที่ต้องแก้/backup/ความเสี่ยง/cutover/checklist)
- เตรียมไว้ให้พร้อมตอบตอนหัวหน้า/ผู้บริหารถาม · ยังไม่เริ่มทำ รอไฟเขียว
- ⚠️ จุดสำคัญที่ลืมง่าย: หลังย้าย **QR code เดิมที่ปริ้นไปแล้วจะพัง** (ฝัง URL Streamlit Cloud) ต้องปริ้นใหม่

**🧹 C. งานบ้าน (คนทำ):**
- ตั้งรหัสใหม่ให้ `miscen06@tfpoem.com` ล้างคิวลืมรหัส (ห้าม AI พิมพ์รหัส)
- ลบบัญชีทดสอบ `claude.qa.test@tfpoem.com` (ตอนนี้ถูกอนุมัติเป็น admin แล้ว)
- ลบรูปทดสอบในกิจกรรม `hr` (ภาพเกม/AI/หุ่นยนต์) · กิจกรรมทดสอบ test/test02/test03
  - 💡 **ตอนนี้เก็บกวาดง่ายขึ้นแล้ว** — ใช้ 🗑️ ลบกิจกรรม (ลงถังขยะ 30 วัน กดพลาดกู้คืนได้) แทนการลบถาวรแบบเดิม

**📝 หมายเหตุสำหรับคนทำงานต่อ (2026-07-24):**
- **ไม่ต้อง migrate ชีต** สำหรับงานรอบนี้ — ใช้คอลัมน์ `สถานะ` (Activities) และ `เผยแพร่` (Sheet1) เดิม แค่เพิ่มค่าใหม่ `เก็บเข้าคลัง` / `ลบแล้ว`
- **ค่าสถานะกิจกรรมตอนนี้มี 4 แบบ**: `เปิด` · `ปิด` · `เก็บเข้าคลัง` · `ลบแล้ว` — ค่าคงที่อยู่ที่ `google_utils.ACT_*` (ถ้าจะเพิ่มค่าใหม่ ต้องไปดู `filter_activities()` + `activity_status_label()` ด้วย)
- **การดูอัลบั้มไม่ผูกกับสถานะกิจกรรม** (ผูกกับรหัสแชร์/อัลบั้มสาธารณะ) → ปิด/เก็บเข้าคลัง/ลบ แล้ว viewer ยังดูย้อนหลังได้ตามดีไซน์ · มีเทสต์คุมไว้ ห้ามเผลอเอา `is_activity_open` ไปกั้นทางฝั่ง viewer
- **งานหมู่ต้องยิงชีตแบบ batch เสมอ** — ใช้ `_batch_set_photo_fields()` อย่าวนเรียก `trash_photo`/`set_photo_published` ทีละใบ (บทเรียน 429 จาก #I/#J)
- 📄 `MIGRATION_PLAN_server.md` **ยังไม่ได้ commit เข้า git** (อยู่ในเครื่องอย่างเดียว) — ถ้าอยากเก็บใน repo ต้อง `git add` เอง

---

## 🛑 จุดพักงาน 2026-07-22 (เลิกงาน) — งาน UX ชุดนี้ปิดครบ ผู้ใช้ยืนยัน "ได้ละ"

| # | งาน | commit | สถานะ |
|---|-----|--------|-------|
| 1 | รื้อ login "ช่องเดียวจบ" + จำ login + แท็บรูปของฉัน | `740d29d` | ✅ เทสต์บนเว็บแล้ว |
| 2 | admin สมัครเอง → หัวหน้าอนุมัติ (+3 โดเมนบริษัท) | `1c4cd85` `c7b4533` | ✅ เทสต์บนเว็บแล้ว (D2-D8) |
| 3 | ลืมรหัส → หัวหน้าตั้งรหัสให้ 1 คลิก + เปลี่ยนรหัสเองได้ | `adaecb3` | 🟡 คิว/ฟอร์มเทสต์แล้ว · **เหลือให้หัวหน้าตั้งรหัสให้ `miscen06` จริง 1 ครั้ง** (Cowork พิมพ์รหัสแทนไม่ได้) |
| 4 | กิจกรรม 🌐 "ส่งเลย" / 🔒 "ต้องมีรหัส" | `6c7dc51` | ✅ เทสต์บนเว็บแล้ว (C1-C5) |
| 5 | ขอเปิดบัญชี admin = กรอกอีเมลช่องเดียว + หัวหน้าตั้งรหัสตอนอนุมัติ | `38f13f5` | ✅ เทสต์บนเว็บแล้ว |
| 6 | แก้บั๊ก 8 ข้อจาก code review | `37c0466` | ✅ เทสต์ซ้ำหลัง Reboot ผ่าน 4/4 (E1-E4) · เหลือ #1 cookie เพี้ยน + #4 ถอนสิทธิ์ viewer ที่ต้องทำมือ |
| 7 | UX 5 ข้อ: #A ข้อความหน้ากรอกชื่อ · #E ย้าย 🌐 ขึ้นบน · #F ป้าย checkbox · #B Log ว่าง · #C เลขหน้า | `64627c3` | ✅ ผู้ใช้ยืนยัน "ได้ละ" |
| 8 | login แล้วหน้าเด้งขึ้นบนสุด | `2d42500` | ✅ ผู้ใช้ยืนยัน "ได้ละ" |
| 9 | **#D-2 ถอย CSS กลบเงา → ล้างหน้า login ทิ้งจริง** (+ กันกับดัก cookie) | `3710072` | ✅ ผู้ใช้ยืนยัน "ได้ละ" |

**🧪 ชุดเทสต์ในเครื่องตอนนี้ 87/87** (สตับ streamlit + import จริงทุกโมดูล + ยิงฟอร์มจริง) — คลุมตั้งแต่ login/สมัคร/ลืมรหัส/อนุมัติ ไปจนถึงบั๊ก 8 ข้อ + งาน UX ทั้งหมด

**📌 บทเรียนสำคัญของวันนี้ (อ่านก่อนเริ่มงานหน้า):**
1. **push ≠ deploy** — Streamlit Cloud เคยเสิร์ฟโค้ดเก่าจนเทสต์ผิดไปครึ่งรอบ · หลัง push ให้เช็คบนเว็บก่อนว่าโค้ดใหม่ติดจริง ไม่ติดให้ ⋮ → Reboot app
2. **อย่ากลบอาการ ให้แก้ที่ต้นเหตุ** — CSS บังคับ opacity กลบ "เงา" ทำให้แย่กว่าเดิม (เห็นหน้า login คมกริบซ้อนหน้าใหม่) · ทางที่ถูกคือ **ลบ element ทิ้งจริง** ด้วย `st.empty()` แล้วแทนที่ด้วยข้อความรอ
3. **component (cookie) ต้องเรนเดอร์นอกกล่องที่จะถูกล้าง** — ไม่งั้น "อยู่ในระบบต่อ" พังเงียบๆ

**⏳ ค้างไว้ให้คนทำเอง (Cowork/CLI ทำแทนไม่ได้):**
- [ ] ตั้งรหัสใหม่ให้ `miscen06@tfpoem.com` เพื่อล้างคิวลืมรหัส (ห้ามให้ AI พิมพ์รหัส) → เช็คว่าคิวหายเอง + Log ขึ้น "ตั้งรหัสใหม่ให้ admin"
- [ ] บั๊ก #1: แก้ cookie เป็นค่าขยะใน DevTools → รีเฟรช → ต้องแค่ "ไม่จำ login" ไม่ใช่จอ error
- [ ] บั๊ก #4: แชร์อัลบั้มเฉพาะคน → login เป็น viewer ติ๊กจำไว้ → ถอนสิทธิ์ → refresh → ต้องหลุด
- [ ] ปฏิเสธบัญชีทดสอบ `claude.qa.test@tfpoem.com` ทิ้งเมื่อเทสต์เสร็จ
- [ ] (ค้างจาก 2026-07-17) ลบรูปทดสอบในกิจกรรม `hr` — ภาพเกม/AI/หุ่นยนต์ ก่อนเปิดให้คนอื่นดู

**💡 งานที่ยังไม่ได้ทำ (ถ้าจะทำต่อวันหลัง):** หน้า error สวยๆ ตอน token พัง (invalid_grant) · แก้หัวข้อ/แผนกรูปหลังอัป · ค้นหาในอัลบั้ม · ล้างถังขยะอัตโนมัติ · แจ้งเตือน Drive ใกล้เต็ม

**🔵 รอหัวหน้าตัดสินใจ — Auto-backup Google Sheet (เสนอ 2026-07-23):**
> ⚠️ ยังไม่ทำ · **รอหัวหน้าโอเคก่อนค่อยเริ่ม**
- **ทำไมสำคัญ:** ทุกอย่างอยู่บัญชี Google เดียว (`tfp.data.mis`) — ถ้าบัญชี/ชีต/token พัง = ข้อมูล index หายหมด ไม่มีสำรอง
- **backup อะไร:** ตัว Sheet (index — เล็กมาก แต่หัวใจ) · รูปจริงใน Drive เอาไว้ทีหลัง
- **หลักการ:** ต้องอยู่**คนละบัญชี/คนละที่**กับของจริง
- **ทางที่เสนอ (เรียงตามแนะนำ):**
  1. **บัญชี Google ที่ 2** — ก๊อป Sheet ไปทุกสัปดาห์ · ปลอดภัย + ไม่เอา email/hash ไปวางที่เสี่ยง **← แนะนำ**
  2. GitHub Actions (มี keep-alive รันอยู่แล้ว) ดึง CSV แล้ว commit · ⚠️ **ต้องเป็น repo private ใหม่เท่านั้น** เพราะชีตมี email+hash รหัส · repo ปัจจุบัน public ห้ามวางเด็ดขาด
  3. ดาวน์โหลด .xlsx ลงไฟล์เซิร์ฟเวอร์บริษัทเอง · ง่ายสุดแต่ต้องจำเอง
- **ถ้าหัวหน้าโอเค:** Cowork เขียนสเปกให้ CLI ทำ (Cowork ไม่แก้โค้ดเอง)

**⚙️ ชีต production migrate ครบแล้ว** (รัน `ensure_schema()` ตัวจริงผ่าน stub — ทุกครั้งเช็คแล้วว่าคอลัมน์เดิมไม่ขยับ + จำนวนแถวเท่าเดิม)
- แท็บ **Users**: +`อีเมล`(6) +`ขอรีเซ็ต`(7) — บัญชี `pim` เดิมอยู่ครบ
- แท็บ **Activities**: +`การเข้าร่วม`(8) — 6 กิจกรรมเดิมค่าว่าง = **ยังต้องใช้รหัสเหมือนเดิมทุกอัน**
- งานข้อ 5 (2026-07-22) **ไม่มีคอลัมน์ใหม่ ไม่ต้อง migrate อะไรเพิ่ม**

**📖 ตำแหน่งเมนูที่ผู้ใช้ถามบ่อย:** ขอเปิดบัญชี admin / ลืมรหัส อยู่ในแถบพับ **`🔐 สำหรับ admin / ผู้ดูแลระบบ`** ล่างสุดของหน้าแรก (3 แท็บ: เข้าสู่ระบบ / ขอเปิดบัญชี admin / ลืมรหัสผ่าน) — ผู้ใช้เคยหาไม่เจอมาแล้ว 1 ครั้ง ถ้าถามอีกให้พิจารณาดึงออกมาเป็นปุ่มข้างนอก

---

## ✅ [แก้แล้ว] เว็บเคยรันโค้ดเก่า — Reboot แล้วเรียบร้อย (2026-07-22)

> **สรุปสั้น:** commit `37c0466` (แก้บั๊ก 8 ข้อ) push ขึ้น GitHub แล้วแต่ Streamlit Cloud ยังเสิร์ฟของเก่า · **หัวหน้ากด Reboot app → deploy ติดทันที** · เทสต์ B7 ซ้ำ **ผ่าน**
>
> **📌 บทเรียนที่ต้องจำ: push แล้วไม่ได้แปลว่า deploy แล้ว** — ครั้งหน้าหลัง push ให้เช็คบนเว็บว่าโค้ดใหม่ติดจริงก่อนเริ่มเทสต์ ไม่งั้นจะเทสต์โค้ดเก่าโดยไม่รู้ตัวแบบรอบนี้ (เสียเวลาไปครึ่งรอบ)
>
> **วิธีเช็คเร็วๆ ว่า deploy ติดหรือยัง:** สร้างบัญชีขอเปิดใหม่ 1 อัน → ถ้าบัญชี `รออนุมัติ` **ไม่โผล่**ในรายชื่อล่างของแท็บจัดการบัญชี = โค้ดใหม่ติดแล้ว

<details>
<summary>รายละเอียดตอนที่ยังพัง (เก็บไว้อ้างอิง)</summary>

> เจอตอนเทสต์ B7 บนเว็บจริง 2026-07-22

**อาการ:** สร้างบัญชี `claude.qa.test@tfpoem.com` (สถานะ `รออนุมัติ`) แล้วเข้าแท็บจัดการบัญชี admin ในฐานะ superadmin — บัญชีนั้น **โผล่ทั้ง 2 ที่**:
1. ในกล่อง "⏳ มี 1 คนขอเปิดบัญชี รอคุณตั้งรหัสให้" (ถูกต้อง)
2. **ใน "รายชื่อบัญชี admin ทั้งหมด" ด้านล่าง พร้อมปุ่ม `▶️ เปิดใช้งาน`** ← **คือบั๊ก #2 เป๊ะๆ ที่บอกว่าแก้ไปแล้ว**

**ตรวจสอบแล้ว — โค้ดถูก แต่ของที่รันอยู่เก่า:**
```
git log -1        → 37c0466 "แก้บั๊ก 8 ข้อจาก code review (2026-07-22)"
git ls-remote     → refs/heads/main = 37c0466   ← GitHub มีโค้ดใหม่แล้ว
git branch -vv    → main 37c0466 [origin/main]  ← ไม่มี commit ค้าง ไม่ต้อง push
page_activity_superuser.py:474
    admins = admins[admins["สถานะ"].astype(str).str.strip() != USER_PENDING]   ← ตัวกรองอยู่ครบ
```
→ **โค้ดใน repo ถูกต้องและ push ขึ้น GitHub แล้ว แต่ Streamlit Cloud ยังเสิร์ฟเวอร์ชันเก่าอยู่**

**✅ วิธีแก้ (ไม่ต้องแตะโค้ด):** Streamlit Cloud → แอป `image-warehouse-mis` → ⋮ → **Reboot app** → รอ ~30 วิ แล้วเทสต์ B7 ซ้ำ

**⚠️ ผลกระทบต่อผลเทสต์ก่อนหน้าในไฟล์นี้ — ต้องอ่านให้ดี:**
- **ทุกอย่างที่เทสต์บนเว็บวันนี้ = เทสต์โค้ด *ก่อน* แก้บั๊ก 8 ข้อ**
- แปลว่าที่จดไว้ว่า "ยืนยันบั๊ก #5 แก้ถูก" (ถังขยะ / กริดรูปซ้ำ) **ยังไม่ได้ยืนยันจริง** — มันไม่พังเพราะข้อมูลจริงไม่เข้าเงื่อนไขบั๊ก (ไม่มีแถวลิงก์ว่าง 2 แถวขึ้นไป) ไม่ใช่เพราะโค้ดใหม่ทำงาน
- **ข่าวดี:** ผลเทสต์ B2/B3/B4/B5/C1-C5/D1-D8 ที่ผ่านหมด = ฟีเจอร์พื้นฐานบนโค้ดเก่าทำงานดีอยู่แล้ว → หลัง reboot ควรเทสต์ซ้ำเฉพาะข้อที่เกี่ยวกับบั๊ก 8 ข้อ (#1 cookie เพี้ยน · #2 pending · #4 viewer ถูกถอนสิทธิ์ · #5 คีย์ปุ่มซ้ำ)

</details>

### ✅ เทสต์ซ้ำหลัง Reboot (2026-07-22, superadmin) — ผ่าน 4/4

| # | เทสต์ | ผล |
|---|-------|-----|
| E1 | **B7 / บั๊ก #2** — บัญชี `claude.qa.test@tfpoem.com` (รออนุมัติ) | ✅ **หายจาก "รายชื่อบัญชี admin ทั้งหมด" แล้ว** เหลือแค่ `pim` + `miscen06` · อยู่เฉพาะในกล่อง "⏳ มี 1 คนขอเปิดบัญชี รอคุณตั้งรหัสให้" · **ไม่มีปุ่ม ▶️ เปิดใช้งาน ให้กดพลาดอีก** |
| E2 | คิวลืมรหัส | ✅ กล่องแดง "🔑 มี 1 คนลืมรหัส รอคุณตั้งรหัสใหม่ให้" · `miscen06@tfpoem.com` · ขอเมื่อ 2026-07-22 14:43:38 |
| E3 | **หารูปซ้ำ (47 รูป) แล้วกดลบ 1 ใบ** | ✅ พบ 1 กลุ่ม 2 รูป → กด 🗑️ ลบใบนี้ → **47 → 46 รูป** · สแกนซ้ำอีกที ขึ้น "ไม่พบรูปซ้ำในชุดนี้" · **ไม่มี DuplicateElementKey** → ยืนยันบั๊ก #5 แก้ถูกจริงบนโค้ดใหม่ |
| E4 | **ถังขยะ → กู้คืน (ปิดวง B3)** | ✅ ถังขยะ 1 → 2 รูป · แถวใหม่ขึ้น `test02 ใครก็ได้ · 2026-07-22 15:01:57 · โดย superadmin` (จดคนลบถูก) · กด ♻️ กู้คืน → ถังขยะกลับเป็น 1 รูป · **ปุ่ม 4 ปุ่ม (กู้คืน/ลบถาวร × 2 รูป) ไม่ชนกัน** |

**📋 Log จดครบทุกการกระทำ (ยืนยันจากแท็บ Log, 50 รายการ):**
```
15:03:28  superadmin             superuser  กู้คืนรูป          test02_ใครก็ได้_003_140942.jpg  test02 ใครก็ได้
15:01:59  superadmin             superuser  ลบรูป(ถังขยะ)     test02_ใครก็ได้_003_140942.jpg  test02 ใครก็ได้
14:43:38  miscen06@tfpoem.com    admin      ขอรีเซ็ตรหัส      —                              —
14:39:46  claude.qa.test@...     guest      ขอเปิดบัญชี admin  claude.qa.test@tfpoem.com      —
14:13:15  Ben                    user       อัปโหลดรูป         test03_004_141307.jpg          test03
```
→ **ข้อมูลกลับสภาพเดิมหมดแล้ว** (ลบแล้วกู้คืน 46 → 47 รูป) เหลือแค่ Log 2 บรรทัดที่เพิ่มมา ถูกต้องตามดีไซน์

**⏳ 3 ข้อที่ Cowork ทำเองไม่ได้ — ต้องให้คนทำ:**
1. **ตั้งรหัสใหม่ให้ `miscen06` เพื่อล้างคิวลืมรหัส** — Cowork **ห้ามพิมพ์รหัสผ่านลงช่องใดๆ** (ข้อจำกัดด้านความปลอดภัย) · ให้หัวหน้าพิมพ์เอง แล้วเช็คว่า **คิวหายเอง + Log ขึ้น "ตั้งรหัสใหม่ให้ admin"**
2. **บั๊ก #1 (cookie เพี้ยน → หน้าแรกต้องไม่ crash)** — ต้องแก้ cookie ในเบราว์เซอร์ให้เป็นค่าขยะ · Cowork ถูกบล็อกไม่ให้อ่าน/เขียน cookie · ทำเองได้ที่ DevTools → Application → Cookies → แก้ค่า token มั่วๆ → รีเฟรช (ต้องแค่ "ไม่จำ login" ไม่ใช่จอ error)
3. **บั๊ก #4 (viewer ถูกถอนสิทธิ์แล้วยังดูได้ ~1 วัน)** — ต้องแชร์อัลบั้มเฉพาะคน → login เป็น viewer ติ๊กจำฉันไว้ → ถอนสิทธิ์ → refresh → ต้องหลุด

---

## 👻 ตัด "เงา" ระหว่างโหลด — #D (2026-07-22)

> **โจทย์จากหัวหน้า:** *"แก้เรื่องเงาเลยไหม มันเห็นแล้วก็ขัดใจ ดูไม่ pro เลย"*
> **ทำทั้ง 2 ทางที่เคยเสนอไว้** (ก = กลบเงา · ข = ทำให้ช่วงรอสั้นลงจริง) เพราะทาง ก อย่างเดียวจะกลายเป็น "หน้านิ่งเฉยๆ" ตอนโหลดนาน

**ทาง ก — `_inject_css()` ใน `app.py` (ฉีดทันทีหลัง `set_page_config`)**
- Streamlit ทำเนื้อหาเดิม "จางลง" ทุกครั้งที่ rerun (ใส่ `data-stale="true"` แล้วลด opacity) → นั่นคือเงาที่เห็น
- บังคับ `opacity: 1 !important` + `filter: none` + `transition: none` (ไม่งั้นยังเห็นวูบตอน fade)
- เขียนเผื่อ selector หลายเวอร์ชัน (`[data-stale]` · `.element-container` · `stElementContainer` · `.stale-element`) — ถ้า Streamlit เปลี่ยนชื่ออีก อย่างมากคือเงากลับมา **ไม่ทำให้แอปพัง**
- ⚠️ **ตั้งใจไม่ซ่อน "ตัวบอกสถานะมุมขวาบน" ของ Streamlit** — ตัดเงาแล้วต้องเหลืออะไรบอกว่ากำลังโหลด ไม่งั้นคนกดซ้ำ

**ทาง ข — ลดการยิง Google ระหว่างใช้งาน (ช่วงรอสั้นลง = เงาโผล่สั้นลงเอง)**
| อะไร | เดิม | ใหม่ | เหตุผล |
|------|------|------|--------|
| `check_connection()` | **ไม่ cache เลย** | cache 5 นาที | อยู่ใน sidebar คลังทั่วไป → ยิง Google **ทุก rerun** (ทุกครั้งที่กดปุ่ม/พิมพ์) ทั้งที่ชื่อชีตแทบไม่เปลี่ยน |
| `load_data()` | 60 วิ | **300 วิ** | ตัวหนักสุด อ่านทั้งชีต |
| `load_activities` · `load_users` · `load_shares` | 60 วิ | **180 วิ** | เปลี่ยนไม่บ่อย |

- 🔒 **ปลอดภัยเพราะทุกจุดที่แก้ข้อมูลสั่งล้าง cache เองอยู่แล้ว** (อัป/ลบ/กู้คืน/เผยแพร่/อนุมัติ/ตั้งรหัส) และ cache เป็นของทั้งแอป → คนอื่นอัปรูป ทุกคนเห็นทันทีเหมือนเดิม **ไม่ได้รอ TTL**
- ที่ค้างได้จริงมีทางเดียว: ไปแก้ชีตด้วยมือใน Google Sheets → กด **🔄 รีเฟรชข้อมูล** (ปุ่มเดิมยังอยู่ทั้ง sidebar และ top bar)

**✅ ทดสอบในเครื่องผ่าน 79/79** — CSS ถูกฉีดก่อน render · ไม่ได้ซ่อนตัวบอกสถานะ · TTL ตรงตามตาราง · **มุตเตชันยังล้าง cache ครบ** (เทสต์นับไว้ กันเผลอถอดออกแล้วข้อมูลค้างนานขึ้นตาม TTL ใหม่)
**⏳ ต้องดูบนเว็บจริง** — เป็นเรื่อง CSS/เบราว์เซอร์ ถ้ายังเห็นเงาอยู่ให้บอก จะไล่หา selector จริงจาก DevTools ของเวอร์ชันที่ deploy อยู่

---

## ⬆️ login แล้วหน้าเด้งขึ้นบนสุด (2026-07-22)

> **โจทย์จากหัวหน้า:** *"ทำแบบให้ login เข้าไปแล้ว หน้าเด้งไปบนสุดได้ไหม"*
> **ต้นเหตุ:** Streamlit ไม่รีเซ็ตตำแหน่งเลื่อนเวลาเปลี่ยนหน้า (มันคือ rerun หน้าเดิม) → เลื่อนลงไปกรอกรหัสในแถบ admin ล่างสุด พอเข้าได้ หน้าถัดไปเปิดมาค้างกลางจอ เหมือนโหลดไม่ครบ

- **`auth.scroll_to_top()`** — ฝัง `<script>` (iframe สูง 0) สั่งเลื่อน "หน้าแม่" ขึ้นบนสุด · เรียกบนสุดของ `app.main()`
- **ทำงานครั้งเดียว** ผ่านธง `session_state["scroll_top"]` ที่ `_do_login()` ตั้งให้ (ครอบทุกทางเข้า: รหัสคลังทั่วไป / กิจกรรม / อัลบั้ม / admin / superuser / QR deep-link) + ตอนกดปุ่มกิจกรรม 🌐 และตอนรหัสกิจกรรมถูก (ไปหน้ากรอกชื่อ) — **ไม่ยิงทุก rerun**
- **ยิงซ้ำ 3 จังหวะ** (ทันที / 60ms / 250ms) เพราะเนื้อหาจริงเรนเดอร์เสร็จทีหลัง ยิงครั้งเดียวมักไม่ทัน
- **เผื่อคอนเทนเนอร์หลายชื่อ** (`window` · `scrollingElement` · `body` · `section.main` · `stMain` · `stAppViewContainer`) เพราะ Streamlit เปลี่ยนชื่อ DOM บ่อยตามเวอร์ชัน
- **ครอบ try/except ทั้งก้อน** — ถ้าฝัง component ไม่ได้ก็แค่ "ไม่เด้ง" แอปไม่พัง

**✅ ทดสอบในเครื่องผ่าน 70/70** — ไม่ได้เพิ่ง login ไม่ฝัง script · เพิ่ง login ฝัง 1 ครั้งแล้วล้างธง · rerun ถัดไปไม่ฝังซ้ำ · `_do_login` ตั้งธงจริง
**⏳ ต้องดูบนเว็บจริง** — เป็นการสั่งงานฝั่งเบราว์เซอร์ เทสต์ในเครื่องยืนยันได้แค่ว่า "ฝัง script ถูกจังหวะ" ยืนยันว่าเลื่อนจริงไม่ได้ · ถ้ายังไม่เด้ง ให้บอกมา จะเปลี่ยนไปวิธี anchor + `scrollIntoView` แทน

---

## 🔎 Bug-hunt รอบเช้า 2026-07-23 (เทสต์บนเว็บจริง, superadmin) — ผ่าน 3/3 + เจอ UX 2 จุด

**✅ ผ่าน:**
- **F1 สลับโหมดกิจกรรม 🌐↔🔒** — สลับ test03 ไป "ใครก็ส่งได้" แล้วสลับกลับ "ต้องมีรหัส" ได้ครบ 2 ทาง ไม่ crash · คืนสภาพเดิมแล้ว
- **F2 ZIP ทั้งอัลบั้ม** — กด "เตรียมไฟล์ ZIP" คลังภาพทุกกิจกรรม 46 รูป → ปุ่ม "ดาวน์โหลด .zip" โผล่ ไม่ error (ไม่ได้กดดาวน์โหลดจริง)
- **F3 สลับแท็บรัวๆ (#J)** — คลิก 5 แท็บติดกันเร็วๆ (Log→ถังขยะ→คลังทั่วไป→Dashboard→จัดการบัญชี) **ไม่ค้าง ไม่จอแดง APIError ไม่ 429** → ยืนยัน #J ลดการยิงชีตได้ผลจริง
- ยืนยันเพิ่ม: **#E deploy แล้ว** (กิจกรรม 🌐 อยู่บนสุดของหน้าแรก) · **#F** ป้าย checkbox เป็น "ให้ฉันอยู่ในระบบต่อ" · **#H** รูปเท่ากัน · **#G** ไม่มี error เหลืองแวบ

**🧹 งานบ้าน:** `claude.qa.test@tfpoem.com` **ถูกอนุมัติเป็น admin แล้ว** (status ใช้งาน โผล่ในรายชื่อ) — เดิมเป็นบัญชีทดสอบ ควรกดลบทิ้ง

### ✅ #K [แก้แล้ว 2026-07-24] แท็บเด้งกลับ Dashboard หลังกดปุ่ม action — st.tabs ไม่จำ selection ข้าม rerun

> **วิธีแก้ (เลือกทาง 1 = เก็บ selection ใน session_state):** เพิ่ม `nav_tabs(key, labels)` ใน `google_utils.py` — ใช้ `st.radio(horizontal=True, label_visibility="collapsed")` เก็บหน้าที่เลือกไว้ใน `st.session_state[key]` แล้วคืน label ให้ผู้เรียกไป `if/elif` เรนเดอร์เฉพาะหน้านั้น
> - แทน `st.tabs` 3 จุด: `app.py` (คลังทั่วไป, key `general_nav`) · `page_activity_superuser.py` (7 แท็บ, key `su_nav`) · `page_activity_admin.py` (4 แท็บ, key `adm_nav`)
> - **ผลพลอยได้:** เรนเดอร์เฉพาะหน้าที่เปิด (เดิม st.tabs รันทุกแท็บทุก rerun) → เบาลง ตรงกับที่เคยเสนอไว้เรื่องลด overhead
> - **ทำไมใช้ `st.radio` ไม่ใช่ `segmented_control`:** radio มีทุกเวอร์ชัน + deselect ไม่ได้ (คืน label เสมอ ไม่มีจอว่าง) ตามหลัก "ไม่พัง"
> - กัน label ค้าง: ถ้า `session_state[key]` ไม่อยู่ใน labels (เปลี่ยนชื่อ/จำนวนแท็บ) รีเซ็ตเป็นอันแรก
> - แท็บย่อย (superuser `_render_general` · หน้า user · login ใน auth.py) ยังเป็น `st.tabs` — คนละจอ ไม่ใช่จุดเจ็บที่รายงาน เลยยังไม่แตะ
> - ทดสอบผ่าน stub: first→A · เลือก B แล้ว rerun ยังอยู่ B · label ค้างรีเซ็ตเป็นอันแรก · ทุกหน้า import ผ่าน
> - ⏳ **เหลือเทสต์บนเว็บ:** เข้าแท็บ "จัดการกิจกรรม" กดปุ่มเปิด/ปิด/เปลี่ยนโหมด → ต้องอยู่แท็บเดิม ไม่เด้งกลับ Dashboard

<details>
<summary>รายละเอียดตอนที่ยังเป็นบั๊ก (เก็บไว้อ้างอิง)</summary>

### 🟡 #K แท็บเด้งกลับ Dashboard หลังกดปุ่ม action — st.tabs ไม่จำ selection ข้าม rerun

- **อาการ (เจอ 2026-07-23):** อยู่แท็บ "จัดการกิจกรรม" กดปุ่ม "เปลี่ยนเป็นใครก็ส่งได้" → หน้า rerun แล้ว **เด้งกลับไปแท็บ Dashboard เอง** ต้องกดกลับมาแท็บเดิม + เลื่อนหาที่เดิมใหม่
- ⚠️ **ไม่สม่ำเสมอ:** เกิดรอบแรก แต่รอบสอง (สลับกลับ) กลับอยู่แท็บเดิม — เดาว่าขึ้นกับว่า frontend "จำ" tab ทันหรือไม่ก่อน rerun
- **ต้นเหตุ:** Streamlit `st.tabs` เก็บ selection ที่ฝั่ง frontend เท่านั้น ไม่ผูกกับ session_state → ปุ่มที่เรียก `st.rerun()` ทำให้ Python วาดใหม่จาก tab แรก (Dashboard) เสมอ ส่วน frontend บางจังหวะ sync ทัน บางจังหวะไม่ทัน
- **กระทบ:** superuser/admin ที่ต้องสลับโหมด/ปิด/เผยแพร่หลายกิจกรรมติดกัน จะโดนเด้งออกทุกครั้ง เสียเวลา
- **แนวทางแก้ (ให้ CLI เลือก):**
  1. เปลี่ยนจาก `st.tabs` เป็น **`st.radio` / `st.segmented_control` เก็บ selection ใน `session_state`** แล้ว render เนื้อหาตามค่าที่เลือก → คุม active view เองข้าม rerun ได้ (งานปานกลาง แต่แก้ถาวร)
  2. หรือใช้ `streamlit-option-menu` (ถ้ายอมเพิ่ม dep)
  3. ทางเบาสุด: ปุ่ม action ที่จำเป็นต้องอยู่หน้าเดิม ใช้ **`@st.fragment`** ครอบส่วนนั้น เพื่อ rerun เฉพาะ fragment ไม่ทั้งหน้า (Streamlit ≥1.33) → ไม่หลุด tab
- ℹ️ ความร้ายแรง: ต่ำ-กลาง (ใช้งานได้ แต่หงุดหงิด)

</details>

### ✅ #L [แก้แล้ว 2026-07-24] ป้ายสถานะกิจกรรมขัดกัน: "เปิด" + "ปิดอัตโนมัติแล้ว (ครบ 7 วัน)" พร้อมกัน

> **วิธีแก้:** เพิ่ม `activity_status_label(row)` ใน `google_utils.py` รวมสถานะเป็นป้ายเดียวสื่อชัด:
> - ปิดในชีต → `ปิด` · เปิดปกติ → `เปิด` · **เปิดแต่ครบ 7 วัน → `ปิดรับรูปแล้ว (ครบ 7 วัน) · ยังเปิดให้ดูย้อนหลัง`**
> - แทนที่โค้ดเดิม (`auto_closed`/`note` โชว์ "เปิด" คู่ "⏰ ปิดอัตโนมัติแล้ว") ทั้ง 2 หน้า: superuser + admin
> - เลือกคำที่สื่อทั้ง 2 อย่าง: **ส่งรูปไม่ได้แล้ว** (auto-close 7 วัน) แต่ **ยังเปิดให้ดูย้อนหลัง** (viewer เข้าดูได้) — ตรงกับ logic จริง
> - ⏳ **เหลือเทสต์บนเว็บ:** กิจกรรมที่ครบ 7 วัน (morning talk / จป. / hr / แห่เทียน) ต้องขึ้นป้ายเดียว ไม่ขัดกันแล้ว

<details>
<summary>รายละเอียดตอนที่ยังเป็นบั๊ก (เก็บไว้อ้างอิง)</summary>

### 🟡 #L ป้ายสถานะกิจกรรมขัดกัน: "เปิด" + "ปิดอัตโนมัติแล้ว (ครบ 7 วัน)" พร้อมกัน

- **อาการ:** ในแท็บจัดการกิจกรรม หลายกิจกรรม (morning talk / จป. / hr / แห่เทียน) ขึ้น `สถานะ: เปิด · 🕐 ปิดอัตโนมัติแล้ว (ครบ 7 วัน)` — อ่านแล้วงงว่าตกลง "เปิด" หรือ "ปิด"
- **น่าจะเป็นดีไซน์:** คอลัมน์ `สถานะ` ในชีตยังเป็น "เปิด" แต่ระบบมี logic auto-close 7 วัน (ไม่ให้ส่งรูปเข้าถ้าเกิน 7 วัน) → เลยโชว์ทั้งคู่ · **ไม่ใช่บั๊ก data แต่เป็นบั๊กการสื่อสาร**
- **แนวทางแก้:** ถ้ากิจกรรมเกิน 7 วัน (auto-closed) ให้เปลี่ยนป้ายหลักเป็น **`สถานะ: ปิดรับรูปแล้ว (ครบ 7 วัน)`** สีเทา แทนการโชว์ "เปิด" คู่กับ "ปิดอัตโนมัติ" · หรือถ้าตั้งใจให้ยังดูรูปได้แต่ส่งไม่ได้ ใช้คำให้ชัด เช่น `เปิดให้ดู · ปิดรับรูปแล้ว`
- ℹ️ ความร้ายแรง: ต่ำ (แค่ชวนสับสน ไม่พัง)

</details>

---

## 📝 งานให้ CLI แก้ (เจอจากการเทสต์ 2026-07-22)

> ⚠️ **หมายเหตุแบ่งงาน:** Cowork = review + test + จดลงไฟล์นี้เท่านั้น · **CLI = คนแก้โค้ด**
>
> ### ✅ CLI แก้ครบแล้ว 5 ข้อ (#A #E #F #B #C) — commit `64627c3` · เทสต์ในเครื่องผ่าน 63/63
> - **#A** ปุ่ม "← เปลี่ยนรหัส" หายแล้ว (Cowork แก้ไว้ ไม่ revert) · **แถมแก้ต้นตอที่ลึกกว่า:** `_render_name_step()` แยกข้อความตามทางเข้าแล้ว — มาทางปุ่ม 🌐 (ไม่เคยกรอกรหัส) ขึ้น `📤 กำลังส่งรูปเข้า: <ชื่อกิจกรรม>` · มาทางรหัส/QR ขึ้น `✅ รหัสถูกต้อง — ...` เหมือนเดิม (ธง `no_code` ใน `pending_act`)
> - **#E** ย้าย 🌐 ขึ้นบนสุดแล้ว: `title` → กิจกรรมไม่ต้องใช้รหัส → เส้นคั่น → **"มีรหัสอยู่แล้ว?"** + ช่องกรอกรหัส → อัลบั้มสาธารณะ → แถบ admin · **เลี่ยงกับดัก `NameError: remember`** ด้วยการเรียก `_render_open_activities()` ไม่ส่งอาร์กิวเมนต์ (default True) · ฟังก์ชันคืน bool บอกว่ามีกิจกรรมไหม จะได้ไม่ขีดเส้นคั่นลอยๆ ตอนไม่มีกิจกรรม
> - **#F** ป้าย checkbox → **"ให้ฉันอยู่ในระบบต่อ"** ทั้ง 2 จุด · เลือกทาง (ข) ตามที่แนะนำ: ย้ายจำนวนวันไป tooltip (`ในเครื่องนี้เท่านั้น · อยู่ได้ 1 วัน · ...`) **ไม่ทิ้ง `REMEMBER_LABEL`** · `value=` ยังต่างกัน (หน้าแรก True / admin False) มีเทสต์คุมไว้
> - **#B** Log กรองแล้วไม่เหลือ → `st.info("ไม่พบรายการที่ตรงกับเงื่อนไข — ลองล้างคำค้นหรือเปลี่ยนตัวกรองดูครับ")` แทนตารางเปล่าที่ขึ้นคำว่า `empty`
> - **#C** ช่องเลขหน้าเปลี่ยนป้ายเป็น `หน้า (1–N)` + `help="มีทั้งหมด N หน้า — พิมพ์ได้ตั้งแต่ 1 ถึง N"`
> - **#D (เงา)** ✅ **แก้แล้ว** ทำทั้งทาง ก + ข (ดูหัวข้อ "👻 ตัดเงาระหว่างโหลด" ด้านบน)
>
> ### ✅ งานใหม่ (เจอเช้า 2026-07-23) — #G #H · CLI แก้แล้ว commit `74aeb28` push แล้ว รอเทสต์บนเว็บ
> - **#G** ✅ แก้แล้ว — เลิกใช้ `@st.cache_resource` ครอบ CookieManager เปลี่ยนเป็น memoize ผ่าน `st.session_state` (ไม่โดน `CachedWidgetWarning` แล้ว กันสร้างซ้ำได้เหมือนเดิม)
> - **#H** ✅ แก้แล้ว — เลือกทาง (2) crop ตอนทำ thumbnail: เพิ่ม `image_utils.crop_to_box()` (center-crop ด้วย `ImageOps.fit`) + `google_utils.get_thumbnail()` แล้วกริดทั้ง 8 จุดเรียก `get_thumbnail()` แทน `get_image_bytes()` · **ปุ่มดาวน์โหลดยังได้ไฟล์เต็ม (ใช้ URL Drive ตรง) · QR + พรีวิว 3 จุดไม่โดนแตะ**
> - 🧪 เทสต์ในเครื่องผ่าน 9/9 (crop รูปแนวตั้ง/แนวนอน/จัตุรัส → 400×300 เท่ากันหมด · bytes เสียคืนของเดิมไม่ throw · ทุกโมดูล import ผ่าน · cookie mgr memoize ถูก)
> - ✅ **เทสต์บนเว็บแล้ว (2026-07-23, superadmin) — ผ่าน:**
>   - **#G** login → **หัวหน้ายืนยัน "หายไปแล้ว" ไม่มี error เหลืองแวบ** ✅ (เหลือยืนยัน refresh ไม่หลุด)
>   - **#H** คลังภาพทุกกิจกรรม 46 รูป → **รูปทุกใบสูงเท่ากันเป๊ะ เรียงเป็นตาราง** ปุ่มดาวน์โหลด/เผยแพร่/ลบ ตรงกันหมด · รูปแนวตั้ง (เก้าอี้) ถูก center-crop พอดีกรอบ ✅ (เหลือยืนยัน QR สแกนได้ + ดาวน์โหลดไฟล์เต็ม)
>   - **#I** กด "หารูปซ้ำในชุดนี้" (46 รูป) → ขึ้น "ไม่พบรูปซ้ำในชุดนี้" สแกนจบ **ไม่มีจอแดง APIError** ✅ (แต่ยังไม่ได้ทดสอบภาระหนัก/ลบรัวๆ ที่เป็นตัวจุด 429 เดิม — ดูหมายเหตุ)
>   - ⏳ **#J + #I ภาระหนัก ยังไม่ยืนยันเต็ม:** ต้นเหตุ 429 เดิมคือ "ลบรูปรัวๆ" ยิงชีต 50-60/นาที · เทสต์จริงต้องลบหลายใบติดๆ = แตะข้อมูลจริง **Cowork ไม่ทำเองโดยไม่ขออนุญาต** · ถ้าอยากยืนยัน ให้ลบรูปทดสอบ 3-4 ใบติดกันแล้วดูว่าไม่ crash + เร็วขึ้น

### ✅ #J ลดการยิงอ่าน Google Sheets — ต้นตอที่ทำให้เสี่ยง 429 (2026-07-23) — commit `086e050`

- **ที่มา:** ตามหา #I ต่อ — "เช็ครูปซ้ำ" ยิงอ่านชีตถี่ไหม · **ตัวสแกนเองสะอาด** (`group_duplicates` ทำในหน่วยความจำล้วน ใช้คอลัมน์ phash ที่เก็บไว้ ไม่อ่านชีตเลย)
- **ต้นตอจริง:** ปุ่มลบหลังสแกน + จุด mutation อีกหลายจุด เรียก `st.cache_data.clear()` (**ล้าง cache ทั้งแอป**) ทั้งที่ฟังก์ชันใน `google_utils` ล้างเจาะจงให้อยู่แล้ว → ล้างเหมารวม 1 ครั้ง = rerun ถัดไปอ่านชีตใหม่ ~5-6 ครั้ง (main/Activities/Users/Shares/Log) **+ ดาวน์โหลด thumbnail ใหม่ทั้งกริด** · `st.tabs` เรนเดอร์ทั้ง 6 แท็บทุก rerun ยิ่งขยายผล → ลบรูปรัวๆ = อ่านชีต 50-60 ครั้ง/นาที ชน quota 60/นาที → 429
- **✅ แก้แล้ว:**
  1. `append_row` ล้าง `load_data` เอง (เดิมไม่ล้าง — เป็นจุดเดียวที่ต้องพึ่ง nuclear) → ตอนนี้ mutation **ทุกตัว** ล้างเจาะจงเองครบ
  2. **ถอด `st.cache_data.clear()` เหมารวม 12 จุด** (superuser 4 · admin 5 · gallery 1 · user 1 · upload 1) เพราะซ้ำซ้อนกับ self-clear ในฟังก์ชัน
  3. **เก็บไว้ 2 จุด** = ปุ่ม "🔄 รีเฟรชข้อมูล" (`app.py`/`auth.py`) — ตั้งใจให้ล้างทั้งหมดตอนคนแก้ชีตด้วยมือ
- **ผล:** ลบ/อัป/กู้คืน 1 ครั้ง → อ่านชีตใหม่เกือบ 0 (เฉพาะที่เปลี่ยนจริง) + ไม่ต้องโหลด thumbnail ใหม่ทั้งกริด → เร็วขึ้น + เงาสั้นลง + เสี่ยง 429 น้อยลงมาก
- 🧪 เทสต์ในเครื่องผ่าน 26/26 (append_row/append_activity_row ล้าง load_data · ทุก page import ผ่าน · #I retry + #H crop ไม่ถอย)

### ✅ #I เว็บ crash เต็มจอตอนโหลดข้อมูล = `APIError` จาก Google Sheets (2026-07-23) — commit `15834a2`

- **อาการ (หัวหน้าเจอตอนใช้ "เช็ครูปซ้ำ"):** จอแดง traceback เต็มหน้า `raise APIError(response)` — ต้นทาง `_render_dashboard()` → `load_activities()` → `get_all_records()` (Google Sheets API)
- **ต้นเหตุ (ความเปราะเดิม ไม่เกี่ยวกับ #G/#H — คนละ API, Sheets ≠ Drive):**
  - `_retry` เดิมจับแค่ `ConnectionError`/`TimeoutError` — **ไม่จับ `APIError`** (429 โควตาเกิน/นาที · 500-504 ฝั่ง Google ล่มชั่วคราว)
  - **read ทั้ง 6 ตัว** (`check_connection`/`load_log`/`load_data`/`load_activities`/`load_shares`/`load_users`) ยิง `get_all_records()` **โดยไม่ผ่าน `_retry` เลย** → เจอ blip ชั่วคราวปุ๊บเด้งเต็มจอ (write ทุกตัวมี `_retry` อยู่แล้ว แต่ read ตกหล่น)
- **✅ แก้แล้ว:**
  1. `_retry` จับ `APIError` ที่ status ∈ {429,500,502,503,504} → backoff 1.5/3/4.5s แล้วลองใหม่ (สูงสุด 4 ครั้ง) · **403 สิทธิ์/404 ชีตหาย = โยนทันที ไม่เสียเวลาลองซ้ำ**
  2. ครอบ read ทั้ง 6 ตัวด้วย `_retry`
- 🧪 เทสต์ในเครื่องผ่าน 13/13 (429/503 retry สำเร็จ · 403/404 โยนทันที · ConnectionError เดิมไม่พัง · แยก retryable ถูก)
- ℹ️ **ถ้ายังเจอซ้ำหลัง deploy** = โควตาเกินแบบต่อเนื่อง (ยิงถี่จริงๆ) หรือ 403 สิทธิ์ — **ขอ traceback บรรทัดสุดท้าย (โค้ด APIError) มาดู** จะได้แก้ตรงจุด (ลดจำนวนอ่าน / เช็คสิทธิ์)

### 🟠 #G error แวบตอน login = `CachedWidgetWarning` — `session_store.py:111 _get_manager()`

- **อาการ (หัวหน้าเจอ 2026-07-23 เช้า):** ตอน login มีกล่อง error สีเหลืองขึ้นมา **แวบเดียวแล้วหายเอง**
- **ข้อความจริง (จากภาพหน้าจอ):** *"Your script uses a widget command in a cached function (function decorated with st.cache_data or st.cache_resource). This code will only be called when we detect a cache 'miss', which can lead to unexpected results."* · traceback ชี้ `CookieManager.__init__` → `components/v1/custom_component` → `caching/cache_utils.py _handle_cache_miss`
- **ต้นเหตุ:** `_get_manager()` เอา `@st.cache_resource` ครอบการสร้าง `stx.CookieManager` (เป็น widget/component) — Streamlit ห้ามสร้าง widget ในฟังก์ชันที่ถูก cache
  ```python
  # session_store.py:111-113
  @st.cache_resource(show_spinner=False)
  def _get_manager():
      import extra_streamlit_components as stx
      return stx.CookieManager(key=_COOKIE_KEY)      # ← widget ใน cached fn = ต้นเหตุ warning
  ```
- **ทำไมแวบเดียวแล้วหาย:** warning ขึ้นเฉพาะตอน **cache miss** (สร้าง manager ครั้งแรก) · พอ cache ติดแล้วรอบถัดไปไม่เรียกซ้ำ → warning หาย · ตรงกับข้อความ Streamlit เป๊ะ
- **⚠️ ที่ต้องระวัง:** เดิมใส่ `@st.cache_resource` เพื่อ**กันสร้าง CookieManager ซ้ำจน key ชน** — ถ้าเอา decorator ออกเฉยๆ อาจเจอ `DuplicateWidgetID` แทน
- **แนวทางแก้ (ให้ CLI เลือก):**
  1. memoize ผ่าน `st.session_state` แทน cache (ไม่ใช่ cache → ไม่โดน warning และยังกันซ้ำได้):
     ```python
     def _get_manager():
         if "_cookie_mgr" not in st.session_state:
             import extra_streamlit_components as stx
             st.session_state["_cookie_mgr"] = stx.CookieManager(key=_COOKIE_KEY)
         return st.session_state["_cookie_mgr"]
     ```
  2. หรือคงไว้แต่เรียก manager เฉพาะตอน save/clear cookie — เพราะ `load()` ใช้ `st.context.cookies` อ่านตรงอยู่แล้ว ไม่ต้องพึ่ง manager
- **🧪 เทสต์หลังแก้:** login → ไม่มี error เหลือง · ติ๊ก "ให้ฉันอยู่ในระบบต่อ" → refresh → ไม่หลุด (cookie ยังเขียน/อ่านได้)
- ℹ️ ความร้ายแรง: **ต่ำ (แค่ warning ไม่พัง)** แต่หัวหน้าเห็นแล้วตกใจ ควรเก็บให้เรียบร้อย

### 🟠 #H รูปในคลังภาพขนาดไม่เท่ากัน อยากให้เท่ากันหมด — ทุกหน้าที่โชว์กริดรูป

- **โจทย์จากหัวหน้า (2026-07-23):** *"ดูรูปทั้งหมดแล้วรูปมันไม่เท่ากัน อยากแก้ให้รูปดูขนาดเท่ากัน แบบรูปที่ 3"* (รูปที่ 3 = แนวนอนพอดีกรอบ · ที่ไม่เท่าคือรูปแนวตั้งสูงกว่าเพื่อน แถวเลยเหลื่อม)
- **ต้นเหตุ:** ทุกกริดใช้ `st.image(..., width="stretch")` — ยืดกว้างเต็มคอลัมน์แต่**คงสัดส่วนเดิม** → รูปแนวตั้งสูงกว่าแนวนอน → กริดไม่เรียง
- **จุดที่ต้องแก้ (8 ที่ แพตเทิร์นเดียวกัน):**
  ```
  page_activity_admin.py:276, 344, 530     (คลัง / ถังขยะ / สแกนรูปซ้ำ)
  page_activity_superuser.py:337, 409       (คลัง / ถังขยะ)
  page_activity_user.py:72                  (รูปของฉัน)
  page_activity_viewer.py:61                (อัลบั้มที่แชร์)
  page_gallery.py:280                       (คลังทั่วไป)
  ```
  ⚠️ **อย่าแตะ 3 จุดนี้ (ไม่ใช่กริด จะเสียถ้าโดน crop):** `page_activity_admin.py:420` (QR width=200) · `page_activity_user.py:115` + `page_upload.py:54` (พรีวิวก่อนส่ง)
- **แนวทางแก้ (ให้ CLI เลือก) — เป้าหมาย "ครอบกรอบเท่ากันแล้ว crop ส่วนเกิน" แบบ object-fit: cover:**
  1. **CSS ครั้งเดียว (เร็ว)** ใน `_inject_css()`: `[data-testid="stImage"] img { height:200px; object-fit:cover; border-radius:8px; }`
     ⚠️ โดน st.image **ทุกอัน** รวม QR+พรีวิว → QR crop จนสแกนไม่ได้ · ต้อง scope ให้แคบ (ครอบกริดด้วย container ที่มี class เฉพาะ) ซึ่ง Streamlit ทำยาก
  2. **crop ตอนทำ thumbnail (สะอาดกว่า แนะนำ)** — ฟังก์ชันใหม่ `get_thumbnail(file_id, w, h)` center-crop ด้วย PIL แล้วกริดเรียกตัวนี้แทน `get_image_bytes` · **ปุ่มดาวน์โหลดต้องยังได้ไฟล์เต็ม ไม่ใช่รูป crop** · ข้อดีแถม: โหลดเบาลง กริดเร็วขึ้น + คุม QR/พรีวิวไม่ให้โดนกระทบแน่นอน
- **🧪 เทสต์หลังแก้:** คลังภาพ → รูปแนวตั้ง+แนวนอนสูงเท่ากันเรียงเป็นตาราง · QR ยังสแกนได้ · พรีวิวก่อนส่งเห็นเต็มรูป · ดาวน์โหลดได้ไฟล์เต็ม

### ✅ [แก้แล้ว commit `64627c3`] #A ปุ่ม "← เปลี่ยนรหัส" ในหน้าเข้าร่วมกิจกรรม ทำให้คนถ่ายรูปสับสน

> ทำแล้ว 2 อย่าง (ยืนยันซ้ำ 2026-07-22): ปุ่มถูกลบ (ของ Cowork ไม่ revert เหลือคอมเมนต์อธิบายแทน) · **และแก้ต้นตอที่ลึกกว่าตามที่แนะนำ** — `_render_name_step()` แยกข้อความตามทางเข้าด้วยธง `no_code`: มาทางปุ่ม 🌐 ขึ้น `📤 กำลังส่งรูปเข้า: <ชื่อกิจกรรม>` · มาทางรหัส/QR ขึ้น `✅ รหัสถูกต้อง — ...` เหมือนเดิม

- **โจทย์จากหัวหน้า:** *"ตรงเข้าร่วมกิจกรรม หรือ กดเข้าไปส่งรูปอะ เอาปุ่มคำว่า เปลี่ยนรหัสออกนะ คนถ่ายไม่ต้องเปลี่ยนรหัส"*
- **ของเดิม:** ใน `_render_name_step()` (หน้ากรอกชื่อ หลังใส่รหัสกิจกรรมถูกแล้ว) มีปุ่ม
  ```python
  if st.button("← เปลี่ยนรหัส", key="back_from_name"):
      st.session_state.pop("pending_act", None)
      st.rerun()
  ```
- **ปัญหา:** จริงๆ มันคือปุ่ม *ย้อนกลับไปกรอกรหัสใหม่* แต่ตั้งชื่อจนคนถ่ายรูปอ่านแล้วนึกว่าตัวเองต้องไปเปลี่ยนรหัสผ่าน
- **🔍 เจอต้นตอจริงตอนเทสต์บนเว็บ (2026-07-22) — แย่กว่าที่คิด:** โฟลว์ 🌐 "ใครก็ได้" **ไม่มีการกรอกรหัสเลย** (กดปุ่มชื่อกิจกรรมบนหน้าแรกตรงๆ) แต่หน้ากรอกชื่อกลับขึ้น:
  - `✅ รหัสถูกต้อง — กิจกรรม: test02 ใครก็ได้` ← **ผู้ใช้ไม่เคยกรอกรหัส แล้วบอกว่ารหัสถูกต้อง**
  - ปุ่ม `← เปลี่ยนรหัส` ← **ยิ่งงงหนัก เพราะไม่มีรหัสให้เปลี่ยนตั้งแต่แรก**
  - → `_render_name_step()` ถูกใช้ร่วมกันทั้งโฟลว์ 🔒 (กรอกรหัส) และ 🌐 (ไม่กรอก) แต่ข้อความเขียนไว้สำหรับ 🔒 อย่างเดียว
  - **แนะนำให้ CLI แก้ทั้งคู่:** ข้อความหัวควรแยกตามโหมด (🌐 → `กำลังส่งรูปเข้า: <ชื่อกิจกรรม>` · 🔒 → `✅ รหัสถูกต้อง — ...`) และปุ่มย้อนกลับควรโผล่เฉพาะโหมด 🔒
- **⚖️ ข้อแลกเปลี่ยนที่ต้องรู้ก่อนตัดสินใจ:** ถ้าลบปุ่มทิ้งเฉยๆ คนที่ใส่รหัสผิดกิจกรรม (แต่เป็นรหัสที่ใช้ได้จริง) จะติดอยู่หน้ากรอกชื่อ ต้องกด **F5** ถึงกลับได้ · ทางเลือกที่เสียหายน้อยกว่าคือ **เปลี่ยนชื่อเป็น "← ย้อนกลับ" แล้วโชว์เฉพาะโหมด 🔒** — **ยังไม่ได้ถามหัวหน้าว่าเอาทางไหน**
- **🚧 สถานะ: Cowork เผลอแก้ไปแล้ว (ลบปุ่มทิ้ง) ใน working tree** — หัวหน้าบอก *"แก้ไปแล้วช่างมัน"* ไม่ต้อง revert · **CLI เช็ค `git diff auth.py` ก่อนทำอะไรต่อ** จะได้ไม่แก้ซ้ำ · ยังไม่ commit ยังไม่ push
- ✅ ยืนยันแล้วว่าไม่กระทบที่อื่น: `page_activity_user.py` ไม่มีอะไรเกี่ยวกับรหัสเลย · `render_change_password_box()` ถูกเรียกที่เดียวคือ `page_activity_admin.py:80` (หน้า admin — คนถ่ายรูปไม่เห็น ไม่ต้องแตะ)

### 🔴 #E ย้ายกิจกรรม "ไม่ต้องใส่รหัส" ขึ้นบนสุดของหน้าแรก — `auth.py` `_render_login_screen()`

- **โจทย์จากหัวหน้า (2026-07-22):** *"เอากิจกรรมที่ไม่ต้องใส่รหัส ไปไว้บนสุด อย่ามาปนกับที่ใส่รหัส admin"*
- **ลำดับปัจจุบัน (auth.py บรรทัด 448-497):**
  ```
  448  st.title("📷 คลังภาพกลางของบริษัท")
  455  st.caption("ใส่รหัสที่คุณได้รับ — ...")
  457  with st.form("login_main"):        ← ช่องกรอกรหัส + จำฉันไว้ + ปุ่มเข้าใช้งาน
  485  st.divider()
  486  _render_open_activities(remember)  ← 🌐 กิจกรรมไม่ต้องใช้รหัส  ★ ต้องย้ายขึ้นบนสุด
  487  _render_public_albums()
  490  with st.expander("🔐 สำหรับ admin / ผู้ดูแลระบบ"):
  ```
- **ทำไมหัวหน้าถึงบ่น:** ปุ่ม 🌐 ถูกวางคั่นกลางระหว่างช่องกรอกรหัสกับแถบ admin → **มองเผินๆ เหมือนเป็นส่วนหนึ่งของโซน admin** ทั้งที่จริงเป็นทางที่ *คนทั่วไปใช้บ่อยที่สุด* (พนักงานถ่ายรูปส่งเข้ากิจกรรม) · ของที่ใช้บ่อยสุดควรอยู่บนสุด
- **ลำดับที่ต้องการ:** `title` → **🌐 กิจกรรมไม่ต้องใช้รหัส** → `divider` → ช่องกรอกรหัส → อัลบั้มสาธารณะ → แถบ admin (พับ)
- **⚠️ กับดักทางเทคนิคที่ต้องระวัง:** `_render_open_activities(remember)` รับค่า `remember` ที่มาจาก `st.checkbox` **ในฟอร์มบรรทัด 460** — ถ้าย้ายฟังก์ชันนี้ขึ้นไปไว้เหนือฟอร์มเฉยๆ จะ **`NameError: remember`** ทันที
  - ทางแก้: เรียกแบบไม่ส่งอาร์กิวเมนต์ `_render_open_activities()` (signature บรรทัด 355 มี default `remember: bool = True` อยู่แล้ว) หรืออ่านค่าจาก `st.session_state` แทน
  - ผลข้างเคียงที่ยอมรับได้: คนที่กดปุ่ม 🌐 จะได้ "จำฉันไว้" เป็น True เสมอ (ซึ่งตรงกับ default ของ checkbox อยู่แล้ว)
- 💡 ถ้าจะให้ดีขึ้นอีก: ใส่หัวข้อคั่นให้ช่องกรอกรหัสด้วย เช่น *"มีรหัสอยู่แล้ว?"* จะได้แยกโซนกันชัดว่าอันบน = กดเลย · อันล่าง = ต้องมีรหัส

### 🟡 #F เปลี่ยนป้าย checkbox เป็น "ให้ฉันอยู่ในระบบต่อ" (ตัดจำนวนวันออก) — `auth.py`

- **หัวหน้าตัดสินใจแล้ว (2026-07-22):** *"3 ดีกว่า ไม่ต้องรู้หรอกกี่วัน"*
- **ของเดิม → ของใหม่:**
  ```
  เดิม:  จำฉันไว้ 1 วัน ในเครื่องนี้
  ใหม่:  ให้ฉันอยู่ในระบบต่อ
  ```
- **ต้องแก้ 2 จุด (ข้อความซ้ำกันเป๊ะ):**
  ```
  auth.py:196   remember = st.checkbox(f"จำฉันไว้ {REMEMBER_LABEL} ในเครื่องนี้", value=False, ...)   ← ฟอร์ม login admin
  auth.py:460   remember = st.checkbox(f"จำฉันไว้ {REMEMBER_LABEL} ในเครื่องนี้", value=True,  ...)   ← หน้าแรก
  ```
  ⚠️ **`value=` ต่างกัน (False / True) — อย่าเผลอทำให้เหมือนกัน** admin ตั้งใจไม่ติ๊กให้อัตโนมัติเพราะสิทธิ์สูงกว่า
- **⚠️ ผลข้างเคียง: `REMEMBER_LABEL` จะไม่ถูกใช้แล้ว**
  ```
  auth.py:19    REMEMBER_LABEL = f"{session_store.REMEMBER_DAYS} วัน"      ← กลายเป็นตัวแปรตายถ้าไม่มีใครอ้าง
  session_store.py:30   REMEMBER_DAYS = 1                                  ← ตัวคุมอายุ token จริง ห้ามลบ
  ```
  - **ห้ามลบ `REMEMBER_DAYS`** — เป็นตัวกำหนดอายุ token จริงใน `make_token()` / `save()`
  - `REMEMBER_LABEL` เลือกได้ 2 ทาง: (ก) ลบทิ้ง (ข) **เก็บไว้ใช้ใน tooltip แทน** เช่น `help="ในเครื่องนี้เท่านั้น · อยู่ได้ {REMEMBER_LABEL} · อย่าติ๊กถ้าใช้เครื่องกลาง"` ← **แนะนำทาง (ข)** ข้อมูลไม่หาย แค่ย้ายไปที่ที่ไม่เกะกะ
- **เหตุผลที่ไม่เอา "Remember me":** ทั้งแอปเป็นไทยหมด · คนใช้จริงคือพนักงานโรงงาน ป้ายอังกฤษเสี่ยงไม่กล้าติ๊กเพราะไม่รู้ว่ามันทำอะไร

### ✅ [แก้แล้ว commit `64627c3`] #B ตาราง Log ตอนไม่มีผลลัพธ์ขึ้นคำว่า `empty` เป็นภาษาอังกฤษ

> ทำแล้ว: `page_activity_superuser._render_log()` เช็ค `if f.empty:` แล้วขึ้น `st.info("ไม่พบรายการที่ตรงกับเงื่อนไข — ลองล้างคำค้นหรือเปลี่ยนตัวกรองดูครับ")` แทนการวาดตารางเปล่า (ยืนยันซ้ำ 2026-07-22)

ค่า default ของ `st.dataframe` — ไม่สอดคล้องกับคลังภาพที่มีข้อความไทย ควรเช็ค `if df.empty: st.info("ไม่พบรายการที่ตรงกับเงื่อนไข")` แทนการวาดตารางเปล่า

### ✅ [แก้แล้ว commit `64627c3`] #C เลขหน้าเกินขอบไม่บอกเหตุผล

> ทำแล้ว: `page_gallery._paginate()` ป้ายช่องเป็น `หน้า (1–N)` + `help="มีทั้งหมด N หน้า — พิมพ์ได้ตั้งแต่ 1 ถึง N"` (ยืนยันซ้ำ 2026-07-22)

พิมพ์ 999 ตอนมีหน้าเดียว → ได้แค่ขอบแดง + ไอคอน ⓘ ผู้ใช้อาจงงว่าทำไมกด Enter แล้วไม่มีอะไรเกิดขึ้น ควรใส่ `help=` หรือข้อความ "หน้าสูงสุดคือ N"

### ✅ [แก้แล้ว] #D-2 — CSS ที่แก้ #D ไปแล้วทำให้แย่ลง ถอยออก + แก้ใหม่ (commit `3710072`)

> **✅ CLI ทำแล้ว 2026-07-22 — เทสต์ในเครื่องผ่าน 87/87**
> 1. **ถอย CSS `data-stale` ออกจาก `app.py` แล้ว** (เหลือคอมเมนต์อธิบายไว้ว่าทำไมถึงถอย กันคนหลังมาใส่ซ้ำ)
> 2. **ทำตามข้อ 0 เป็นหลัก** — `render_landing()` ครอบทั้งหน้าไว้ใน `st.empty()` (`_login_box`) แล้วเนื้อหาจริงย้ายไป `_render_landing_body()` · ตอนกำลังเข้าระบบเรียก `_clear_login_screen()` → **แทนที่ทั้งกล่องด้วยข้อความ "⏳ กำลังเข้าสู่ระบบ..." = หน้า login หายจาก DOM ทันที** ไม่ว่าหน้าใหม่จะโหลดนานแค่ไหน (ตรงเกณฑ์ "ไม่ควรโผล่มาให้เห็นเลย")
> 3. **ครบทั้ง 3 ทางเข้าตามที่กำชับ** — `_do_login()` (ครอบ login ทุกแบบรวม admin/superuser) · ปุ่มกิจกรรม 🌐 · รหัสกิจกรรมถูก→หน้ากรอกชื่อ (2 อันหลังขึ้น "⏳ กำลังเปิดหน้ากรอกชื่อ...")
> 4. **กับดัก cookie ที่เตือนไว้ในข้อ 3 — จัดการแล้ว:** `_do_login` **ไม่เขียน cookie ตรงนั้นแล้ว** (component จะไปอยู่ในกล่องที่กำลังถูกล้าง เสี่ยงเขียนไม่ติด) เปลี่ยนเป็นฝากไว้ใน `session_state["pending_cookie"]` แล้วให้ `auth.flush_pending_cookie()` ใน `main()` เขียนในรอบถัดไป **ซึ่งอยู่นอกกล่องและไม่ถูกล้าง** · เทสต์ยืนยัน: ติ๊กแล้วเขียน 1 ครั้ง · ไม่เขียนซ้ำทุก rerun · ไม่ติ๊กแล้วไม่เขียน
> 5. ทาง "เสริม" (ข้อ 2) ที่ทำไปรอบก่อน — **แคชการอ่านชีตยังเก็บไว้ทั้งหมด** (`check_connection` 5 นาที · `load_data` 300 วิ · กิจกรรม/บัญชี/แชร์ 180 วิ) เพราะทำให้ spinner หมุนสั้นลงจริง ไม่เกี่ยวกับ CSS ที่ถอย
>
> **🧪 ต้องเทสต์บนเว็บ (ตามที่ระบุไว้):** login → หน้าใหม่ขึ้น → **หน้า login ต้องหายหมด ไม่เหลือช่องรหัสค้าง** · ติ๊ก "ให้ฉันอยู่ในระบบต่อ" → refresh → **ต้องไม่หลุด** (ยืนยันว่า cookie ยังเขียนติดหลังย้ายจังหวะเขียน)

<details>
<summary>รายละเอียดปัญหาเดิม (เก็บไว้อ้างอิง)</summary>


> **⚠️ คำแนะนำเดิมของ Cowork ในข้อ #D ผิด — ขอถอนคำแนะนำนั้น** CLI ทำตามแล้วอาการหนักกว่าเดิม

**อาการที่หัวหน้าเจอ (มีภาพยืนยัน 2026-07-22):** login สำเร็จแล้ว Dashboard ขึ้นแล้ว แต่ **หน้า login ยังค้างอยู่ข้างล่างเต็มๆ** — เห็นทั้งหัวข้อ "มีรหัสอยู่แล้ว?" · ช่องรหัสของคุณ · ปุ่มเข้าใช้งาน · แถบ admin ที่ยังมี `superadmin` + รหัสกรอกค้างไว้ · **คมชัดเหมือนใช้งานได้จริง ไม่ได้จางเลย**

**ทำไมถึงแย่ลง — เข้าใจกลไกผิดตั้งแต่ต้น:**
```css
/* app.py commit 561b6f9 */
[data-stale="true"], ... { opacity: 1 !important; filter: none !important; }
```
- Streamlit ทำเนื้อหาเก่า **จางลง** ระหว่าง rerun **โดยตั้งใจ** — เพื่อบอกผู้ใช้ว่า "ของนี้เก่าแล้ว กำลังโหลดของใหม่"
- CSS นี้ไป **บังคับให้กลับมาทึบ 100%** → แทนที่จะเห็นเงาจางๆ กลายเป็น **เห็นหน้า login เต็มๆ คมกริบ** ซ้อนอยู่ใต้ Dashboard
- **การจางคืออาการ ไม่ใช่โรค** · โรคจริงคือ **rerun หลัง login ช้า** (ยิง Google Sheet หลายรอบ) ทำให้ element เก่าค้างบนจอนาน
- ยิ่งอันตรายตรงที่ **ช่องรหัสผ่านที่กรอกไว้ยังโชว์ค้างอยู่บนจอหลัง login แล้ว**

**🎯 เกณฑ์ที่หัวหน้ากำหนด (2026-07-22) — อ่านก่อนเริ่มแก้:**
> *"แต่สักพักมันก็หายไปนะ แต่มันไม่ควรโผล่มาให้เห็นเลยนะ"*

**= ไม่ใช่แค่ "ให้หายเร็วขึ้น" แต่ต้อง "ไม่โผล่ให้เห็นตั้งแต่แรก"** · แก้ให้ rerun เร็วขึ้นเฉยๆ **ยังไม่ผ่านเกณฑ์** เพราะถ้าเน็ตช้าหรือ Google ตอบช้า เงาก็กลับมาอยู่ดี · ต้องลบ element ทิ้งจริง ไม่ใช่แข่งกับเวลา

**✅ สิ่งที่ควรทำ:**
0. **ทางที่ตรงเกณฑ์ที่สุด — ล้างหน้า login ทิ้งทันทีที่กดปุ่ม (ทำข้อนี้เป็นหลัก):**
   ```python
   # auth.py _render_login_screen()  — แนวคิด ไม่ใช่โค้ดสำเร็จรูป
   login_box = st.empty()                 # ครอบทั้งหน้า login ไว้ในกล่องเดียว
   with login_box.container():
       ... st.title / caption / form / _render_open_activities / expander admin ...

   if ok:
       kind, data = identify_code(code)
       if kind:                           # รหัสถูก → กำลังจะเข้าระบบ
           login_box.empty()              # ★ ลบ element ทิ้งจาก DOM เดี๋ยวนั้น
           with st.spinner("กำลังเข้าสู่ระบบ..."):
               _do_login(...)             # ข้างในค่อย save cookie + st.rerun()
   ```
   → หน้า login **หายทันที** ไม่ว่า Dashboard จะโหลดนานแค่ไหน · ผู้ใช้เห็น spinner แทน ซึ่งสื่อสารถูกต้องว่า "กำลังทำงาน"
   → **ต้องครอบให้ครบทั้ง 3 จุดที่พาเข้าระบบ**: ฟอร์มรหัสหน้าแรก · ปุ่มกิจกรรม 🌐 · ฟอร์ม login admin (ไม่งั้นแก้ได้แค่ทางเดียว อีก 2 ทางยังเห็นเงา)
1. **ถอย CSS block `data-stale` ออก** (`_inject_css()` ใน `app.py`) — ทำคู่กับข้อ 0 · ถ้าล้าง element ถูกแล้วก็ไม่ต้องมี CSS นี้อีก และถ้าเผลอเหลือไว้ มันจะทำให้เงาที่หลุดมาคมชัดกว่าเดิม
2. เสริม (ไม่ใช่ตัวหลัก) = **ทำให้ rerun หลัง login เร็วขึ้น** เพื่อลดเวลาหมุน spinner:
   - แคชการอ่านชีต (`load_users` / `load_activities` / `load_data`) ด้วย `@st.cache_data(ttl=...)`
   - ดูจุดที่ยิงซ้ำ: `_do_login()` → `session_store.save()` → `st.rerun()` → `main()` → `ensure_session/handle_deeplink/restore_session` → หน้า Dashboard อ่านชีตอีกหลายรอบ
3. ถ้าอยากให้จอสะอาดจริงระหว่างโหลด ให้ใช้ **`st.empty()` ครอบทั้งหน้า login แล้วสั่ง `.empty()` ก่อน `st.rerun()`** — ล้าง element เก่าทิ้งเลย ไม่ใช่ไปบังคับ opacity
   - ⚠️ ถ้าเลือกทางนี้ ระวัง `session_store.save()` ที่เรียก `cm.set(..., key="iw_cookie_set")` ก่อน `st.rerun()` — เป็น component (iframe) ต้องได้ render รอบนึงถึงจะเขียน cookie สำเร็จ **ถ้าล้างจอผิดจังหวะ cookie อาจไม่ถูกเขียน = "จำฉันไว้" พัง** ต้องเทสต์ซ้ำหลังแก้

**🧪 ต้องเทสต์หลังแก้:** login → Dashboard ขึ้น → **หน้า login ต้องหายไปหมด ไม่เหลือช่องรหัสค้าง** · ติ๊ก "ให้ฉันอยู่ในระบบต่อ" → refresh → **ต้องไม่หลุด** (ยืนยันว่า cookie ยังเขียนติด)

</details>


### 🟡 #D "เงา" หลัง login หายช้า (หัวหน้าบ่นเอง) — ⚠️ **คำแนะนำในข้อนี้ผิด ดู #D-2 แทน**

ยืนยันต้นเหตุแล้ว = Streamlit ทำเนื้อหาเดิม**จางลง (stale/opacity)** ระหว่าง rerun ไม่ใช่ element ค้าง · เห็นชัดตอนกดค้นหาใน Log (ปุ่ม Stop ขึ้นมุมขวาบน เนื้อหาจางทั้งหน้า) · หลัง login จางนานเพราะต้องอ่าน Google Sheet หลายรอบ (users + activities + photos)
- **ทาง ก (เร็ว ~5 นาที):** CSS `[data-stale="true"]{opacity:1!important}` กลบที่ปลายเหตุ — แต่ระหว่างโหลดจะดูเหมือนหน้าค้าง
- **ทาง ข (ช้ากว่า ได้จริง):** ลด/แคชการอ่านชีตตอน login → เงาสั้นลงเอง + เร็วขึ้นทั้งแอป
- **ยังไม่ได้เลือกทาง** — หัวหน้าบอก "เทสต์ก่อน เงาไว้ทีหลัง"

---

## 🐞 Bug-hunt (code review โดย Claude, 2026-07-22)

> ไล่อ่านโค้ดจริงหาบั๊ก เน้นของใหม่ที่ "รอทดสอบบนเว็บจริง" (auth อีเมล/สมัคร/ลืมรหัส + data layer)
> **✅ แก้ครบทั้ง 8 ข้อแล้ว (2026-07-22) — เทสต์ในเครื่องผ่าน 51/51** (รายละเอียดวิธีแก้ต่อท้ายแต่ละข้อ)

**🔴 #1 [Med-High] cookie ที่ถูกแก้/เพี้ยน → หน้าแรก crash** — `session_store.py:75-77` (`read_token`)
- `body.encode("ascii")` และ `hmac.compare_digest(sig, expect)` อยู่ **นอก** try/except ; `sig`/`body` มาจาก cookie ที่ผู้ใช้แก้เองได้ ถ้ามีอักขระ non-ASCII → `UnicodeEncodeError`/`TypeError` (บั๊กคลาส `compare_digest` เดิมเป๊ะ แต่ตกหล่นตรงจุด input ที่ไม่น่าเชื่อถือ)
- `load()` (บรรทัด 126) และ `app.main()` (บรรทัด 72) ก็ไม่ครอบ → error เด้งถึงหน้าจอ **ผู้ใช้เข้าเว็บไม่ได้จนกว่าจะล้าง cookie เอง**
- คอมเมนต์บรรทัด 76 ที่ว่า "ทั้งคู่เป็น hex ASCII จึงปลอดภัย" จริงเฉพาะ token ที่ถูกต้อง ไม่จริงกับ cookie ที่ถูกแก้
- ✅ **แก้แล้ว:** ครอบทั้ง `read_token` ด้วย `try/except Exception: return None` — cookie เพี้ยน = "ไม่ได้จำ login ไว้" · เทสต์ยิง cookie ขยะ 7 แบบ (ไทย/ตัดกลาง/ยาว 500 ตัว) ไม่ throw + token ปกติยังอ่านได้

**🟠 #2 [Med] กด "เปิดใช้งาน" บัญชีที่รออนุมัติได้ โดยไม่ตั้งรหัส → บัญชีค้าง login ไม่ได้** — `page_activity_superuser.py:485-489`
- "รายชื่อบัญชี admin ทั้งหมด" วน `load_users()` รวมบัญชีสถานะ `รออนุมัติ` ด้วย → บัญชี pending โชว์ปุ่ม "▶️ เปิดใช้งาน" → กดแล้วเรียก `set_user_status(uname,"ใช้งาน")` เฉยๆ **ไม่ได้ตั้งรหัส**
- บัญชีสมัครเอง hash ว่าง → active แต่ `verify_secret` คืน False เสมอ = login ไม่ได้ + หลุดจากคิว `pending_users()` (ไม่โผล่ในกล่องอนุมัติอีก) → superuser ต้องไปตั้งรหัสใน expander เอง ถึงจะใช้ได้
- ✅ **แก้แล้ว:** กรอง `USER_PENDING` ออกจาก loop รายชื่อ → เปิดใช้งานได้ทางเดียวคือฟอร์มอนุมัติที่บังคับตั้งรหัสก่อน

**🟠 #3 [Low-Med] SUPERUSER_PASS ว่าง → รหัสว่างล็อกอิน superuser ได้** — `auth.py:207`
- `secrets_equal("","")` คืน True (sha256 ของ "" เท่ากัน) ; ถ้า `SUPERUSER_PASS` ไม่ตั้ง/ว่าง แล้ว `SUPERUSER_USER` ตั้ง → ใครกรอก user นั้น + เว้นรหัสว่าง = ได้สิทธิ์ superuser
- ต่างจาก admin (กัน hash ว่าง) และ APP_PASSWORD (กัน code ว่าง) — จุดนี้ไม่มีการ์ด ; ปัจจุบันปลอดภัยถ้า secrets ตั้งครบ แต่ควรกันเชิงรับ
- ✅ **แก้แล้ว:** เพิ่มการ์ด `su_user and su_pass and p` ก่อนเทียบ → secrets ตกหล่นก็ไม่มีทางเข้าด้วยรหัสว่าง

**🟡 #4 [Low-Med] ถอนสิทธิ์ดูอัลบั้มแล้ว viewer ที่ติ๊ก "จำฉันไว้" ยังดูได้ ~1 วัน** — `auth.py restore_session` (role=viewer)
- ตอน login `find_share_by_code()` กันแชร์ที่ถูกถอน (สถานะ ปิด) แต่ `restore_session` ของ viewer เช็คแค่ว่ากิจกรรมยังอยู่ ไม่ได้เช็ค share code ซ้ำ → refresh แล้วได้สิทธิ์คืนจนกว่า cookie หมดอายุ
- ✅ **แก้แล้ว:** เพิ่ม `auth._viewer_still_allowed(activity_id, viewer_name)` เรียกใน `restore_session` — อัลบั้มสาธารณะผ่านเสมอ / อัลบั้มเฉพาะคนต้องยังมีแถวแชร์ชื่อนั้นและสถานะไม่ใช่ "ปิด" (อ่านชีตไม่ได้ = ไม่คืนสิทธิ์)

**🟡 #5 [Low] แถวรูปที่ลิงก์ว่าง 2 แถวขึ้นไป → widget key ซ้ำ ทั้งแท็บพัง** — `page_activity_superuser.py` (gallery ~348-368, trash ~415-438)
- key ปุ่มใช้ `file_id` ล้วน ; `extract_file_id()` คืน "" ถ้า `ลิงก์รูป` ว่าง/ไม่ใช่ลิงก์ Drive → 2 แถวเสีย = key ชนกัน → `StreamlitDuplicateElementKey` เรนเดอร์ทั้งแท็บล้ม
- ✅ **แก้แล้ว:** ทำ `uid = f"{i+j}_{file_id}"` ใช้เป็นคีย์ทุกปุ่มในกริดรูป — **แก้ทั้ง 3 หน้าที่มีแพตเทิร์นเดียวกัน** (superuser คลัง+ถังขยะ · admin คลัง+ถังขยะ+สแกนรูปซ้ำ · คลังทั่วไป) ไม่ใช่เฉพาะที่ review ชี้

**🟡 #6 [Low] `add_user` เขียนอีเมลแบบอิงตำแหน่ง (เปราะ)** — `google_utils.py:813`
- `append_row([username,password_hash,fullname,role,status,email])` = อิงตำแหน่งช่อง 6 ; แต่ทุกจุดที่ *อ่าน* (`find_user`,`email_taken`) และ reset ใช้ header-lookup — ตอนนี้ยังถูกเพราะลำดับคอลัมน์ตรง แต่ถ้าคอลัมน์สลับ อีเมลจะไปลงผิดช่อง
- ✅ **แก้แล้ว:** `add_user` หาตำแหน่งคอลัมน์อีเมลด้วย `_users_col(EMAIL_HEADER)` (คอลัมน์ 1-5 ยังตรึงตำแหน่งเหมือนเดิมตามที่โค้ดอื่นอ้าง)

**🟡 #7 [Low] `set_user_status`/`set_activity_status` ไม่มี guard `row > 1`** — `google_utils.py` (~831, ~648)
- ฟังก์ชันลบอื่นกัน `row>1` กันทับหัวตาราง แต่ 2 ตัวนี้ไม่กัน ; ถ้า username=="username" (หรือ activity_id=="activity_id") → เขียนทับ header
- ✅ **แก้แล้ว:** เติม `and row > 1` ทั้ง `set_user_status` และ `set_activity_status` (+ `set_user_status` เรียก `resolve_username` ให้รองรับการพิมพ์อีเมล)

**🟡 #8 [Low] `delete_photo` ไม่ล้าง `load_data` cache** — `google_utils.py:139-160`
- ต่างจาก `trash_photo`/`restore_photo` ที่ล้าง ; ตอนนี้ไม่พังเพราะ caller ล้าง cache ต่อ แต่เป็นกับดัก stale cache สำหรับ caller ในอนาคต
- ✅ **แก้แล้ว:** เติม `load_data.clear()` ท้าย `delete_photo` ให้เหมือน `trash_photo`/`restore_photo`

**✅ จุดที่ review แล้วปลอดภัย (ยืนยัน):** บัญชี hash ว่าง login ไม่ได้จริง (`verify_secret` กัน) · cookie ปลอมยกระดับเป็น superuser ไม่ได้ (HMAC + เช็ค username ซ้ำกับ secrets) · บัญชี pending/ปิด คืนสิทธิ์ผ่าน cookie ไม่ได้ · `find_user("")` คืน None แล้ว (บั๊กเดิมแก้จริง) · `secrets_equal`/`verify_secret` ไม่ throw กับภาษาไทย (บั๊ก compare_digest เดิมแก้ครบ **ยกเว้น #1**)

**🧪 เทสต์หลังแก้ (สตับ streamlit + import จริงทุกโมดูล): ผ่าน 51/51** — เพิ่มเคสคุมบั๊กทั้ง 8 ข้อไว้แล้ว (cookie ขยะ · pending ไม่โผล่ในรายชื่อ · superuser รหัสว่าง · viewer ถูกถอนสิทธิ์ · คีย์ปุ่มซ้ำ · header lookup · guard หัวตาราง · cache)

**🌐 หลัง deploy ควรลองบนเว็บ:** เข้าเว็บด้วย cookie เดิม (ต้องไม่หลุด) · superuser: บัญชีรออนุมัติต้องอยู่แค่ในกล่องอนุมัติ ไม่โผล่ในรายชื่อล่าง · ลบ/กู้คืนรูปในคลังภาพยังทำงานปกติ (คีย์ปุ่มเปลี่ยน)

**✅ เทสต์ผ่านเบราว์เซอร์จริงครบแล้ว (2026-07-22, บัญชี superadmin) — ผ่าน 4/4 ไม่เจอบั๊ก**

> รอบก่อนติดตรง "login แท็บที่ควบคุมไม่ได้" — รอบนี้แก้ด้วยการให้ผู้ใช้ login เองแล้ว Claude คุมต่อในแท็บเดิม

| # | เทสต์ | ผล |
|---|-------|-----|
| B2 | **ค้นหา/กรองคลังภาพ** (ฝ่ายHR) — คำค้นมั่ว `zzzไม่มีคำนี้แน่นอน999` | ✅ "พบ 0 รูป" + กล่องเหลือง "ไม่พบรูปที่ตรงกับเงื่อนไข ลองปรับตัวกรองดูครับ" · ปุ่ม ZIP หายตาม · ล้างคำค้น → กลับมา 1 รูป |
| B2 | ช่วงวันที่ที่ไม่มีรูป (2026/06/01–06/10 แต่รูปอยู่ 06/25) | ✅ พบ 0 รูป ไม่ crash |
| B2 | **ช่วงวันที่เลือกไม่ครบ** (เลือกวันเริ่มอย่างเดียว → `2026/06/20 –`) | ✅ **ไม่ crash** ตกกลับไปไม่กรองวันที่ (พบ 1 รูป) — นี่คือเคสที่ `date_input` แบบ range พังบ่อยสุด (คืน tuple 1 ตัว) |
| B3 | **ปุ่มหารูปซ้ำ pHash** (คลังภาพทุกกิจกรรม 37 รูป) | ✅ "ไม่พบรูปซ้ำในชุดนี้" สแกนจบไม่ค้าง ไม่ error |
| B4 | **สร้างกิจกรรมชื่อว่าง** | ✅ "⚠️ กรอกชื่อกิจกรรมก่อน" ไม่สร้างแถวขยะ |
| B4 | สร้างกิจกรรมชื่อ **เว้นวรรคล้วน** (`"    "`) | ✅ ถูกปฏิเสธเหมือนกัน (มี `.strip()` จริง) |
| B5 | **Log ค้นหาไม่เจอ** (`zzzไม่มีแน่นอน999`) | ✅ "0 รายการ" ตารางว่าง ไม่ crash |
| B5 | **เลขหน้าเกินขอบ** (พิมพ์ 999 ตอนมีหน้าเดียว) | ✅ ขอบแดง + ไอคอนเตือน ไม่ apply · ยังคง "หน้า 1/1" แสดงรูปปกติ (`number_input` มี max_value กันไว้) |

**🎨 ของเล็กที่เจอระหว่างเทสต์ (ไม่ใช่บั๊ก แต่ควรปรับ):**
1. **ตาราง Log ตอนไม่มีผลลัพธ์ขึ้นคำว่า `empty` เป็นภาษาอังกฤษ** (ค่า default ของ `st.dataframe`) — ไม่สอดคล้องกับคลังภาพที่มีข้อความไทยสวยๆ ควรเช็ค `if df.empty: st.info("ไม่พบรายการที่ตรงกับเงื่อนไข")` แทนการวาดตารางเปล่า
2. **เลขหน้าเกินขอบไม่มีข้อความบอกเหตุผล** — มีแค่ขอบแดง + ไอคอน ⓘ ผู้ใช้อาจงงว่าทำไมกด Enter แล้วไม่มีอะไรเกิดขึ้น ควรใส่ `help=` หรือข้อความ "หน้าสูงสุดคือ N"
3. **"เงา" ที่ผู้ใช้บ่นว่าหายช้าหลัง login — ยืนยันต้นเหตุแล้ว** = Streamlit ทำเนื้อหาเดิม**จางลง (stale/opacity)** ระหว่าง rerun ไม่ใช่ element ค้าง · เห็นชัดตอนกดค้นหาใน Log (มีปุ่ม Stop ขึ้นมุมขวาบน เนื้อหาจางทั้งหน้า) · **หลัง login จางนานเพราะต้องอ่าน Google Sheet หลายรอบ** (users + activities + photos) → แก้ได้ 2 ทาง: (ก) CSS `[data-stale="true"]{opacity:1!important}` กลบที่ปลายเหตุ (ข) ลด/แคชการอ่านชีตตอน login = เร็วขึ้นจริง — **ยังไม่ได้ทำ ผู้ใช้ขอเทสต์ก่อน**

**✅ เทสต์เพิ่มรอบเดียวกัน (อ่านอย่างเดียว ไม่แตะข้อมูล):**

| # | เทสต์ | ผล |
|---|-------|-----|
| B8 | **แท็บถังขยะ** (ยืนยันบั๊ก #5 คีย์ปุ่มซ้ำ) | ✅ เรนเดอร์ปกติ "พบ 1 รูปในถังขยะ" (hr · 2026-07-16 13:27:27 · โดย admin) มีปุ่ม ♻️ กู้คืน / 🔥 ลบถาวร ครบ · **ไม่มี StreamlitDuplicateElementKey** |
| — | แท็บจัดการบัญชี admin | ✅ เรนเดอร์ปกติ 2 บัญชี: `pim` (admin/ใช้งาน) · `miscen06@tfpoem.com` (admin/ใช้งาน มีอีเมล) · ไม่มีคิวรออนุมัติ/คิวลืมรหัสค้าง |

**🔎 หลักฐานว่าโฟลว์ขอเปิดบัญชีใช้งานได้จริง (จาก Log + รายชื่อบัญชี):**
- Log 2026-07-22 10:16:31 — `ขอเปิดบัญชี admin` โดย `miscen06@tfpoem.com` (role: guest)
- Log 2026-07-22 10:17:48 และ 10:17:59 — `ตั้งรหัสใหม่ให้ admin` โดย `superadmin` (2 ครั้ง)
- ปัจจุบันบัญชีนี้อยู่ในรายชื่อ สถานะ **ใช้งาน** พร้อมอีเมล
- ⏳ **เหลือยืนยันแค่ว่า login ด้วยอีเมล+รหัสนั้นเข้าได้จริง** (ยังไม่ได้ลอง)

**⚠️ B7 เทสต์ไม่ได้ในรอบนี้ — ยังไม่ยืนยันบั๊ก #2:** จะพิสูจน์ว่า "บัญชี `รออนุมัติ` ไม่โผล่ในรายชื่อล่าง (ไม่มีปุ่มเปิดใช้งาน)" ได้ **ต้องมีบัญชี pending อยู่จริง** — ตอนนี้ในระบบไม่มีเลย (ถูกอนุมัติหมดแล้ว) · วิธีเทสต์: ออกจากระบบ → หน้าแรก > `🔐 สำหรับ admin` > แท็บขอเปิดบัญชี admin → กรอกอีเมลบริษัทใหม่ 1 อัน → login superuser กลับมาดูว่าโผล่**เฉพาะ**ในกล่องอนุมัติ

**✅ เทสต์กิจกรรมโหมด 🌐/🔒 + "รูปของฉัน" บนเว็บจริง (2026-07-22) — ผ่าน 4/4**

> ของทดสอบที่หัวหน้าสร้างไว้: `test02 ใครก็ได้` (🌐 · 10 รูป · ผู้ส่ง "Ben") และ `test03` (🔒 · รหัส 3115)

| # | เทสต์ | ผล |
|---|-------|-----|
| C1 | **ออกจากระบบ → หน้าแรกโชว์เฉพาะกิจกรรม 🌐** | ✅ ขึ้นหัวข้อ "📤 ส่งรูปเข้ากิจกรรมที่เปิดให้ทุกคน — กดได้เลย ไม่ต้องใช้รหัส" มีปุ่มเดียว = **test02 ใครก็ได้** |
| C2 | **กิจกรรม 🔒 ต้องไม่โผล่** | ✅ `test03` **ไม่โผล่** บนหน้าแรก (ต้องใช้รหัส 3115 เท่านั้น) |
| C3 | **กิจกรรมเก่า 6 อัน (คอลัมน์ `การเข้าร่วม` ว่าง) ต้องไม่หลุดมาให้ส่งฟรี** | ✅ ไม่มีอันไหนโผล่เลย — ยืนยัน "ค่าว่าง = ต้องมีรหัส (ปลอดภัยไว้ก่อน)" ทำงานจริงบน production |
| C4 | **กด 🌐 → กรอกชื่อ → เข้าหน้าส่งรูปได้** | ✅ เข้าได้ หัวข้อขึ้น "ส่งรูปเข้ากิจกรรม: test02 ใครก็ได้ · คุณ: เทสต์-ไม่ใช่Ben" มีปุ่ม Upload + ส่งรูป ครบ |
| C5 | **แท็บ "รูปของฉัน" ต้องไม่เห็นรูปคนอื่น** | ✅ ขึ้น "เห็นเฉพาะรูปที่ส่งในชื่อ *เทสต์-ไม่ใช่Ben* — ไม่เห็นรูปของคนอื่น" และ "ยังไม่มีรูปที่คุณส่ง" · **รูป 10 ใบของ Ben ในกิจกรรมเดียวกันไม่โผล่แม้แต่ใบเดียว** → หลัก "ถ่าย ≠ ดู" ยังอยู่ครบ |

✅ ยืนยันด้วยตาอีกรอบ: **หน้าส่งรูปไม่มีอะไรเกี่ยวกับรหัสผ่านเลย** (ตรงกับที่ไล่โค้ดแล้ว) — เรื่องปุ่ม "เปลี่ยนรหัส" อยู่ที่หน้ากรอกชื่อก่อนหน้าเท่านั้น ดูข้อ #A

**✅ เทสต์โฟลว์บัญชี + QR ในสถานะ guest (2026-07-22) — ผ่าน 8/8**

| # | เทสต์ | ผล |
|---|-------|-----|
| D1 | **QR deep-link `?actcode=3115`** ตอนออกจากระบบ | ✅ ข้ามช่องกรอกรหัส ไปหน้ากรอกชื่อของ `test03` ทันที (ตรงนี้ข้อความ "รหัสถูกต้อง" **ถูกต้อง** เพราะมีรหัสมาทาง URL จริง — ต่างจากเคส 🌐 ในข้อ #A) |
| D2 | ขอเปิดบัญชี: **gmail** | ✅ ปฏิเสธ "ต้องใช้อีเมลบริษัทที่ลงท้ายด้วย @tfpoem.com หรือ @tfpthailand.com หรือ @tfpcar.com เท่านั้น" |
| D3 | ขอเปิดบัญชี: **โดเมนหลอก** `hacker@tfpoem.com.evil.com` | ✅ ปฏิเสธ — ยืนยันว่าเช็คแบบ *ลงท้ายด้วย* จริง ไม่ใช่ *มีคำนี้อยู่* |
| D4 | ขอเปิดบัญชี: **อีเมลซ้ำ พิมพ์ใหญ่ทั้งหมด** `MISCEN06@TFPOEM.COM` | ✅ ปฏิเสธ "อีเมลนี้ขอไว้แล้ว — ถ้ายังไม่ได้รหัส หรือลืมรหัส ให้ทักผู้ดูแลระบบได้เลย" → `email_taken()` ไม่สนตัวพิมพ์ ถูกต้อง |
| D5 | ขอเปิดบัญชี: **อีเมลใหม่ถูกโดเมน** `claude.qa.test@tfpoem.com` | ✅ "ส่งคำขอแล้ว — ผู้ดูแลจะตั้งรหัสให้แล้วแจ้งคุณ จากนั้นเข้าสู่ระบบด้วย อีเมล + รหัสที่ได้รับ" |
| D6 | **login ทันทีด้วยบัญชีที่ยังไม่ได้รับรหัส** | ✅ ขึ้น "⏳ ยังไม่ได้รับรหัส — ผู้ดูแลระบบกำลังตั้งรหัสให้ แล้วจะแจ้งคุณเอง" (ไม่ใช่ "รหัสผิด") → **ยืนยัน hash ว่าง = login ไม่ได้** |
| D7 | ลืมรหัส: **อีเมลที่ไม่มีในระบบ** | ✅ "ส่งคำขอแล้ว — ติดต่อผู้ดูแลระบบเพื่อรับรหัสใหม่ได้เลย" |
| D8 | ลืมรหัส: **อีเมลที่มีจริง** (`miscen06@tfpoem.com`) | ✅ **ข้อความเหมือน D7 เป๊ะ** → กันไล่เดาว่ามีใครอยู่ในระบบ (user enumeration) ได้จริง |

**🧹 ข้อมูลทดสอบที่ค้างอยู่ในชีต — ต้องเก็บกวาด:**
1. **บัญชี `claude.qa.test@tfpoem.com` สถานะ `รออนุมัติ`** — สร้างไว้เพื่อเทสต์ B7 · เก็บไว้ก่อนจนกว่าจะเทสต์ B7 เสร็จ แล้วค่อยกด 🚫 ปฏิเสธ ให้หายไป
2. **`miscen06@tfpoem.com` อยู่ในคิวลืมรหัส** — ⚠️ **รหัสเดิมไม่ถูกแตะ ยัง login ได้ปกติ** · superuser ตั้งรหัสใหม่ให้ 1 ครั้งก็ล้างคิวเอง หรือปล่อยไว้ก็ได้
3. กิจกรรมทดสอบ `test02` (10 รูป) · `test03` (0 รูป) — หัวหน้าสร้างเอง
4. ผู้ส่งชื่อ **"เทสต์-ไม่ใช่Ben"** เคยเข้า `test02` แต่**ไม่ได้ส่งรูป** → ไม่มีข้อมูลค้าง
5. (ค้างเก่าจาก 2026-07-17) รูปทดสอบในกิจกรรม `hr` — ภาพเกม/AI/หุ่นยนต์

**⏳ เหลืออีก 3 ข้อ — ต้อง login เป็น superuser ถึงจะยืนยันได้ (Cowork ไม่มีรหัส):**
1. **B7 (บั๊ก #2)** — เข้าแท็บจัดการบัญชี admin แล้วดูว่า `claude.qa.test@tfpoem.com` โผล่**เฉพาะ**ในกล่อง "⏳ มี N คนรอคุณอนุมัติ" และ**ไม่มี**ปุ่ม "▶️ เปิดใช้งาน" ในรายชื่อล่าง
2. **คิวลืมรหัส** — ต้องเห็นกล่องแดง "🔑 มี N คนลืมรหัส" มี `miscen06@tfpoem.com` อยู่ · ลองตั้งรหัสใหม่ให้ → คิวหายเอง + Log จด
3. **ปิดวง B3** — กด 🗑️ ลบใบนี้ ที่รูปซ้ำ 1 ใบ → ต้องลงถังขยะ → กดกู้คืน → กลับมาที่เดิม

**⏳ ยังไม่ได้เทสต์ (เหลืออยู่):**
- ~~B3 ทางบวก~~ → **✅ เทสต์แล้ว ผ่าน (2026-07-22, บัญชี miscen06 admin)** — หัวหน้าอัปรูปซ้ำเข้ากิจกรรม `test02` แล้วกด 🔍 หารูปซ้ำในชุดนี้ (10 รูป):
  - ขึ้น "พบ 1 กลุ่มที่อาจซ้ำ — เก็บไว้ 1 ใบ ที่เหลือกดลบ (ลงถังขยะ กู้คืนได้)"
  - "กลุ่มที่ 1 · 2 รูปคล้ายกัน" เรนเดอร์ครบทั้ง 2 ใบ: `test02_ใครก็ได้_003_140942.jpg` (Ben · 14:09:42) และ `test02_ใครก็ได้_002_140918.jpg` (Ben · 14:09:18)
  - **ปุ่ม 🗑️ ลบใบนี้ 2 ปุ่มอยู่ในกลุ่มเดียวกันไม่ชนกัน → ยืนยันบั๊ก #5 แก้ถูก** (คีย์ `{gi}_{idx}_{file_id}`)
  - ℹ️ pHash จับรูปถ่ายมุมใกล้กัน (วิวเครื่องบินคนละช็อต ห่างกัน 24 วินาที) ว่า "อาจซ้ำ" — ถูกต้องตามดีไซน์ที่ใช้คำว่า *อาจ* ไม่ได้ลบให้เอง
  - ⏳ **ยังไม่ได้กดปุ่มลบจริง** (ไม่แตะข้อมูลโดยไม่ขออนุญาต) → ยังไม่ยืนยันว่า ลบ→ลงถังขยะ→กู้คืน ทำงานครบวง
  - ℹ️ ชื่อไฟล์มีภาษาไทย (`test02_ใครก็ได้_003_...`) ใช้งานได้ปกติ ไม่พัง
- โฟลว์ขอเปิดบัญชี admin → หัวหน้าตั้งรหัส → login (ดู Log 2026-07-22 10:16-10:17 เหมือนผู้ใช้ลองเองไปแล้วบางส่วน: `ขอเปิดบัญชี admin` โดย miscen06@tfpoem.com แล้ว superadmin `ตั้งรหัสใหม่ให้ admin` 2 ครั้ง — ยังไม่ยืนยันว่า login เข้าได้จริง)
- ลืมรหัส → หัวหน้าตั้งรหัสแผนกให้ → เจ้าตัวเข้าได้
- กิจกรรม 🌐 โผล่หน้าแรกตอนออกจากระบบ / 🔒 ต้องไม่โผล่

### ✅ Review รอบ 2 (ผู้รีวิว, 2026-07-22) — ตรวจ diff `37c0466` แล้ว: แก้ถูกครบ 8 ข้อ ไม่มี regression
- **#1** `read_token` ครอบ `try/except Exception: return None` ครอบ logic ทั้งหมด (split/encode ascii/compare_digest/json/exp) — cookie เพี้ยนทุกแบบ = None ไม่ throw ✔
- **#2** `_render_admin_accounts` กรอง `USER_PENDING` ออกจากรายชื่อจริง (import `USER_PENDING` เพิ่มแล้ว) ✔
- **#3** เพิ่มการ์ด `su_user and su_pass and p` ก่อน `secrets_equal` ✔
- **#4** `_viewer_still_allowed` ถูกต้อง: สาธารณะผ่านเสมอ · เฉพาะคนต้องมีแถวแชร์ชื่อตรง + สถานะ≠ปิด · ถอนสิทธิ์=ลบแถว→ไม่เจอ→ถูกล็อก · สลับ public→private (viewer_name="(สาธารณะ)") ก็ไม่แมตช์→ถูกล็อก · อ่านชีตพังคืน False (ปลอดภัยไว้ก่อน) ✔ — ยืนยันแล้วว่าตอน login เก็บ `viewer_name` จริง (สาธารณะ/จากคอลัมน์ "ชื่อผู้ดู")
- **#5** ใส่ `uid=f"{i+j}_{file_id}"` ครบทุกกริด (superuser/admin/คลังทั่วไป + สแกนรูปซ้ำ `{gi}_{idx}_{file_id}`) · **เช็คแล้วว่า success path pop `del_key`/`purge_key` ทุกจุด** → ไม่มีบั๊กใหม่ "กล่องยืนยันค้างที่ตำแหน่งเดิม" ✔
- **#6** `add_user` ใช้ `_users_col(EMAIL_HEADER)` วางอีเมล (คอลัมน์ 1-5 ยังตรึง) · fallback ถ้าไม่เจอคอลัมน์ = ข้ามอีเมล ไม่ error ✔
- **#7** `set_user_status`/`set_activity_status` เติม `and row > 1` ✔ · `set_user_status` เพิ่ม `resolve_username()` (robust ขึ้น รองรับพิมพ์อีเมล/ตัวใหญ่ — เพิ่ม load_users 1 ครั้ง ถือว่าโอเค) ✔
- **#8** `delete_photo` เติม `load_data.clear()` ✔
- **สรุป:** ปล่อยขึ้น production ได้ · หลัง deploy ให้ทดสอบบนเว็บตามหัวข้อ "🌐 หลัง deploy ควรลองบนเว็บ" ด้านบน (เน้น: เข้าเว็บด้วย cookie เดิมไม่หลุด · ลบ/กู้คืนรูปยังปกติ · บัญชี pending อยู่แค่กล่องอนุมัติ)

---

## 📧 ขอเปิดบัญชี = กรอกอีเมลช่องเดียว (2026-07-22)

> **โจทย์จากหัวหน้า (2 ข้อ):** *"ตรงสมัครบัญชี ไม่ต้องใช้ user ใช้เมลไปเลย"* + *"เอารหัสผ่านออกตอนสมัคร เราเป็นคนให้รหัสเอง ... ปิดให้ใส่รหัสแบบนี้ เดี๋ยวก็ตั้งกันแล้วก็จำไม่ได้อีก"*

**ทางเดินใหม่ทั้งเส้น:** คนขอกรอก **อีเมลอย่างเดียว** → superuser เห็นในคิว **พิมพ์รหัสให้ + กดอนุมัติในปุ่มเดียว** → บอกรหัสเจ้าตัวเอง (ทางไลน์/ปากเปล่า) → เขา login ด้วยอีเมล+รหัสนั้น
- **ไม่มีช่องตั้งรหัสตอนขอบัญชีแล้ว** — บันทึก `password_hash` เป็นค่าว่าง ; `verify_secret()` คืน False เสมอเมื่อ hash ว่าง → **บัญชีที่ยังไม่ได้ตั้งรหัส login ไม่ได้แน่นอน** แม้เผลอถูกเปิดใช้งาน
- ถ้าเขาลอง login ก่อนได้รหัส → ขึ้น "⏳ ยังไม่ได้รับรหัส ผู้ดูแลกำลังตั้งให้" (ไม่ปล่อยให้งงว่าพิมพ์รหัสผิด — ไม่ถือว่าเผยข้อมูล เพราะกรอกอีเมลซ้ำในแท็บขอบัญชีก็รู้อยู่ดี)
- ตอนอนุมัติ **ตั้งรหัสก่อน ค่อยเปลี่ยนสถานะเป็นใช้งาน** — ถ้าตั้งรหัสพลาด บัญชีจะยังเข้าไม่ได้ (ปลอดภัยไว้ก่อน)
- แท็บเปลี่ยนชื่อเป็น **"ขอเปิดบัญชี admin"** · ตัดช่อง "ชื่อ-นามสกุล" ออกด้วย (อีเมลบอกอยู่แล้วว่าใคร)

- **ฟอร์มขอบัญชี** เหลือช่องเดียว = อีเมลบริษัท → เก็บอีเมล (พิมพ์เล็ก) ลงคอลัมน์ `username` **และ** คอลัมน์ `อีเมล` · เช็ค "ต้องมี @" + โดเมนบริษัท
- **หน้า login / ลืมรหัส** เปลี่ยนป้ายเป็น "อีเมล" · superuser สร้างบัญชีให้ก็กรอกอีเมลแทน username
- **`find_user()` รับได้ 3 แบบ**: username ตรงเป๊ะ → ไม่สนตัวพิมพ์ → ตรงคอลัมน์อีเมล → **บัญชีเก่า (`pim`) ยัง login เหมือนเดิม ไม่ต้อง migrate ชีต** (ไม่มีคอลัมน์ใหม่)
- **`resolve_username()` ใหม่** — แปลงสิ่งที่พิมพ์เป็น username จริงในชีต ก่อนส่งให้ฟังก์ชันที่หาแถวจากคอลัมน์ 1 (`set_user_password` / `request_password_reset`) ; `_login_staff` เก็บ `identity.username` จากชีตไม่ใช่จากที่พิมพ์ (ไม่งั้น "กิจกรรมของฉัน" กรองไม่เจอเวลาพิมพ์ตัวใหญ่)
- 🐛 **เจอตอนเทสต์:** `find_user("")` ไป**แมตช์บัญชีเก่าที่คอลัมน์อีเมลว่าง** → กันด้วยการคืน None ทันทีถ้าค่าที่พิมพ์ว่าง

**✅ ทดสอบในเครื่องผ่าน 35/35** — ขอบัญชีด้วยอีเมลอย่างเดียว/บันทึกพิมพ์เล็ก/รหัสว่าง · gmail+ไม่มี@+อีเมลซ้ำต่างตัวพิมพ์ → ปฏิเสธ · login ด้วยอีเมลพิมพ์ใหญ่ผ่าน · บัญชีเก่า `pim` ยังเข้าได้ · **บัญชีรหัสว่างเข้าไม่ได้ทั้งตอนใส่รหัสมั่วและตอนเว้นว่าง** · รหัสภาษาไทยไม่ throw · superuser ปกติ · **อนุมัติ = ตั้งรหัสจริงแล้วค่อยเปิดใช้งาน + จด Log** · ไม่พิมพ์รหัส → ไม่อนุมัติ

**⏳ รอทดสอบบนเว็บจริง:** ขอบัญชีด้วยอีเมล → (ลอง login ก่อน ต้องขึ้น "ยังไม่ได้รับรหัส") → superuser พิมพ์รหัสให้แล้วกดอนุมัติ → เจ้าตัว login ด้วยอีเมล+รหัสนั้นได้ · บัญชี `pim` เดิมยังเข้าได้ · ลืมรหัสกรอกอีเมล → เข้าคิว

---

## 🚪 รื้อ login ให้ง่ายขึ้น — รอบ 1 (2026-07-20)

> **โจทย์จากหัวหน้า:** "มันดูใช้ยากไป login หลายขั้นไป — คลังทั่วไป / ส่งรูปกิจกรรม / ดูอัลบั้ม ควรรวมอยู่ในหน้าเดียวกัน"
> **สรุปที่คุยกัน:** ปัญหาจริงไม่ใช่แค่จำนวนขั้น แต่เป็น **เขาวงกต 4 การ์ด** (กดผิดต้องกด "← กลับหน้าแรก" เริ่มใหม่)

**🔑 ทางที่เลือก: "ช่องเดียวจบ"** — ผู้ใช้ไม่ต้องรู้ว่าตัวเองเป็นประเภทไหน ใส่รหัสที่มีในมือ ระบบเดาให้เองแล้วพาไปหน้าที่ตรงกับสิทธิ์
- ทำได้เพราะรหัส 3 แบบเป็นคนละค่ากันอยู่แล้ว → `identify_code()` ไล่เช็ค APP_PASSWORD → รหัสกิจกรรม → รหัสดูอัลบั้ม
- **ไม่ต้องสร้างบัญชีให้พนักงานทุกคน** (ทางเลือกนั้นถูกตัดทิ้ง เพราะสวนทางกับ "ไม่ยุ่งยาก" — คนลืมรหัสต้องมีคนรีเซ็ตให้)

**🆕 `session_store.py` — "จำฉันไว้ 1 วัน" (cookie)**
- Streamlit เก็บ login ใน `session_state` ซึ่ง **หายทุกครั้งที่ refresh** → นี่คือต้นเหตุจริงที่ต้องกรอกรหัสบ่อย
- token เซ็นด้วย HMAC-SHA256 (กุญแจ = `HASH_SALT` เดิม) — **ห้ามเชื่อ cookie ตรงๆ** เพราะผู้ใช้แก้เองได้
- **อ่าน** cookie ด้วย `st.context.cookies` (sync จาก request header — เลี่ยงปัญหา component อ่านแบบ async แล้วหน้า login กะพริบ) · **เขียน/ลบ** ด้วย `extra-streamlit-components`
- ถ้า lib พัง/หาย → **แอปยังทำงานปกติ แค่ไม่จำ login** (กลืน error หมด)
- แก้จำนวนวันที่ `REMEMBER_DAYS` ที่เดียว (ข้อความบนจออ้างค่านี้ผ่าน `auth.REMEMBER_LABEL`)

**🔒 `restore_session()` — ไม่เชื่อ cookie อย่างเดียว เช็คสิทธิ์ซ้ำกับชีตทุกครั้ง**
- role=user → กิจกรรมต้องยังเปิด (`is_activity_open`) · role=viewer → ดูย้อนหลังได้แม้ปิดแล้ว
- role=admin → บัญชีต้องยัง "ใช้งาน" + role ยังเป็น admin · role=superuser → username ต้องตรง secrets
- ⚠️ ถ้าไม่เช็คซ้ำ = สิทธิ์ค้าง 1 วันหลังถูกปิดบัญชี/ปิดกิจกรรม

**🖼️ แท็บ "รูปของฉัน"** (`page_activity_user.py`) — คนส่งรูปเห็นรูปที่ตัวเองส่ง
- กรอง 2 ชั้น: `activity_id` **และ** ชื่อผู้ส่ง → **ไม่เห็นรูปของคนอื่น** (คงหลัก "ถ่าย ≠ ดู" จาก Round 3) · ดาวน์โหลดได้ ลบไม่ได้
- ไม่ต้อง migrate schema — คอลัมน์ "ผู้ส่ง" มีอยู่แล้ว

**QR deep-link เดิมยังใช้ได้** — `?actcode=` รหัสถูกแล้วข้ามช่องรหัสไปกรอกชื่อเลย (เร็วกว่าเดิม)

**✅ ทดสอบในเครื่องผ่าน 32/32** (stub streamlit + import จริงทุกโมดูล รวม `app.py` ที่รัน `main()` ตอน import)
- token: เซ็น/อ่านกลับ/หมดอายุ/**แก้ payload เป็น superuser แล้วโดนตีตก**/ขยะไม่ throw
- `identify_code`: รหัสคลังทั่วไปถูก · รหัสมั่วภาษาไทย**ไม่ throw TypeError** (บทเรียน `compare_digest` เดิม)
- `restore_session`: cookie ปลอม/superuser ชื่อไม่ตรง/admin ไม่มีบัญชี → **ไม่ได้สิทธิ์ทุกกรณี**

**⏳ รอทดสอบบนเว็บจริง:**
1. ⚠️ **Reboot app ก่อนเลย** — มี dep ใหม่ `extra-streamlit-components` (บทเรียนเดิมจาก `piexif`)
2. ⚠️ **จุดเสี่ยงที่สุด = cookie จริงบนเบราว์เซอร์** (ในเครื่องเทสต์ด้วยของปลอมเท่านั้น) — เทสต์: login → ติ๊กจำฉันไว้ → **refresh หน้า** → ต้องไม่หลุด · กดออกจากระบบ → ต้องหลุดจริง
3. รหัสกิจกรรม/รหัสดูอัลบั้มของจริง (ในเครื่องเทสต์ไม่ได้ เพราะชีตเก็บ hash)
4. แท็บ "รูปของฉัน" — ส่งรูปแล้วเห็นของตัวเอง · **ไม่เห็นของคนอื่น**

---

## 📤 กิจกรรมแบบ "ส่งเลย ไม่ต้องมีรหัส" (2026-07-20)

> **โจทย์จากหัวหน้า:** *"เวลาส่งรูปกันเนี่ย เค้าก็คงอยากส่งเลย ไม่ต้องไรมาก เหมือนในกลุ่มไลน์ ใครอยู่ก็ส่งๆ ไป"*
> แต่ยังต้องมีแบบใช้รหัสอยู่: **แห่เทียนพรรษา** = ทั้งโรงงาน ไม่มีความลับ ใครส่งก็ได้ · **ลูกค้าเข้าเยี่ยมชม** = เฉพาะ sale/RD ต้องมีรหัส

**ทางที่เลือก:** เพิ่ม "โหมดการเข้าร่วม" ต่อกิจกรรม (คนละเรื่องกับ "การมองเห็น" ที่คุมการ *ดู* อัลบั้ม)
- 🌐 **ใครก็ได้** — โผล่เป็นปุ่มบนหน้าแรก `📤 ส่งรูปเข้ากิจกรรมที่เปิดให้ทุกคน` กดแล้วกรอกแค่ชื่อตัวเอง → ส่งรูปได้เลย
- 🔒 **ต้องมีรหัส** — เหมือนเดิมทุกอย่าง (ช่องรหัสหน้าแรก / QR deep-link)
- สลับไปมาได้ทีหลังด้วยปุ่มเดียวในการ์ดกิจกรรม (ทั้ง admin และ superuser)

**⚠️ ค่าเริ่มต้นตอนสร้างกิจกรรมใหม่ = 🌐 ใครก็ได้** (ตามที่หัวหน้าอยากให้ส่งง่ายเป็นค่าปกติ) — แต่ **กิจกรรมเก่าที่มีอยู่แล้ว คอลัมน์ว่าง = ยังต้องใช้รหัสเหมือนเดิม** (ไม่มีอะไรถูกเปิดให้ทุกคนโดยไม่ตั้งใจ)
**ℹ️ ยังออกรหัสให้ทุกกิจกรรมเสมอ** แม้เป็นแบบใครก็ได้ — เผื่อเปลี่ยนใจสลับเป็น 🔒 ทีหลังจะได้มีรหัสพร้อมใช้ · กิจกรรมแบบใครก็ได้ยังขอ QR ไปแปะหน้างานได้ (พับไว้ใน expander)

**schema:** Activities + คอลัมน์ **8 = `การเข้าร่วม`** (""/`ใครก็ได้`/`ต้องมีรหัส`) — ✅ migrate ชีต production แล้ว (6 กิจกรรมเดิมคอลัมน์ใหม่ว่าง)
**ของใหม่:** `google_utils`: `get_activity_join()` · `set_activity_join()` · `open_join_activities()` (กรอง auto-close 7 วันด้วย) · `add_activity(..., join=)` · `auth._render_open_activities()`

**✅ ทดสอบในเครื่องผ่าน 57/57** — หน้าแรกโชว์เฉพาะกิจกรรมที่ตั้งเป็นใครก็ได้ **และยังเปิดอยู่** · กิจกรรมต้องมีรหัสไม่หลุดมาให้กดส่งฟรี · กิจกรรมปิดแล้วไม่โผล่ · **คอลัมน์ว่าง → ต้องมีรหัส (ปลอดภัยไว้ก่อน)**

**⏳ รอทดสอบบนเว็บจริง:** สร้างกิจกรรมแบบใครก็ได้ → ออกจากระบบ → หน้าแรกต้องเห็นปุ่มชื่อกิจกรรม → กด → กรอกชื่อ → ส่งรูปได้ · สร้างแบบต้องมีรหัส → ต้อง **ไม่** โผล่บนหน้าแรก · กดปุ่มสลับโหมด → ปุ่มบนหน้าแรกโผล่/หายตาม

---

## 🙋 รอบ 2 — admin สมัครเอง + หัวหน้ากดอนุมัติ (2026-07-20)

> รอบ 1 ผู้ใช้เทสต์บนเว็บผ่านแล้ว ("เทสละ น่าจะไม่ติด") → ทำรอบ 2 ต่อ

**ทางเดินใหม่:** คนอยากเป็น admin → หน้าแรก > `🔐 สำหรับ admin` > แท็บ **"สมัครบัญชี admin"** → กรอก username/ชื่อ/อีเมลบริษัท/รหัส 2 ครั้ง → บันทึกสถานะ `รออนุมัติ` **ยัง login ไม่ได้** → superuser เห็นกล่อง "⏳ มี N คนรอคุณอนุมัติ" บนสุดของแท็บจัดการบัญชี → กด ✅ อนุมัติ (→ `ใช้งาน`) หรือ 🚫 ปฏิเสธ (ลบแถว มีขั้นยืนยัน) → จดลง Log ทั้งคู่

**โดเมนอีเมลบริษัท (ผู้ใช้ยืนยันแล้ว 2026-07-20) — ตั้งที่ `config.py` → `COMPANY_EMAIL_DOMAINS`:** `@tfpoem.com` · `@tfpthailand.com` · `@tfpcar.com` (ตรงอันใดอันหนึ่งก็ผ่าน) · เพิ่ม/ลบโดเมนแก้ที่ list นี้ที่เดียว · ตั้งเป็น `[]` = ไม่บังคับโดเมน

**schema:** แท็บ Users + คอลัมน์ **6 = `อีเมล`** (ต่อท้ายเสมอ — คอลัมน์ 1-5 ห้ามขยับ เพราะ `add_user`/`set_user_status` อ้างตำแหน่ง) · ✅ รัน `ensure_schema()` ตัวจริงกับชีต production แล้ว (บัญชี `pim` เดิมอยู่ครบ อีเมลว่าง = backward-compatible)

**ของใหม่ในโค้ด:** `google_utils`: ค่าคงที่ `USER_ACTIVE/USER_DISABLED/USER_PENDING` + `pending_users()` + `email_taken()` (กันสมัครซ้ำอีเมลเดิม ไม่สนตัวพิมพ์) + `add_user(..., email=)` · `auth._signup_admin()` · `page_activity_superuser._render_pending_approvals()`

**🔒 จุดที่ระวังไว้:**
- `_login_staff` **เช็ครหัสผ่านให้ถูกก่อน** ค่อยบอกว่า "รออนุมัติ/ถูกระงับ" — ถ้าบอกก่อนเช็ค = เผยให้คนนอกรู้ว่ามี username นี้อยู่จริง
- `restore_session` ใช้ `USER_ACTIVE` → cookie ของบัญชีที่ยังรออนุมัติ/ถูกระงับ คืนสิทธิ์ไม่ได้
- ⚠️ **กันเข้าใจผิด:** อีเมลที่พิมพ์เอง **ไม่ได้พิสูจน์ว่าเป็นเจ้าของอีเมลจริง** (ไม่ได้ส่งเมลยืนยัน) — ความปลอดภัยมาจาก "หัวหน้ารู้จักคนที่อนุมัติ" เขียนเตือนไว้บนหน้าจอ superuser แล้ว. ถ้าจะพิสูจน์จริงต้องตั้ง SMTP (มติ: ไม่ทำ)

**✅ ทดสอบในเครื่องผ่าน 37/37** (stub streamlit + import จริงทุกโมดูล): บัญชีรออนุมัติ+รหัสถูก→เข้าไม่ได้ · รหัสผิด→บอกแค่ "รหัสผิด" ไม่เผยว่ามีบัญชี · บัญชีปิด→เข้าไม่ได้ · รหัสไทยไม่ throw · cookie รออนุมัติไม่คืนสิทธิ์ · ครบ 3 โดเมนผ่าน + พิมพ์ใหญ่/มีช่องว่างก็ผ่าน · โดเมนหลอก (`@nottfpoem.com`, `@tfpcar.com.evil.com`, gmail) ไม่ผ่าน · `add_user` เขียน 6 ช่องถูกตำแหน่ง

---

## 🔑 ลืมรหัสผ่าน — ทำให้ง่ายที่สุด (2026-07-20)

> **โจทย์จากหัวหน้า:** "อะไรก็ได้ แต่อย่าหางานเพิ่มให้ตัวเราเอง เช่น ลืมรหัส เราก็ต้องมานั่งใส่รหัสใหม่ให้"
> **เดิมแย่กว่าที่คิด:** ไม่มีทางรีเซ็ตรหัสเลย — ลืม = superuser ต้องลบบัญชีแล้วสร้างใหม่

**ทางที่เลือก (ผู้ใช้เสนอเอง):** superuser ตั้งรหัสใหม่เป็น **รหัสเข้าเครื่องของแผนก** ที่เจ้าตัวจำได้อยู่แล้ว → **ไม่ต้องส่งรหัสใหม่ให้กันเลย** ไม่ต้องพิมพ์ไลน์ ไม่ต้องกลัวเขาลืมซ้ำ

- หน้าแรก > `🔐 สำหรับ admin` > แท็บ **"ลืมรหัสผ่าน"** → กรอก username → เข้าคิว (`ขอรีเซ็ต` = วันเวลา) **ไม่แตะรหัสเดิม** (นึกออกทีหลังยัง login ได้)
- superuser แท็บจัดการบัญชี เห็นกล่องแดง **"🔑 มี N คนลืมรหัส"** → พิมพ์รหัสใหม่ → กดปุ่มเดียวจบ + ล้างคิว + จด Log
- ตั้งรหัสให้บัญชีไหนก็ได้ตลอดเวลา (expander `🔑 ตั้งรหัสใหม่ให้ ...` ใต้ทุกบัญชี) เผื่อโทรมาบอกปากเปล่า ไม่ได้กดปุ่ม
- admin เปลี่ยนรหัสเองได้ (กล่องพับ `🔑 เปลี่ยนรหัสผ่านของฉัน` ในแท็บกิจกรรมของฉัน — ต้องใส่รหัสเดิมก่อน)

**⚖️ ตัดสินใจแล้ว: ไม่บังคับเปลี่ยนรหัสหลังถูกรีเซ็ต** — ผมเสนอให้บังคับ (เพราะรหัสแผนกใช้ร่วมกันทั้งแผนก `user: hr / pass: hr6789` = คนอื่นสวมรอยได้ Log จะโทษผิดคน) **ผู้ใช้ตัดสินใจไม่เอา** เหตุผล: *"แผนกเดียวกัน มันก็ดูกันได้ ปล่อยไป"* — ยอมรับความเสี่ยงนี้แล้วโดยรู้ตัว แลกกับความง่าย · ถ้าวันหน้าต้องสืบว่าใครลบรูปจริงๆ ให้กลับมาอ่านย่อหน้านี้
> 💡 **บทเรียน (หัวหน้าสอน):** *"นายต้องคิดว่า ถ้านายใช้เอง มันจะต้องเป็นยังไง นายถึงอยากใช้"* — อย่าใส่ขั้นตอนความปลอดภัยตามตำราถ้ามันไม่ได้กันอะไรจริงในบริบทนี้ ให้คิดจากมุมคนใช้ก่อน

**schema:** Users + คอลัมน์ **7 = `ขอรีเซ็ต`** (""=ไม่ได้ขอ / วันเวลา) — ✅ migrate ชีต production แล้ว
**ของใหม่:** `google_utils`: `request_password_reset()` · `set_user_password()` · `reset_requests()` · `_users_col()` (หาเลขคอลัมน์จากหัวตารางจริง ไม่ hardcode) · `auth`: `_forgot_password()` · `render_change_password_box()`

**✅ ทดสอบในเครื่องผ่าน 48/48** — ขอรีเซ็ตไม่แตะรหัสเดิม · ตั้งรหัสใหม่แล้วรหัสเก่าใช้ไม่ได้ · ล้างคิวอัตโนมัติ · username ไม่มีจริง → คืน False (แต่หน้าจอขึ้นข้อความเดียวกัน กันไล่เดาว่ามีใครในระบบ)

**⏳ รอทดสอบบนเว็บจริง:** กดลืมรหัส → superuser เห็นคิว → ตั้งรหัสเป็นรหัสแผนก → เจ้าตัว login ได้เลย ไม่มีหน้าจอบังคับอะไรมาขวาง · admin เปลี่ยนรหัสเอง (ใส่รหัสเดิมผิด → ต้องไม่ให้เปลี่ยน)

---

**⏳ รอทดสอบบนเว็บจริง (ของรอบ 2 สมัคร/อนุมัติ):**
1. สมัครใหม่ 1 บัญชี → ต้องขึ้น "รอหัวหน้าอนุมัติ" · ลอง login ทันที → ต้องเข้าไม่ได้ (ขึ้น ⏳ รออนุมัติ)
2. superuser เห็นกล่องรออนุมัติ → กดอนุมัติ → บัญชีนั้น login ได้จริง + แท็บ Log จด "อนุมัติบัญชี admin"
3. ลองปฏิเสธอีก 1 บัญชี → หายจากรายการ + สมัครใหม่ด้วยอีเมลเดิมได้
4. อีเมล gmail → ต้องถูกปฏิเสธ · อีเมลซ้ำ → ต้องถูกปฏิเสธ

---

## ✅ จุดพักงาน (2026-07-17) — เทสต์ผ่านหมดแล้ว ไม่มีงานค้าง

**✅ ของรอบ 2026-07-16 ทดสอบบนเว็บจริงผ่านหมดแล้ว** (ผู้ใช้ยืนยัน "เทสหมดละ ได้หมด") — แผนหลัก 5 ฟีเจอร์ + ของเสริมทั้งหมดใช้งานได้จริง

**✅ เสร็จวันนี้ (2026-07-17):** เผยแพร่รูปกิจกรรมเข้าคลังทั่วไป + คลังภาพแบบโฟลเดอร์ — push แล้ว commit `ff5f041` (รายละเอียดหัวข้อ "📁 เผยแพร่รูป + คลังภาพแบบโฟลเดอร์" ด้านล่าง)

**✅ ทดสอบบนเว็บจริงแล้ว (2026-07-17 ~15:00-15:10) — ผ่าน 8/8 ไม่เจอบั๊ก**
> ทดสอบผ่านเบราว์เซอร์จริงบน image-warehouse-mis.streamlit.app ด้วยบัญชี superadmin
> ℹ️ **ไม่ต้อง reboot** — รอบนี้ไม่เจอ ImportError (เข้าใช้ได้เลยหลัง redeploy) ต่างจากที่คาดไว้

- [x] เปิดคลังทั่วไป → เห็นการ์ดโฟลเดอร์แผนก (ฝ่ายHR/MIS/PDC/PLN อย่างละ 1 รูป) + ปุ่ม "แสดงแผนกที่ยังไม่มีรูป (20)" ✅
- [x] กดเข้าโฟลเดอร์แผนก → เห็นรูป ("แอร์ 36" · ฝ่ายHR/อุปกรณ์) · กรองหมวดปุ่มแนวนอน 6 อัน · expander "ตัวกรองเพิ่มเติม (คำค้น/ช่วงวันที่)" · ปุ่ม "กลับไปหน้าโฟลเดอร์" ทำงาน · ZIP + ลบรูปได้ปกติ ✅
- [x] หน้าคลังภาพเปิดเร็วขึ้นจริง — หน้าโฟลเดอร์ขึ้นทันที ไม่มีดีเลย์ (ตัดรูปปกออกได้ผล) ✅
- [x] กด 🌐 เผยแพร่รูป 1 ใบ (`hr_009_165451.jpg`) → โฟลเดอร์กิจกรรม `hr` ในคลังทั่วไป **5 → 6 รูป** + รูปโผล่ครบ · ปุ่มเปลี่ยนเป็น "ยกเลิกเผยแพร่" + ขึ้นป้าย "🌐 เผยแพร่อยู่ในคลังทั่วไป" ✅
- [x] โฟลเดอร์กิจกรรม **ไม่มีปุ่มลบ** — มีแค่ "⬇️ ดาวน์โหลดรูปนี้" (ต่างจากโฟลเดอร์แผนกที่มีปุ่มลบ) + รูปยังอยู่ในอัลบั้มกิจกรรมด้วย ✅
- [x] กดยกเลิกเผยแพร่ → โฟลเดอร์ `hr` **6 → 5 รูป** กลับสภาพเดิม ✅
- [x] แท็บ Log จดครบ: `เผยแพร่รูป` (15:03:52) และ `ยกเลิกเผยแพร่รูป` (15:05:17) — superadmin / superuser / hr_009_165451.jpg / hr (รายการ 20 → 22) ✅
- [x] superuser มีปุ่มเผยแพร่เหมือน admin (แท็บ "คลังภาพทุกกิจกรรม") ✅

> ✅ **ข้อมูลกลับสภาพเดิมหมดแล้ว** — เผยแพร่ 1 ใบแล้วยกเลิกคืน ไม่มีข้อมูลค้าง เหลือแค่ Log 2 บรรทัดที่เพิ่มมา (ถูกต้องตามดีไซน์)

**🧹 เจอระหว่างเทสต์ (ไม่ใช่บั๊ก แต่ควรเคลียร์):** ในคลังภาพกิจกรรม `hr` มี **รูปทดสอบปนอยู่เยอะ** (ภาพเกม Battle Pass, ตัวการ์ตูน AI, หุ่นยนต์) — ถ้าจะเปิดเว็บให้หัวหน้า/คนอื่นดู ควรลบทิ้งก่อน (ลบลงถังขยะได้ กู้คืนได้ 30 วัน)

**🎨 ถ้าอยากปรับหลังเทสต์:** ขนาด/จำนวนการ์ดต่อแถว (`COLS_PER_ROW` = 4) · ข้อความบนการ์ด · จำนวนรูปต่อหน้า (`PAGE_SIZE` = 12) — แก้ที่หัวไฟล์ `page_gallery.py`

**📋 ของเสริมที่ยังไม่ทำ (ถ้าอยากทำต่อ):**
- หน้า error สวยๆ ตอน token พัง (invalid_grant) · แก้หัวข้อ/แผนกรูปหลังอัป · ค้นหาในอัลบั้ม · ล้างถังขยะอัตโนมัติ(ลบแถวค้าง>30วัน) · แจ้งเตือน Drive ใกล้เต็ม

**🔑 วิธีทดสอบโค้ดก่อน push (สำคัญ — local streamlit รันไม่ได้):** สตับ streamlit ปลอมแล้ว import จริงทุกโมดูล (compile เฉยๆ ไม่จับ NameError ตอนโหลด) · ใส่ st.secrets จริง = รัน ensure_schema migrate ชีตได้เลย

---

## 🆘 กู้คืนด่วน: เว็บเชื่อม Google ไม่ได้ `invalid_grant: Token has been expired or revoked` (runbook)

> เกิดครั้งแรก 2026-07-03 (ตอนกำลัง present) — refresh token ถูก Google เพิกถอน เว็บดูรูปไม่ได้ทั้งหมด
> กู้คืนเสร็จใน ~5 นาที ทำตามนี้ครั้งหน้าจะเร็วกว่าเดิม:

**อาการ:** เปิดเว็บแล้วขึ้น error เชื่อม Google ไม่ได้ `('invalid_grant: Token has been expired or revoked.')` — ทุกหน้าที่ต้องอ่านรูป/ชีตพัง

**วิธีกู้ (5 ขั้น):**
1. เปิด terminal ที่โฟลเดอร์โปรเจกต์ → รัน `python get_refresh_token.py` (สคริปต์บังคับเลือกบัญชีทุกครั้งแล้ว)
2. เบราว์เซอร์เด้ง → **เลือกบัญชี `tfp.data.mis@gmail.com` เท่านั้น** (เจ้าของ Sheet/Drive — เลือกผิดบัญชีจะเข้าคลังไม่ได้) → ถ้าเจอ "Google hasn't verified this app" กด Advanced → Go to... (unsafe) → Allow ทุกหน้า
3. ก๊อป `refresh_token = "..."` ที่ terminal พิมพ์ออกมา
4. วาง 2 ที่:
   - ไฟล์ `.streamlit/secrets.toml` (บรรทัด refresh_token ใต้ `[google_oauth]`)
   - **Streamlit Cloud → แอป image-warehouse-mis → ⋮ Settings → Secrets** (บรรทัด refresh_token ใต้ `[google_oauth]`) → Save (แอป reboot เอง ~30 วิ)  ← **อันนี้คือตัวที่กู้เว็บจริง** local ไม่เกี่ยวกับเว็บ
5. (ก่อน save cloud) ทดสอบ token ใหม่ในเครื่องก่อนได้: รันสคริปต์เล็กๆ เปิด Sheet + เช็ค `drive.about().get(fields='user')` ต้องได้อีเมล `tfp.data.mis@gmail.com` = เลือกบัญชีถูก

**หมายเหตุต้นเหตุ (2026-07-03):** consent screen เป็น "In production" อยู่แล้ว → **ไม่ใช่**เรื่องหมดอายุ 7 วัน. สาเหตุที่น่าจะเป็น = ออก refresh token ใหม่บ่อยจน Google ตัด token เก่าสุดทิ้ง (มีลิมิตต่อ client/บัญชี) หรือมีเหตุการณ์ความปลอดภัยของบัญชี. **บทเรียน: อย่ารัน `get_refresh_token.py` พร่ำเพรื่อ** — รันเฉพาะตอนจำเป็นจริง เพราะทุกครั้งที่ออกใหม่เสี่ยงเบียด token เก่าให้ถูกถอน

**✅ ทำแล้ว (กันเชิงรุก, 2026-07-03): keep-alive ด้วย GitHub Actions** — ไฟล์ `.github/workflows/keep-alive.yml` ping `https://image-warehouse-mis.streamlit.app/healthz` ทุก ~10 นาที ช่วง 00:00-11:59 UTC (07:00-18:59 เวลาไทย) ทุกวัน → กันแอปหลับ (ลด cold start มือถือ + token ถูกใช้สม่ำเสมอ). ฟรีเพราะ repo public.
- ⚠️ ต้องใช้ `/healthz` (คืน 200 ตรง) — `/` และ `/_stcore/health` โดน 303 redirect ไป `share.streamlit.io/-/auth` (curl ตามไม่ได้เพราะไม่มีเบราว์เซอร์/JS)
- ⚠️ GitHub ปิด scheduled workflow อัตโนมัติถ้า repo เงียบ (ไม่มี commit) เกิน 60 วัน → เข้าแท็บ Actions กด "Enable workflow" อีกครั้งถ้าเงียบนาน
- ยืนยันแล้ว: กดรันเอง (workflow_dispatch) → `GET /healthz -> HTTP 200` สำเร็จ

**ยังพิจารณาทำต่อ:** ปรับแอปให้โชว์ข้อความสวยๆ บอกวิธีแก้แทนจอ error ดิบ ถ้า token พังอีก

---

## ✅ ทำเสร็จแล้ว

| เฟส | งาน | สถานะ |
|-----|-----|-------|
| C | OAuth + refresh token + verify เข้าถึง Sheet/Drive | ✅ |
| 1 | ติดตั้งไลบรารี (แก้ starlette ให้เข้ากับ Python 3.14) | ✅ |
| 2 | ระบบ login + ตัวเชื่อม Google + แก้หัวตาราง Sheet เป็น 8 คอลัมน์ | ✅ |
| 3 | หน้าส่งรูป (ย่อรูป → อัป Drive → บันทึก Sheet) — *ทดสอบ pipeline จริงผ่าน* | ✅ |
| 4 | หน้าคลังภาพ (filter + grid + ดาวน์โหลดเดี่ยว/zip + แบ่งหน้า) | ✅ |
| 5 | Dashboard + start.bat + README.md — *ทดสอบ AppTest ทั้ง 3 หน้าผ่าน* | ✅ |

โค้ดครบทุกไฟล์ compile ผ่านหมด

---

## ✅ Blocker ปลดล็อกแล้ว (2026-06-25)

**OAuth client เดิมกลับมาใช้ได้แล้ว** — Google เปิด client คืนหลังพ้นช่วง anti-abuse
- ทดสอบยิง refresh token **1 ครั้ง** → `REFRESH_OK` (ได้ access token)
- อ่าน Sheet `Image_Warehouse_DB` ได้ (998 rows)
- รัน AppTest: login ผ่าน, 3 แท็บเรนเดอร์, 0 error, sidebar ขึ้น "เชื่อม Google Sheet สำเร็จ"
- **ไม่ต้องสร้าง client ใหม่ / ไม่ต้องขอ refresh token ใหม่** — ค่าใน `.streamlit/secrets.toml` ใช้ได้เลย

---

## ▶️ เหลือทำต่อ

1. ✅ **เปลี่ยนรหัสผ่านแล้ว** (2026-06-25): `APP_PASSWORD` เปลี่ยนจาก `1234` เป็นรหัสใหม่ใน `.streamlit/secrets.toml` — ยืนยันด้วย AppTest ว่า login รหัสใหม่ผ่าน / รหัสเก่าใช้ไม่ได้แล้ว
2. ✅ **Deploy เสร็จสมบูรณ์ (2026-06-25)** 🎉
   - Live: **https://image-warehouse-mis.streamlit.app/**
   - โค้ด: https://github.com/Ben45-alt/image-warehouse (public, ไม่มีไฟล์ลับ)
   - Secrets วางใน Streamlit Cloud แล้ว
   - OAuth consent screen = **Production** → refresh token ไม่หมดอายุ 7 วัน

> หมายเหตุ: ตอนนี้พร้อมใช้งานเต็มรูปแบบบนเครื่องแล้ว — ดับเบิลคลิก `start.bat` → login ด้วยรหัสใหม่ → ใช้ได้ครบ 3 หน้า

---

## 🐛 บั๊กที่แก้แล้ว

**(2026-07-17) ใส่รหัสผิดแล้วขึ้น `TypeError` แทนที่จะบอกว่ารหัสผิด** (`auth.py`)
- อาการ: หน้า "🔐 เข้าสู่ระบบ" (admin/superuser) พิมพ์รหัสผิด → เด้ง `TypeError` ดิบใส่หน้าผู้ใช้ ไม่ขึ้นข้อความ "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"
- สาเหตุ: `_login_staff()` เทียบรหัส superuser ด้วย `hmac.compare_digest(p, su_pass)` **ตรงๆ กับสตริง** — ฟังก์ชันนี้รองรับเฉพาะ ASCII ถ้ามีอักขระ non-ASCII แม้ตัวเดียวจะ **โยน `TypeError: comparing strings with non-ASCII characters is not supported` แทนที่จะคืน False** → เจอง่ายมากเพราะ **ลืมสลับแป้นพิมพ์ ยังค้างภาษาไทยอยู่** แล้วพิมพ์รหัส (reproduce ยืนยันแล้ว)
- แก้: เพิ่ม `secrets_equal(a, b)` ใน `auth.py` — sha256 ทั้งสองฝั่งเป็น bytes ก่อนค่อย `compare_digest` → รับอักขระอะไรก็ได้ (ไทย/อีโมจิ) และยังกัน timing attack เหมือนเดิม ; `_login_staff` เปลี่ยนมาใช้ตัวนี้
- ℹ️ จุดอื่นปลอดภัยอยู่แล้ว: `verify_secret()` (admin จากชีต + รหัสกิจกรรม + รหัสดูอัลบั้ม) hash เป็น hex ASCII ก่อนเทียบเสมอ · `_login_general()` ใช้ `==` ธรรมดา ไม่ throw
- ✅ ทดสอบผ่าน: รหัสผิด 6 แบบ (ไทยล้วน/แป้นค้างไทย/ไทยปนอังกฤษ/อีโมจิ/ascii/ว่าง) → คืน False ไม่ throw · รหัสถูกยัง login ได้ · รหัสจริงที่เป็นภาษาไทยก็ใช้ได้
- ⚠️ **บทเรียน: ห้ามใช้ `hmac.compare_digest()` กับ str ที่รับจากผู้ใช้โดยตรง** — hash เป็น bytes ก่อนเสมอ

**(2026-06-25) รูปค้างหลังบันทึก → เสี่ยงบันทึกซ้ำ** (`page_upload.py`)
- อาการ: ช่องอัปรูปอยู่ "นอก" `st.form` แต่ `clear_on_submit` ล้างได้แค่ช่องในฟอร์ม → หลังกดบันทึก รูปเดิมยังค้าง พอเปลี่ยนแผนก/หมวด/หัวข้อแล้วกดบันทึกอีกครั้ง รูปเดิมถูกอัปซ้ำเป็นรายการใหม่ (reproduce ยืนยันแล้ว)
- แก้: ย้ายช่องอัปรูป (`file_uploader`/`camera_input`) เข้าไป "ใน" `st.form` (radio เลือกวิธียังอยู่นอกฟอร์มเพื่อสลับได้ทันที) → frontend ล้างรูปให้พร้อมช่องอื่นตอน submit
- เช็คเพิ่ม: ลำดับหัวตาราง Sheet ตรงกับลำดับที่โค้ดเขียน + ค่าที่เลือก (แผนก/หมวด) บันทึกถูกช่อง — ไม่มีบั๊กแมปคอลัมน์

**(2026-06-25) config.py ตกคอมม่า → แผนกรวมกัน** (`config.py`)
- string หลายตัววางติดกันไม่มีคอมม่า → Python ต่อเป็นก้อนเดียว (เช่น HR+HRSL, และก้อนยาว MTN→CARRICHS)
- แก้: เติมคอมม่าครบทุกบรรทัด (24 ฝ่ายแยกกัน) + คอมเมนต์เตือน
- เคลียร์ข้อมูลทดสอบเก่า 3 แถวใน Sheet + ลบรูปใน Drive (เคยใช้ชื่อแผนกเก่าที่ไม่มีใน config ใหม่ ทำให้ Dashboard/ฟิลเตอร์เพี้ยน)

**(2026-06-25) กด Enter ที่หน้า login ไม่ submit** (`app.py`)
- เดิมใช้ `text_input` + `button` แยกกัน → Enter ไม่กดปุ่ม
- แก้: ครอบด้วย `st.form` (ช่องเดียว) → Enter = submit ทันที

**(2026-06-25) พรีวิวรูปหน้าคลังภาพไม่ขึ้น** (`page_gallery.py`, `google_utils.py`)
- เดิมใช้ URL `drive.google.com/thumbnail?id=...` → รูปเพิ่งอัป Google ยังไม่สร้าง thumbnail / โดนบล็อก hotlink
- แก้: เพิ่ม `get_image_bytes()` (cache 10 นาที) โหลด bytes รูปจริงมาแสดงด้วย `st.image` — ยืนยัน round-trip ผ่าน

**(2026-06-25) บันทึกรูปล้มเหลว WinError 10053 (เน็ต/AV ตัดการเชื่อมต่อ)** (`google_utils.py`)
- อาการ: อัปรูปแล้วขึ้น `[WinError 10053] An established connection was aborted...` ระหว่างต่อ Google
- แก้: เพิ่ม `_retry()` ลองใหม่อัตโนมัติเมื่อเจอ ConnectionError/TimeoutError (1.5s,3s,4.5s) + ใส่ `num_retries=5` ใน Drive API
- ครอบ `upload_to_drive` (create + permission, seek buffer ก่อน retry กันอัปไฟล์เปล่า) และ `append_row`
- ยืนยัน: retry กู้คืนได้จริง / error ที่ไม่ใช่เน็ตไม่ถูก retry / อัปโหลดจริงยังทำงานปกติ

---

## ➕ ฟีเจอร์ที่เพิ่มหลัง deploy

**(2026-06-25) ปุ่มลบรูป ในหน้าคลังภาพ** (`page_gallery.py`, `google_utils.py`)
- แต่ละรูปมีปุ่ม **🗑️ ลบรูปนี้** → กดแล้วถามยืนยัน (✅ ลบเลย / ❌ ยกเลิก) กันกดพลาด
- ลบ = ลบไฟล์ใน Drive + ลบแถวใน Sheet (หาแถวจากคอลัมน์ "ลิงก์รูป" ที่ไม่ซ้ำกัน) ผ่าน `delete_photo()`
- ยืนยันแล้ว: round-trip ลบจริงครบ (Drive หาย + แถวหาย + ไม่มีตกค้าง) + กริดเรนเดอร์ปกติ

---

## 🚧 ระบบกิจกรรม + สิทธิ์ 3 ระดับ (กำลังทำ — local เท่านั้น ยังไม่ push)

สเปคเต็มอยู่ใน `Prompt_Activity_Feature.md` — ทำทีละเฟส

**✅ เฟส 1: โครงข้อมูล + hash (เสร็จ 2026-06-25)**
- เพิ่มไฟล์ `auth.py` — hash รหัสด้วย sha256 + salt (salt อยู่ใน secrets `HASH_SALT` ไม่ฮาร์ดโค้ด)
- `google_utils.py` เพิ่ม: `ensure_schema()`, แท็บ Activities/Users, คอลัมน์ `activity_id` (คอลัมน์ 9 ใน Sheet1), CRUD (add/load/set_status/find) + `get_storage_quota()` (โควตา Drive จริง)
- มุตเตชันล้าง cache เจาะจง (`load_activities.clear()`/`load_users.clear()`)
- secrets เพิ่ม: `HASH_SALT`, `SUPERUSER_USER`, `SUPERUSER_PASS` (local + .example)
- ✅ ทดสอบผ่าน: schema สร้างครบ / คอลัมน์เดิม 8 ไม่พัง / hash ถูก / CRUD round-trip / quota จริง (0.001/15 GB) / **ระบบเดิม 3 หน้ายังทำงานปกติ**
- ⚠️ Sheet จริงถูกเพิ่มคอลัมน์/แท็บแล้ว (backward-compatible — แอป production เดิมไม่กระทบ ยืนยันด้วย smoke test)

**✅ เฟส 2: Landing + login 4 ทาง (เสร็จ 2026-06-25)**
- `app.py` รื้อเป็น router: ดู `st.session_state["role"]` แล้วพาไปหน้าที่ถูก (คลังทั่วไปเดิมไม่แตะ logic)
- `auth.py` เพิ่ม: `ensure_session()`, `render_landing()` (3 การ์ด), ฟอร์ม login 4 แบบ, `logout()`, `render_topbar_logout()`, `find_open_activity()`
- login: general=APP_PASSWORD / user=รหัสกิจกรรม+ชื่อ (เช็คกิจกรรมเปิด+hash) / admin=Users tab / superuser=secrets
- หน้า user/admin/superuser ตอนนี้เป็น placeholder (รอเฟส 3-5)
- ✅ ทดสอบผ่าน (AppTest): landing 3 การ์ด / login ครบ 4 ทาง / รหัสผิดถูกปฏิเสธ / รหัสกิจกรรมผิดถูกปฏิเสธ / logout กลับ landing / คลังทั่วไปยังเข้าได้ 3 แท็บ

**✅ เฟส 3: หน้า user (เสร็จ 2026-06-25)**
- เพิ่มไฟล์ `page_activity_user.py` — 2 แท็บ: ส่งรูปเข้ากิจกรรม + ดูรูปในกิจกรรมนี้
- ส่งรูป: กรอกแค่รูป (ชื่อผู้ส่งมาจาก login) → reuse pipeline เดิม (บีบอัด/อัป Drive/retry) → บันทึกผ่าน `append_activity_row()` (แผนก/หมวด/ชื่อเรื่อง/แท็กว่าง + activity_id)
- ดูรูป: กรองด้วย activity_id เห็นเฉพาะกิจกรรมตัวเอง + ดาวน์โหลดเดี่ยว/zip (reuse `build_zip`)
- เชื่อมเข้า `app.py` (แทน placeholder user)
- ✅ ทดสอบผ่าน (AppTest + round-trip จริง): ส่งรูปสำเร็จ / แถวมี activity_id + ผู้ส่งถูก + คอลัมน์เดิมว่าง / แท็บดูรูปเห็น "พบ 1 รูป" / **isolation: รูปคลังทั่วไป+กิจกรรมอื่นไม่โผล่** (ลบข้อมูลทดสอบหมดแล้ว)

**✅ เฟส 4: หน้า admin (เสร็จ 2026-06-25)**
- เพิ่มไฟล์ `page_activity_admin.py` — 3 แท็บ: กิจกรรมของฉัน / คลังภาพกิจกรรม / ภาพรวม
- สร้างกิจกรรม: ตั้งรหัสเอง หรือเว้นว่าง=สุ่มให้ (6 ตัว ตัด 0/O/1/I) → เก็บ hash → โชว์รหัสจริงให้ก๊อปแจก + กันรหัสซ้ำกับกิจกรรมที่เปิดอยู่
- เปิด/ปิดกิจกรรม, คลังภาพ+ดาวน์โหลด+ลบรูป (เฉพาะกิจกรรมตัวเอง), ภาพรวม (การ์ด+กราฟ)
- ทุกอย่างกรองด้วย "คนสร้าง" = username → admin เห็นเฉพาะของตัวเอง
- เชื่อมเข้า `app.py`
- ✅ ทดสอบผ่าน (AppTest + round-trip): login / สร้าง(ตั้งเอง+สุ่ม) / กันรหัสซ้ำ / เปิด-ปิด / **isolation กิจกรรม+รูปของ admin อื่นไม่โผล่** (ลบข้อมูลทดสอบหมดแล้ว)

**✅ เฟส 5: หน้า superuser (เสร็จ 2026-06-26)**
- เพิ่มไฟล์ `page_activity_superuser.py` — 5 แท็บ:
  1) 📊 **Dashboard** — พื้นที่ Drive จริง (progress bar ใช้ไป/15GB + เหลือ + รูปทั้งหมด/ขนาดเฉลี่ยประมาณ), ภาพรวมกิจกรรม (การ์ด: เปิดอยู่/รูปเดือนนี้/admin ใช้งาน + ตารางกิจกรรมเรียงตามจำนวนรูป), แจ้งเตือน (เปิดแล้วยังไม่มีรูป / เปิดค้าง >30 วันไม่มีรูปใหม่)
  2) 🎯 **จัดการกิจกรรม** — superuser สร้างกิจกรรมเองได้ (reuse `_create_activity` จากหน้า admin, creator = username superuser) + เปิด/ปิดได้ "ทุก" กิจกรรมของทุก admin
  3) 🖼️ **คลังภาพทุกกิจกรรม** — เห็นรูปของทุก admin/กิจกรรม + กรองตามกิจกรรม + ZIP + ลบรูป (โชว์ชื่อกิจกรรม+คนสร้างใต้รูป)
  4) 👥 **จัดการบัญชี admin** — สร้างบัญชี (กันชื่อซ้ำ, รหัสเก็บ hash) + เปิด/ปิดบัญชี + **ลบบัญชีถาวร** (ขั้นยืนยัน, ใช้ `delete_user()` ใหม่ — ลบเฉพาะแถวใน Users กิจกรรม/รูปที่เคยสร้างไม่หาย)
  5) 📁 **คลังภาพทั่วไป (เดิม)** — reuse 3 หน้าเดิมเป็น sub-tabs (superuser เข้าคลังเก่าได้)
- `google_utils.py`: เพิ่ม `delete_user()` (ลบแถวใน Users, กันลบแถวหัวตาราง)
- `app.py`: เชื่อม superuser (แทน placeholder) + เปิดปุ่มรีเฟรชใน top bar (`show_refresh=True`)
- ✅ ทดสอบผ่าน (AppTest): login superuser → 5 แท็บเรนเดอร์ครบ / มีปุ่มสร้างกิจกรรม + ลบบัญชี admin / top bar มีรีเฟรช+ออกจากระบบ / การ์ด metric ขึ้น / คลังทั่วไปฝังในแท็บ 5 ทำงานปกติ / **0 exception**

**✅ ปรับปรุงหลังเฟส 5 (2026-06-26): หน้า "เข้าร่วมกิจกรรม" มี dropdown เลือกชื่อ**
- `auth.py` `_login_user()`: เปลี่ยนจาก "กรอกรหัสอย่างเดียว" → **เลือกชื่อกิจกรรมที่เปิดอยู่ (dropdown) + กรอกรหัส + ชื่อ** รองรับหลายกิจกรรมเปิดพร้อมกันชัดเจนขึ้น
- dropdown ใช้ `activity_id` เป็นค่าจริง (กันชื่อซ้ำ) โชว์เป็นชื่อด้วย `format_func` ; โชว์เฉพาะกิจกรรมสถานะ "เปิด" ; ถ้าไม่มีเปิดเลย → ข้อความแนะนำแทนฟอร์ม
- รหัสต้องตรงกับ "กิจกรรมที่เลือก" เท่านั้น (verify hash ของ activity_id ที่เลือก) — กันกรอกรหัสกิจกรรมอื่นแล้วหลุดเข้าผิดอัน
- ⚖️ trade-off ที่ผู้ใช้เลือก: ยอมโชว์รายชื่อกิจกรรมที่เปิดให้ทุกคนเห็น แต่ยังต้องมีรหัสถึงเข้าได้
- ✅ ทดสอบผ่าน (AppTest round-trip): dropdown โชว์กิจกรรมที่เปิด / รหัสผิดถูกปฏิเสธ / รหัสถูกเข้าได้+ผูก activity ถูกต้อง (ลบข้อมูลทดสอบแล้ว)

**✅ เฟส 6: ทดสอบรวมทุก role + แก้บั๊ก isolation (เสร็จ 2026-06-26)**
- 🐛 **เจอบั๊ก isolation:** หน้าคลังทั่วไปเดิม (`page_gallery` + `page_dashboard`) ใช้ `load_data()` ดึง "ทุกแถว" รวมรูปกิจกรรม (แผนก/หมวดว่าง) → รูปกิจกรรมหลุดมาโผล่ในคลังทั่วไปเมื่อเลือกแผนก "ทั้งหมด" + ทำตัวเลข Dashboard เพี้ยน
- 🔧 **แก้:** เพิ่ม `load_general_data()` ใน `google_utils.py` (กรองเฉพาะแถว `activity_id` ว่าง) → `page_gallery`/`page_dashboard` เปลี่ยนมาใช้ตัวนี้ (`page_upload` ไม่ต้องแก้ เพราะแค่เขียน บันทึก activity_id ว่างอยู่แล้ว)
- ✅ ชุดทดสอบรวม (AppTest) ผ่านครบ 6 ข้อ:
  1) Landing 3 การ์ด · 2) คลังทั่วไป login รหัสผิด/ถูก + 3 แท็บ · 3) **isolation** (เพิ่มแถวรูปกิจกรรม → คลังทั่วไปไม่เห็น, ลบทิ้งแล้ว) · 4) user dropdown + รหัสผิดปฏิเสธ · 5) admin สร้างบัญชีชั่วคราว→login รหัสผิด/ถูก + แท็บ (ลบบัญชีแล้ว) · 6) superuser รหัสผิด/ถูก + 5 แท็บ
- ℹ️ ข้อมูลจริงในชีตตอนนี้มี ~4 รูป (get_all_records) — เลข "998" ที่เคยเห็นคือ `row_count` (ขนาดกริดที่จองไว้) ไม่ใช่จำนวนรูป

**✅ เฟส 7: README + push ขึ้น production (เสร็จ 2026-06-26) 🎉**
- อัปเดต `README.md` (ระบบกิจกรรม + 3 ระดับสิทธิ์ + ตารางไฟล์ใหม่ + วิธีตั้ง secrets ใหม่) + `secrets.toml.example` มี `HASH_SALT`/`SUPERUSER_USER`/`SUPERUSER_PASS`
- ตรวจไม่มีไฟล์ลับ staged → commit `fa2e3f4` (12 ไฟล์, +1490 บรรทัด) → **push `main` สำเร็จ** → Streamlit Cloud redeploy
- ✅ ใส่ Streamlit Cloud Secrets (HASH_SALT/SUPERUSER_USER/SUPERUSER_PASS) ครบ → **ใช้งานจริง live ครบทุก role แล้ว** ที่ https://image-warehouse-mis.streamlit.app/

**🩹 ปัญหาตอน deploy ครั้งแรก + วิธีแก้ (บันทึกกัน ติดซ้ำ — 2026-06-26)**
1. **ImportError หลัง push** (`cannot import ... from google_utils`): Streamlit Cloud รีรัน `app.py` แต่ไม่รีสตาร์ท process → โมดูล `google_utils` ตัวเก่าค้างใน memory (sys.modules) ไม่มีฟังก์ชันใหม่ พอไฟล์ใหม่ไปเรียกเลยหาไม่เจอ
   → **แก้: Manage app → เมนู ⋮ → Reboot app** (บูต process ใหม่สะอาด) — โค้ดไม่ได้ผิด ยืนยันด้วย clean import ในเครื่องผ่าน
2. **superadmin login บอกรหัสผิด + error แวบเดียว**: วาง 3 บรรทัด secret ใหม่ผิดตำแหน่ง — ไปอยู่ "ใต้" `[google_oauth]` ใน Secrets → TOML นับเป็น `google_oauth.SUPERUSER_USER` → `st.secrets["SUPERUSER_USER"]` ระดับบนสุดหาไม่เจอ (ค่าว่าง) → login ไม่ผ่าน + `HASH_SALT` ก็หาไม่เจอเลยเด้ง error แวบนึง
   → **แก้: กฎ TOML — คีย์เดี่ยว (ไม่มี `[ ]`) ต้องอยู่บนสุดทั้งหมด ก่อนบรรทัด `[google_oauth]` เสมอ** (อะไรที่อยู่ใต้ table header จะถูกนับเป็นของกลุ่มนั้น) ใช้กับทั้งไฟล์ local และ Cloud Secrets

---

## ℹ️ ข้อควรรู้เรื่อง Hosting (Streamlit Community Cloud) — อ่านเมื่อสงสัย

**ฟรีไหม / ต้องต่ออายุไหม?**
- ✅ **ฟรีถาวร** สำหรับ public app — ไม่ต้องใส่บัตร ไม่มีค่ารายเดือน ไม่มีวันหมดอายุ ไม่ต้องต่ออายุ
- เงื่อนไข: repo บน GitHub ต้องยังอยู่และเป็น public (https://github.com/Ben45-alt/image-warehouse)

**แอป "หลับ" (sleep) — ปกติ ไม่ใช่แอปพัง**
- ถ้าไม่มีคนเข้าสักพัก แอปจะหลับเพื่อประหยัดทรัพยากร
- พอมีคนเปิดลิงก์อีกครั้ง จะ "ตื่น" เอง รอ ~30 วิ–1 นาที (บูตใหม่) แล้วใช้ได้ปกติ
- ถ้าทิ้งไว้นานมากๆ (เป็นเดือน) Streamlit อาจส่งอีเมลถามว่ายังใช้อยู่ไหม → กดยืนยัน/reboot ในเมลก็กลับมา

**ฝั่ง Google (สำคัญกว่า)**
- OAuth consent screen = **Production** แล้ว → refresh token ไม่หมดอายุตามเวลา
- ⚠️ Google จะเพิกถอน token ถ้า **ไม่ถูกใช้เลยเกิน 6 เดือน** → ถ้าใช้งานปกติไม่ต้องห่วง
- รูปเก็บใน Google Drive ของ `tfp.data.mis@gmail.com` (ฟรี 15 GB) → ถ้ารูปเยอะค่อยขยายทีหลัง

**อัปเดตแอปออนไลน์ยังไง?**
- แก้โค้ดในเครื่อง → `git add` → `git commit` → `git push` → Streamlit Cloud จะ redeploy ให้เองอัตโนมัติ

**ถ้าจะเปลี่ยน Secrets (เช่น เปลี่ยนรหัสผ่าน / refresh token ใหม่)**
- ต้องแก้ **2 ที่**: ไฟล์ `.streamlit/secrets.toml` ในเครื่อง + Secrets ใน Streamlit Cloud (App → Settings → Secrets)

---

## ➕ ฟีเจอร์เพิ่มหลัง production

**✅ ปุ่มลบกิจกรรม 2 ระดับ (เสร็จ 2026-06-30)** (`google_utils.py`, `page_activity_admin.py`, `page_activity_superuser.py`)
- เพิ่ม `delete_activity(activity_id)` ใน `google_utils.py` — ลบรูปทุกใบของกิจกรรม (ไฟล์ใน Drive + แถวใน Sheet1 ผ่าน `delete_photo` หาแถวจากลิงก์ใหม่ทุกครั้ง กันเลขแถวเลื่อน) แล้วลบแถวกิจกรรมในแท็บ Activities (กันลบแถวหัวตาราง) + ล้าง cache + คืนจำนวนรูปที่ลบ
- **superuser** (หน้า `page_activity_superuser` แท็บ "จัดการกิจกรรม"): ลบได้ "ทุก" กิจกรรม **พร้อมลบรูปทั้งหมด** — มีขั้นยืนยัน + เตือนว่ารูป N ใบจะถูกลบทั้ง Drive และ Sheet กู้คืนไม่ได้
- **admin** (หน้า `page_activity_admin` แท็บ "กิจกรรมของฉัน"): ลบได้ "เฉพาะกิจกรรมของตัวเอง" ที่ **0 รูป** (ไว้แก้ตอนสร้างผิด) — ถ้ามีรูปแล้วปุ่มถูก disable + บอก "แจ้ง superuser ให้ลบแทน"
  - 🔒 กัน race: ตอนกดลบจริง อ่านชีตสดอีกครั้ง ถ้าเจอว่ามีรูปเพิ่งถูกส่งเข้ามาพอดี → ปฏิเสธการลบ
- ✅ ทดสอบผ่าน (AppTest round-trip จริง 17/17): admin ลบกิจกรรมเปล่าได้/กิจกรรมมีรูปปุ่ม disable ลบไม่ได้ + กิจกรรมมีรูปยังอยู่ ; superuser ลบกิจกรรมมีรูปได้ → รูปหายทั้ง Drive+Sheet + แถวกิจกรรมหาย (ลบข้อมูลทดสอบหมดแล้ว ชีตกลับ 4 กิจกรรม/21 แถว)

---

## 🚀 อัปเกรดใหญ่ Round 1+2 (deploy แล้ว 2026-07-16, commit 6e707ab)

> ประชุมกับผู้ใช้ตกลง 5 ฟีเจอร์ แบ่งทำ 3 รอบ — Round 1+2 push ขึ้น production แล้ว, Round 3 (แชร์อัลบั้มเฉพาะคน) ยังไม่ทำ

**✅ Round 1 — metadata + ชื่อไฟล์ + phash** (`image_utils.py`, `google_utils.py`, `page_upload.py`, `page_activity_user.py`)
- รูป .jpg ฝัง metadata: เก็บ EXIF กล้องเดิม (DateTimeOriginal/GPS) **+** ฝังบริบท (ชื่อเรื่อง/กิจกรรม=ImageDescription, ผู้ส่ง=Artist, วันเวลา) — ใช้ `piexif` + ช่อง **XP (UTF-16)** ให้ Windows Explorer อ่านไทยถูก (ช่อง EXIF มาตรฐานเป็น ASCII ไทยจะ mojibake ในบางโปรแกรม แต่ XP ครอบให้แล้ว)
- ชื่อไฟล์: กิจกรรม = `ชื่อกิจกรรม_ลำดับ3หลัก_เวลา.jpg` / คลังทั่วไป = `แผนก_หมวด_เวลา.jpg` (มี `sanitize_name` ตัดอักขระอันตราย เฉพาะรูปใหม่)
- เก็บ `phash` (dHash 64 บิต เขียนเอง ไม่พึ่ง lib นอก) ตอนอัป ไว้ตรวจรูปซ้ำภายหลัง (UI เตือน/สแกนซ้ำ = งานรอบหลัง)
- ✅ ทดสอบในเครื่อง: metadata ฝังไทยอ่านกลับครบ + เก็บ DateTimeOriginal เดิมไม่ทับ / phash ซ้ำ=0 ต่าง=23 / ชื่อไฟล์ถูก

**✅ Round 2 — ถังขยะ + audit log + auto-close + ล็อก user** (`google_utils.py`, `auth.py`, ทุกหน้า gallery)
- **ถังขยะ 30 วัน:** ลบรูป = `trashed=True` ใน Drive (Google ล้างเอง 30 วัน) + ทำเครื่องหมายแถว (สถานะ/วันที่ลบ/ลบโดย) ไม่ลบแถวจริง → รูปหายจากคลังทันทีแต่กู้คืนได้. `trash_photo`/`restore_photo` + `load_active_data`/`load_trash_data`
  - แท็บถังขยะใหม่: **admin** เห็น/กู้เฉพาะกิจกรรมตัวเอง · **superuser** เห็นทั้งระบบ (ทุกกิจกรรม+คลังทั่วไป) กู้คืน+ลบถาวร · **user ไม่มีถังขยะ**
  - หลัง 30 วัน Drive ลบไฟล์เอง แถวในชีตค้าง → **ตั้งใจลบเอง** (ไม่ทำ cron)
- **Audit log:** แท็บ `Log` (เวลา/ผู้ทำ/role/การกระทำ/รายละเอียด/activity_id) — จดตอน อัป/ลบ/กู้คืน/ลบกิจกรรม/ลบถาวร. `log_action` ครอบ try/except ไม่ให้ log พังงานหลัก
- **auto-close 7 วัน:** กิจกรรมครบ 7 วันจากวันสร้าง → ผู้เข้าร่วม login ไม่ได้ (virtual ไม่แก้ชีต, `is_activity_open`/`open_activities`) + หน้า admin/superuser โชว์ "⏰ ปิดอัตโนมัติแล้ว"
- **ล็อก role=user ลบไม่ได้:** ยืนยันด้วย static test ว่า `page_activity_user` ไม่มีฟังก์ชันลบเลย
- ✅ ทดสอบในเครื่อง: auto-close ถูกทุกขอบเขต / compile+cross-import ครบ / user ปลอดฟังก์ชันลบ

**⚙️ schema (รันจริงกับชีต production แล้ว):** เพิ่ม Sheet1 คอลัมน์ J=`phash` K=`สถานะ` L=`วันที่ลบ` M=`ลบโดย` + สร้างแท็บ `Log` — ผ่านสคริปต์ `_migrate_schema.py` (ทำงานเทียบเท่า `ensure_schema` แต่รันนอก streamlit เพราะ local streamlit พังจาก starlette/Py3.14) แล้วลบสคริปต์ทิ้ง. ยืนยัน `get_all_records` อ่าน 21 แถวเดิมได้ปกติ คอลัมน์ใหม่ว่าง (backward-compatible)

**⏳ รอทดสอบบนเว็บจริง (หลัง redeploy เสร็จ):**
1. ถ้าเจอ ImportError → **Reboot app** (module เก่าค้าง sys.modules — บทเรียนเดิม) + piexif เป็น dep ใหม่ อาจต้อง reboot ให้ติดตั้ง
2. ทดสอบ: อัปรูป (เช็คชื่อไฟล์ใหม่ + เปิด properties รูปดู metadata ไทยบน Windows) · ลบรูป → ไปถังขยะ · กู้คืน · ดูแท็บ Log ว่าจดครบ

**✅ Round 3 — แชร์อัลบั้มเฉพาะคน (deploy แล้ว 2026-07-16, commit 460472f)** (`auth.py`, `google_utils.py`, `app.py`, `page_activity_user/viewer/admin/superuser.py`)
- **ถ่าย ≠ ดู:** หน้า user (role=user) เหลือ "ส่งรูป" อย่างเดียว ไม่เห็นอัลบั้มคนอื่นแล้ว (ตัดแท็บดูรูปออก)
- **role ใหม่ `viewer`** — ไฟล์ `page_activity_viewer.py` ดู+ดาวน์โหลด/zip อัลบั้ม ส่ง/ลบไม่ได้
- **แชร์อัลบั้มแบบไดรฟ์:** แต่ละกิจกรรมเจ้าของเลือก 🌐 ทุกคน (สาธารณะ) / 👤 เฉพาะคนที่แชร์ — โหมดเฉพาะคน admin กด "เพิ่มคนดู" → ระบบออก **รหัสดูส่วนตัวรายคน** (เก็บ hash ในแท็บ Shares) ก๊อปแจก + **ถอนสิทธิ์รายคนได้** · **default = เฉพาะคน** (กันส่อง)
- **landing เปลี่ยนเป็น 3 การ์ด:** คลังทั่วไป / กิจกรรม (เมนูย่อย: 📤 ส่งรูป | 🖼️ ดูอัลบั้ม) / เข้าสู่ระบบ
- **viewer เข้า 2 ทาง:** กดชื่ออัลบั้มสาธารณะ (ไม่ต้องรหัส) หรือกรอกรหัสดูส่วนตัว → เข้าอัลบั้มที่ถูกแชร์
- admin/superuser: กล่อง `render_share_panel` (reuse) ต่อกิจกรรม ตั้ง visibility + จัดการคนดู
- schema: Activities +คอลัมน์ `การมองเห็น` + แท็บ `Shares` (รัน `ensure_schema()` ตัวจริงผ่าน stub streamlit กับชีต production แล้ว — กิจกรรมเดิม 4 อัน default=เฉพาะคน)
- ✅ ทดสอบในเครื่อง: compile + stub-import ทุกโมดูลผ่าน + cross-import ครบ (end-to-end บนเว็บรอผู้ใช้เทสต์)

**🔑 หมายเหตุ deploy:** เจอบั๊ก NameError รอบแรก (commit 53477fd แก้แล้ว) — `is_activity_expired(days=AUTO_CLOSE_DAYS)` อ้างค่าคงที่เป็น default arg ที่ประเมินตอนโหลดโมดูล แต่ค่าคงที่อยู่ทีหลังในไฟล์. **บทเรียน: local streamlit พัง (starlette/Py3.14) compile อย่างเดียวไม่จับ NameError → ใช้วิธีสตับ streamlit ปลอมแล้ว import จริงทุกโมดูลก่อน push ทุกครั้ง** (จับ load-time error ได้). สตับ + ใส่ st.secrets จริง = รัน ensure_schema ตัวจริง migrate ชีตได้โดยไม่ต้องเขียน migration ซ้ำ

**⏳ รอทดสอบบนเว็บจริง (ทั้ง 3 รอบ):** ถ้า error → Reboot app. เทสต์: อัป(ชื่อไฟล์+metadata) · ลบ→ถังขยะ+กู้คืน · Log · แชร์อัลบั้มเฉพาะคน→เอารหัสไปเปิดหน้าดูอัลบั้ม · อัลบั้มสาธารณะ

**✅ landing 4 การ์ด (commit b3bcfbb):** คลังทั่วไป / ส่งรูปกิจกรรม / ดูอัลบั้ม / เข้าสู่ระบบ (แยกส่ง-ดู จบใน 1 คลิก)

**✅ ตรวจรูปซ้ำ pHash — ครบข้อ 5 (commit 67fb614)** (`google_utils.py`, `page_upload.py`, `page_activity_user.py`, `page_activity_admin/superuser.py`)
- **เตือนตอนอัป:** คำนวณ phash รูปใหม่ → เทียบในขอบเขตเดียวกัน (กิจกรรม=ในกิจกรรมนั้น / คลังทั่วไป=ในคลัง) Hamming<=5 = คล้าย → พักไว้ถามยืนยัน "จะส่งซ้ำไหม" (ใช้ session flash + pending confirm)
- **สแกนหาซ้ำ:** ปุ่ม "🔍 หารูปซ้ำ" ใน gallery admin/superuser → `group_duplicates` (union-find) จัดกลุ่มรูปคล้ายกัน → เลือกลบใบซ้ำ (ลงถังขยะ)
- `find_similar_photo` + `group_duplicates` ใน google_utils (import `hamming_distance` จาก image_utils) · ไม่ต้อง migrate schema (phash มีอยู่แล้ว)
- ✅ ทดสอบ logic ในเครื่อง: find_similar เจอ/ไม่เจอถูก · group_duplicates จัดกลุ่มถูก · stub-import ทุกโมดูลผ่าน

**🎉 แผนหลัก 5 ฟีเจอร์ครบแล้ว**

**✅ ของเสริมหลังครบแผนหลัก:**
- **QR Code + deep-link** (commit e062e92, 6a23b8a): `qr_utils.py` + `render_code_with_qr(code,key,kind)` โชว์รหัสตัวอักษร+ปุ่มเปิด/ปิด QR. QR เก็บ URL deep-link (view→`?viewcode=` เข้าอัลบั้มเลย / act→`?actcode=` เปิดส่งรูป+เติมรหัส). `auth.handle_deeplink()` อ่าน query param ตอนเปิดแอป auto-login/prefill. deps +qrcode
- **Audit Log viewer** (commit 373b409): `load_log()` + แท็บ '📋 Log' ในหน้า superuser (ตาราง ใคร/ทำอะไร/เมื่อไหร่ ใหม่สุดก่อน + กรอง action/role + ค้นหา + map activity_id→ชื่อ)
- **อัปหลายรูป + เอากล้องในเว็บออก**: หน้าส่งรูป (คลังทั่วไป+กิจกรรม) เปลี่ยน `st.camera_input` → `st.file_uploader(accept_multiple_files=True)` (บนมือถือช่องแนบไฟล์มีปุ่มถ่ายรูป fullscreen ให้อยู่แล้ว จอไม่เล็กเหมือน camera_input). อัปทีละหลายรูป มี progress bar + ตรวจรูปซ้ำแบบชุด (เจอซ้ำ→เลือก ส่งทั้งหมด/เฉพาะไม่ซ้ำ/ยกเลิก). กันชื่อไฟล์ซ้ำในชุด (กิจกรรม=seq เพิ่มต่อ / คลังทั่วไป=เติม _NN)

---

## 📁 เผยแพร่รูปกิจกรรม + คลังภาพแบบโฟลเดอร์ (2026-07-17)

> **โจทย์:** มีคนที่ไม่เกี่ยวกับกิจกรรมอยากดูรูป — ไม่อยากเปิดทั้งอัลบั้มให้ อยากให้ดูแค่ไม่กี่ใบ
> **ทางที่เลือก (ผู้ใช้เสนอเอง):** รูปที่เลือกไปโผล่ใน "คลังทั่วไป" เป็นโฟลเดอร์ของกิจกรรมนั้น → คนในบริษัทที่มีรหัสคลังทั่วไปอยู่แล้วเข้าดูเองได้ **ไม่ต้องออกรหัสใหม่/ส่ง QR ให้ใคร**

**🔑 หลักการสำคัญ: ไม่ได้ "ย้าย" รูป** — รูปมี `activity_id` ติดตัวอยู่แล้ว แค่ติดป้าย `เผยแพร่=ใช่` แล้วหน้าคลังทั่วไปหยิบมาโชว์เพิ่ม → รูปยังอยู่ในอัลบั้มกิจกรรมเหมือนเดิม เห็นได้ 2 ที่พร้อมกัน ปลดป้ายก็หายทันที **ไม่แตะ Drive / ชื่อไฟล์ / metadata เลย** (เลี่ยงปัญหาชื่อไฟล์+EXIF ที่ฝังชื่อกิจกรรมไว้)

**schema:** Sheet1 + คอลัมน์ **N = `เผยแพร่`** (`""` = ไม่เผยแพร่ / `ใช่`) — รัน `ensure_schema()` ตัวจริงผ่าน stub กับชีต production แล้ว (33 แถวเดิมอ่านได้ปกติ คอลัมน์ใหม่ว่าง = backward-compatible)

**ฝั่งกิจกรรม (คนติดป้าย):** `render_publish_toggle()` ใน `page_activity_admin.py` (reuse ทั้ง admin + superuser) — ปุ่ม 🌐 เผยแพร่เข้าคลังทั่วไป / ยกเลิกเผยแพร่ ใต้รูปแต่ละใบในคลังภาพกิจกรรม + จดลง Log · **role=user / viewer ไม่มีปุ่มนี้**

**หน้าคลังทั่วไป (`page_gallery.py`) รื้อเป็นโฟลเดอร์ 2 ชั้น:**
- ชั้นนอก: การ์ดโฟลเดอร์ 📁 **แผนก** (โชว์เฉพาะแผนกที่มีรูป + ปุ่ม "แสดงแผนกที่ยังไม่มีรูป") และ 🎉 **รูปจากกิจกรรม** (1 กิจกรรม = 1 โฟลเดอร์ เฉพาะที่มีรูปเผยแพร่)
- ชั้นใน: กริดรูปเดิม + breadcrumb + ZIP + แบ่งหน้า
  - โฟลเดอร์แผนก → กรองหมวด (ปุ่มแนวนอน ไม่ทำเป็นโฟลเดอร์อีกชั้น กันกดลึก) + คำค้น/ช่วงวันที่ใน expander + **ลบรูปได้** (ของคลังทั่วไปเอง)
  - โฟลเดอร์กิจกรรม → ดู+ดาวน์โหลดอย่างเดียว **ลบไม่ได้** (เจ้าของตัวจริงคือฝั่งกิจกรรม)
- ⚠️ **ตั้งใจไม่ใส่รูปปกในการ์ดโฟลเดอร์** — ทุกรูปต้องโหลด bytes จริงจาก Drive (hotlink thumbnail ใช้ไม่ได้) ถ้าใส่รูปปกต้องโหลดรูปเท่าจำนวนโฟลเดอร์ก่อนหน้าจะขึ้น = ยิ่งรูปเยอะยิ่งช้า (มติผู้ใช้: ไม่เอารูปปก)

**พฤติกรรมพ่วง:** รูปเผยแพร่ที่ถูกลบลงถังขยะ → หายจากคลังทั่วไปทันที (กู้คืนก็กลับมา) · กิจกรรมปิด/auto-close แล้ว รูปที่เผยแพร่ไว้ยังอยู่ · ลบกิจกรรมทั้งอัน → โฟลเดอร์หายเอง · **Dashboard ไม่นับรูปเผยแพร่** (ยังใช้ `load_general_data()` เดิม — ผู้ใช้บอกไม่ได้ใช้ส่งรายงานใคร)

**✅ ทดสอบในเครื่องผ่านครบ:** stub-import ทุกโมดูล · logic กรอง (เผยแพร่/ถังขยะ/ชีตเก่าไม่มีคอลัมน์) · **isolation: รูปกิจกรรมที่เผยแพร่ไม่หลุดเข้า `load_general_data()`** (บั๊กเฟส 6 ไม่กลับมา) · round-trip จริงกับชีต production (เปิด→เช็ค→ปิดคืน ไม่มีข้อมูลค้าง)

---

## 👀 ปัญหาที่เจอ — รอสังเกตเพิ่ม (ยังไม่แก้ 2026-06-30)

**ถ่ายรูปบนมือถือ (Safari) บางครั้งหมุนนาน บางครั้งไวมาก**
- อาการจริง (ผู้ใช้ยืนยัน): ใช้ต่อเนื่องในวันเดียวกัน = ปกติไว; แต่พอ **ปิดแอป Safari แล้วเปิดลิงก์ใหม่เพื่อถ่ายอีกรอบ** → ช้าบ้างไวบ้าง
- 🔍 วินิจฉัย: **ไม่ใช่บั๊กโค้ด** — เป็นพฤติกรรม Streamlit Community Cloud (ฟรี) ที่ "หลับ" แอปเมื่อไม่มีทราฟฟิกสักพัก พอเปิดลิงก์ใหม่ตอนแอปหลับต้องบูตใหม่ ~30วิ–1นาที (cold start); ถ้าแอปยังตื่นอยู่ก็ไว → อธิบาย "บางครั้งช้า บางครั้งไว" ได้พอดี
- ปัจจัยเสริม (เล็กน้อย): `st.camera_input` อัปรูป "ดิบ" เป็น PNG ขนาดใหญ่ขึ้น server ก่อนค่อยบีบอัดตอน save (บีบอัดเกิดทีหลัง เลยรู้สึกว่า "save ไว"); และ `st.tabs` รันทุกแท็บทุก rerun
- ✅ มติ (ผู้ใช้): **ปล่อยไว้ก่อน ยังไม่แก้** — ต้องให้คนใช้หลายๆ คนจริงก่อน ค่อยดูว่าคุ้มจะแก้ไหม
- 🛠️ ถ้าจะแก้ทีหลัง (เรียงตามผล):
  1. **keep-alive** ใช้บริการ uptime ฟรีภายนอก (UptimeRobot / cron-job.org) ping ลิงก์ทุก ~5 นาที (เฉพาะเวลาทำงานก็ได้) → แอปไม่หลับ เปิดมาไวตลอด *(แก้ตรงอาการที่สุด)*
  2. มือถือใช้ "แนบไฟล์ → ถ่ายจากกล้องเครื่อง" แทนกล้องในเว็บ (ได้ JPEG บีบแล้ว เล็กกว่า PNG ของ camera_input มาก)
  3. เปลี่ยน `st.tabs` → เมนูเลือกหน้า (รันเฉพาะหน้าที่เปิด) ลด overhead คงที่ทุก rerun
- ℹ️ `st.camera_input` ปรับความละเอียด/บีบอัดฝั่งเบราว์เซอร์ผ่าน Python ไม่ได้ — ต้องเลี่ยงด้วยข้อ 2 หรือทำ custom component (งานใหญ่)

---

## ⚠️ บทเรียน
อย่ารัน OAuth refresh token ถี่ๆ ในเวลาสั้น — Google จะ disable client ทดสอบกับ Google API เท่าที่จำเป็นพอ
