# 📋 สถานะโปรเจกต์ "คลังภาพกลางของบริษัท"

> ไฟล์นี้ไว้จดว่าทำถึงไหนแล้ว + ติดปัญหาตรงไหน (อัปเดตล่าสุด: 2026-07-20 — รื้อ login ให้ง่ายขึ้น รอบ 1)

---

## 🚪 รื้อ login ให้ง่ายขึ้น — รอบ 1 (2026-07-20)

> **โจทย์จากหัวหน้า:** "มันดูใช้ยากไป login หลายขั้นไป — คลังทั่วไป / ส่งรูปกิจกรรม / ดูอัลบั้ม ควรรวมอยู่ในหน้าเดียวกัน"
> **สรุปที่คุยกัน:** ปัญหาจริงไม่ใช่แค่จำนวนขั้น แต่เป็น **เขาวงกต 4 การ์ด** (กดผิดต้องกด "← กลับหน้าแรก" เริ่มใหม่)

**🔑 ทางที่เลือก: "ช่องเดียวจบ"** — ผู้ใช้ไม่ต้องรู้ว่าตัวเองเป็นประเภทไหน ใส่รหัสที่มีในมือ ระบบเดาให้เองแล้วพาไปหน้าที่ตรงกับสิทธิ์
- ทำได้เพราะรหัส 3 แบบเป็นคนละค่ากันอยู่แล้ว → `identify_code()` ไล่เช็ค APP_PASSWORD → รหัสกิจกรรม → รหัสดูอัลบั้ม
- **ไม่ต้องสร้างบัญชีให้พนักงานทุกคน** (ทางเลือกนั้นถูกตัดทิ้ง เพราะสวนทางกับ "ไม่ยุ่งยาก" — คนลืมรหัสต้องมีคนรีเซ็ตให้)

**🆕 `session_store.py` — "จำฉันไว้ 1 วัน" (cookie)**
- Streamlit เก็บ login ใน `session_state` ซึ่ง **หายทุกครั้งที่ refresh** → นี่คือต้นเหตุจริงที่ต้องกรอกรหัสบ่อย
- token เซ็นด้วย HMAC-SHA256 (กุญแจ = `HASH_SALT` เดิม) — **ห้ามเชื่อ cookie ตรงๆ** เพราะผู้ใช้แก้เองได้
- **อ่าน** cookie ด้วย `st.context.cookies` (sync จาก request header — เลี่ยงปัญหา component อ่านแบบ async แล้วหน้า login กะพริบ) · **เขียน/ลบ** ด้วย `extra-streamlit-components`
- ถ้า lib พัง/หาย → **แอปยังทำงานปกติ แค่ไม่จำ login** (กลืน error หมด)
- แก้จำนวนวันที่ `REMEMBER_DAYS` ที่เดียว (ข้อความบนจออ้างค่านี้ผ่าน `auth.REMEMBER_LABEL`)

**🔒 `restore_session()` — ไม่เชื่อ cookie อย่างเดียว เช็คสิทธิ์ซ้ำกับชีตทุกครั้ง**
- role=user → กิจกรรมต้องยังเปิด (`is_activity_open`) · role=viewer → ดูย้อนหลังได้แม้ปิดแล้ว
- role=admin → บัญชีต้องยัง "ใช้งาน" + role ยังเป็น admin · role=superuser → username ต้องตรง secrets
- ⚠️ ถ้าไม่เช็คซ้ำ = สิทธิ์ค้าง 1 วันหลังถูกปิดบัญชี/ปิดกิจกรรม

**🖼️ แท็บ "รูปของฉัน"** (`page_activity_user.py`) — คนส่งรูปเห็นรูปที่ตัวเองส่ง
- กรอง 2 ชั้น: `activity_id` **และ** ชื่อผู้ส่ง → **ไม่เห็นรูปของคนอื่น** (คงหลัก "ถ่าย ≠ ดู" จาก Round 3) · ดาวน์โหลดได้ ลบไม่ได้
- ไม่ต้อง migrate schema — คอลัมน์ "ผู้ส่ง" มีอยู่แล้ว

**QR deep-link เดิมยังใช้ได้** — `?actcode=` รหัสถูกแล้วข้ามช่องรหัสไปกรอกชื่อเลย (เร็วกว่าเดิม)

**✅ ทดสอบในเครื่องผ่าน 32/32** (stub streamlit + import จริงทุกโมดูล รวม `app.py` ที่รัน `main()` ตอน import)
- token: เซ็น/อ่านกลับ/หมดอายุ/**แก้ payload เป็น superuser แล้วโดนตีตก**/ขยะไม่ throw
- `identify_code`: รหัสคลังทั่วไปถูก · รหัสมั่วภาษาไทย**ไม่ throw TypeError** (บทเรียน `compare_digest` เดิม)
- `restore_session`: cookie ปลอม/superuser ชื่อไม่ตรง/admin ไม่มีบัญชี → **ไม่ได้สิทธิ์ทุกกรณี**

**⏳ รอทดสอบบนเว็บจริง:**
1. ⚠️ **Reboot app ก่อนเลย** — มี dep ใหม่ `extra-streamlit-components` (บทเรียนเดิมจาก `piexif`)
2. ⚠️ **จุดเสี่ยงที่สุด = cookie จริงบนเบราว์เซอร์** (ในเครื่องเทสต์ด้วยของปลอมเท่านั้น) — เทสต์: login → ติ๊กจำฉันไว้ → **refresh หน้า** → ต้องไม่หลุด · กดออกจากระบบ → ต้องหลุดจริง
3. รหัสกิจกรรม/รหัสดูอัลบั้มของจริง (ในเครื่องเทสต์ไม่ได้ เพราะชีตเก็บ hash)
4. แท็บ "รูปของฉัน" — ส่งรูปแล้วเห็นของตัวเอง · **ไม่เห็นของคนอื่น**

---

## 🙋 รอบ 2 — admin สมัครเอง + หัวหน้ากดอนุมัติ (2026-07-20)

> รอบ 1 ผู้ใช้เทสต์บนเว็บผ่านแล้ว ("เทสละ น่าจะไม่ติด") → ทำรอบ 2 ต่อ

**ทางเดินใหม่:** คนอยากเป็น admin → หน้าแรก > `🔐 สำหรับ admin` > แท็บ **"สมัครบัญชี admin"** → กรอก username/ชื่อ/อีเมลบริษัท/รหัส 2 ครั้ง → บันทึกสถานะ `รออนุมัติ` **ยัง login ไม่ได้** → superuser เห็นกล่อง "⏳ มี N คนรอคุณอนุมัติ" บนสุดของแท็บจัดการบัญชี → กด ✅ อนุมัติ (→ `ใช้งาน`) หรือ 🚫 ปฏิเสธ (ลบแถว มีขั้นยืนยัน) → จดลง Log ทั้งคู่

**โดเมนอีเมลบริษัท (ผู้ใช้ยืนยันแล้ว 2026-07-20) — ตั้งที่ `config.py` → `COMPANY_EMAIL_DOMAINS`:** `@tfpoem.com` · `@tfpthailand.com` · `@tfpcar.com` (ตรงอันใดอันหนึ่งก็ผ่าน) · เพิ่ม/ลบโดเมนแก้ที่ list นี้ที่เดียว · ตั้งเป็น `[]` = ไม่บังคับโดเมน

**schema:** แท็บ Users + คอลัมน์ **6 = `อีเมล`** (ต่อท้ายเสมอ — คอลัมน์ 1-5 ห้ามขยับ เพราะ `add_user`/`set_user_status` อ้างตำแหน่ง) · ✅ รัน `ensure_schema()` ตัวจริงกับชีต production แล้ว (บัญชี `pim` เดิมอยู่ครบ อีเมลว่าง = backward-compatible)

**ของใหม่ในโค้ด:** `google_utils`: ค่าคงที่ `USER_ACTIVE/USER_DISABLED/USER_PENDING` + `pending_users()` + `email_taken()` (กันสมัครซ้ำอีเมลเดิม ไม่สนตัวพิมพ์) + `add_user(..., email=)` · `auth._signup_admin()` · `page_activity_superuser._render_pending_approvals()`

**🔒 จุดที่ระวังไว้:**
- `_login_staff` **เช็ครหัสผ่านให้ถูกก่อน** ค่อยบอกว่า "รออนุมัติ/ถูกระงับ" — ถ้าบอกก่อนเช็ค = เผยให้คนนอกรู้ว่ามี username นี้อยู่จริง
- `restore_session` ใช้ `USER_ACTIVE` → cookie ของบัญชีที่ยังรออนุมัติ/ถูกระงับ คืนสิทธิ์ไม่ได้
- ⚠️ **กันเข้าใจผิด:** อีเมลที่พิมพ์เอง **ไม่ได้พิสูจน์ว่าเป็นเจ้าของอีเมลจริง** (ไม่ได้ส่งเมลยืนยัน) — ความปลอดภัยมาจาก "หัวหน้ารู้จักคนที่อนุมัติ" เขียนเตือนไว้บนหน้าจอ superuser แล้ว. ถ้าจะพิสูจน์จริงต้องตั้ง SMTP (มติ: ไม่ทำ)

**✅ ทดสอบในเครื่องผ่าน 37/37** (stub streamlit + import จริงทุกโมดูล): บัญชีรออนุมัติ+รหัสถูก→เข้าไม่ได้ · รหัสผิด→บอกแค่ "รหัสผิด" ไม่เผยว่ามีบัญชี · บัญชีปิด→เข้าไม่ได้ · รหัสไทยไม่ throw · cookie รออนุมัติไม่คืนสิทธิ์ · ครบ 3 โดเมนผ่าน + พิมพ์ใหญ่/มีช่องว่างก็ผ่าน · โดเมนหลอก (`@nottfpoem.com`, `@tfpcar.com.evil.com`, gmail) ไม่ผ่าน · `add_user` เขียน 6 ช่องถูกตำแหน่ง

**⏳ รอทดสอบบนเว็บจริง:**
1. สมัครใหม่ 1 บัญชี → ต้องขึ้น "รอหัวหน้าอนุมัติ" · ลอง login ทันที → ต้องเข้าไม่ได้ (ขึ้น ⏳ รออนุมัติ)
2. superuser เห็นกล่องรออนุมัติ → กดอนุมัติ → บัญชีนั้น login ได้จริง + แท็บ Log จด "อนุมัติบัญชี admin"
3. ลองปฏิเสธอีก 1 บัญชี → หายจากรายการ + สมัครใหม่ด้วยอีเมลเดิมได้
4. อีเมล gmail → ต้องถูกปฏิเสธ · อีเมลซ้ำ → ต้องถูกปฏิเสธ

---

## ✅ จุดพักงาน (2026-07-17) — เทสต์ผ่านหมดแล้ว ไม่มีงานค้าง

**✅ ของรอบ 2026-07-16 ทดสอบบนเว็บจริงผ่านหมดแล้ว** (ผู้ใช้ยืนยัน "เทสหมดละ ได้หมด") — แผนหลัก 5 ฟีเจอร์ + ของเสริมทั้งหมดใช้งานได้จริง

**✅ เสร็จวันนี้ (2026-07-17):** เผยแพร่รูปกิจกรรมเข้าคลังทั่วไป + คลังภาพแบบโฟลเดอร์ — push แล้ว commit `ff5f041` (รายละเอียดหัวข้อ "📁 เผยแพร่รูป + คลังภาพแบบโฟลเดอร์" ด้านล่าง)

**✅ ทดสอบบนเว็บจริงแล้ว (2026-07-17 ~15:00-15:10) — ผ่าน 8/8 ไม่เจอบั๊ก**
> ทดสอบผ่านเบราว์เซอร์จริงบน image-warehouse-mis.streamlit.app ด้วยบัญชี superadmin
> ℹ️ **ไม่ต้อง reboot** — รอบนี้ไม่เจอ ImportError (เข้าใช้ได้เลยหลัง redeploy) ต่างจากที่คาดไว้

- [x] เปิดคลังทั่วไป → เห็นการ์ดโฟลเดอร์แผนก (ฝ่ายHR/MIS/PDC/PLN อย่างละ 1 รูป) + ปุ่ม "แสดงแผนกที่ยังไม่มีรูป (20)" ✅
- [x] กดเข้าโฟลเดอร์แผนก → เห็นรูป ("แอร์ 36" · ฝ่ายHR/อุปกรณ์) · กรองหมวดปุ่มแนวนอน 6 อัน · expander "ตัวกรองเพิ่มเติม (คำค้น/ช่วงวันที่)" · ปุ่ม "กลับไปหน้าโฟลเดอร์" ทำงาน · ZIP + ลบรูปได้ปกติ ✅
- [x] หน้าคลังภาพเปิดเร็วขึ้นจริง — หน้าโฟลเดอร์ขึ้นทันที ไม่มีดีเลย์ (ตัดรูปปกออกได้ผล) ✅
- [x] กด 🌐 เผยแพร่รูป 1 ใบ (`hr_009_165451.jpg`) → โฟลเดอร์กิจกรรม `hr` ในคลังทั่วไป **5 → 6 รูป** + รูปโผล่ครบ · ปุ่มเปลี่ยนเป็น "ยกเลิกเผยแพร่" + ขึ้นป้าย "🌐 เผยแพร่อยู่ในคลังทั่วไป" ✅
- [x] โฟลเดอร์กิจกรรม **ไม่มีปุ่มลบ** — มีแค่ "⬇️ ดาวน์โหลดรูปนี้" (ต่างจากโฟลเดอร์แผนกที่มีปุ่มลบ) + รูปยังอยู่ในอัลบั้มกิจกรรมด้วย ✅
- [x] กดยกเลิกเผยแพร่ → โฟลเดอร์ `hr` **6 → 5 รูป** กลับสภาพเดิม ✅
- [x] แท็บ Log จดครบ: `เผยแพร่รูป` (15:03:52) และ `ยกเลิกเผยแพร่รูป` (15:05:17) — superadmin / superuser / hr_009_165451.jpg / hr (รายการ 20 → 22) ✅
- [x] superuser มีปุ่มเผยแพร่เหมือน admin (แท็บ "คลังภาพทุกกิจกรรม") ✅

> ✅ **ข้อมูลกลับสภาพเดิมหมดแล้ว** — เผยแพร่ 1 ใบแล้วยกเลิกคืน ไม่มีข้อมูลค้าง เหลือแค่ Log 2 บรรทัดที่เพิ่มมา (ถูกต้องตามดีไซน์)

**🧹 เจอระหว่างเทสต์ (ไม่ใช่บั๊ก แต่ควรเคลียร์):** ในคลังภาพกิจกรรม `hr` มี **รูปทดสอบปนอยู่เยอะ** (ภาพเกม Battle Pass, ตัวการ์ตูน AI, หุ่นยนต์) — ถ้าจะเปิดเว็บให้หัวหน้า/คนอื่นดู ควรลบทิ้งก่อน (ลบลงถังขยะได้ กู้คืนได้ 30 วัน)

**🎨 ถ้าอยากปรับหลังเทสต์:** ขนาด/จำนวนการ์ดต่อแถว (`COLS_PER_ROW` = 4) · ข้อความบนการ์ด · จำนวนรูปต่อหน้า (`PAGE_SIZE` = 12) — แก้ที่หัวไฟล์ `page_gallery.py`

**📋 ของเสริมที่ยังไม่ทำ (ถ้าอยากทำต่อ):**
- หน้า error สวยๆ ตอน token พัง (invalid_grant) · แก้หัวข้อ/แผนกรูปหลังอัป · ค้นหาในอัลบั้ม · ล้างถังขยะอัตโนมัติ(ลบแถวค้าง>30วัน) · แจ้งเตือน Drive ใกล้เต็ม

**🔑 วิธีทดสอบโค้ดก่อน push (สำคัญ — local streamlit รันไม่ได้):** สตับ streamlit ปลอมแล้ว import จริงทุกโมดูล (compile เฉยๆ ไม่จับ NameError ตอนโหลด) · ใส่ st.secrets จริง = รัน ensure_schema migrate ชีตได้เลย

---

## 🆘 กู้คืนด่วน: เว็บเชื่อม Google ไม่ได้ `invalid_grant: Token has been expired or revoked` (runbook)

> เกิดครั้งแรก 2026-07-03 (ตอนกำลัง present) — refresh token ถูก Google เพิกถอน เว็บดูรูปไม่ได้ทั้งหมด
> กู้คืนเสร็จใน ~5 นาที ทำตามนี้ครั้งหน้าจะเร็วกว่าเดิม:

**อาการ:** เปิดเว็บแล้วขึ้น error เชื่อม Google ไม่ได้ `('invalid_grant: Token has been expired or revoked.')` — ทุกหน้าที่ต้องอ่านรูป/ชีตพัง

**วิธีกู้ (5 ขั้น):**
1. เปิด terminal ที่โฟลเดอร์โปรเจกต์ → รัน `python get_refresh_token.py` (สคริปต์บังคับเลือกบัญชีทุกครั้งแล้ว)
2. เบราว์เซอร์เด้ง → **เลือกบัญชี `tfp.data.mis@gmail.com` เท่านั้น** (เจ้าของ Sheet/Drive — เลือกผิดบัญชีจะเข้าคลังไม่ได้) → ถ้าเจอ "Google hasn't verified this app" กด Advanced → Go to... (unsafe) → Allow ทุกหน้า
3. ก๊อป `refresh_token = "..."` ที่ terminal พิมพ์ออกมา
4. วาง 2 ที่:
   - ไฟล์ `.streamlit/secrets.toml` (บรรทัด refresh_token ใต้ `[google_oauth]`)
   - **Streamlit Cloud → แอป image-warehouse-mis → ⋮ Settings → Secrets** (บรรทัด refresh_token ใต้ `[google_oauth]`) → Save (แอป reboot เอง ~30 วิ)  ← **อันนี้คือตัวที่กู้เว็บจริง** local ไม่เกี่ยวกับเว็บ
5. (ก่อน save cloud) ทดสอบ token ใหม่ในเครื่องก่อนได้: รันสคริปต์เล็กๆ เปิด Sheet + เช็ค `drive.about().get(fields='user')` ต้องได้อีเมล `tfp.data.mis@gmail.com` = เลือกบัญชีถูก

**หมายเหตุต้นเหตุ (2026-07-03):** consent screen เป็น "In production" อยู่แล้ว → **ไม่ใช่**เรื่องหมดอายุ 7 วัน. สาเหตุที่น่าจะเป็น = ออก refresh token ใหม่บ่อยจน Google ตัด token เก่าสุดทิ้ง (มีลิมิตต่อ client/บัญชี) หรือมีเหตุการณ์ความปลอดภัยของบัญชี. **บทเรียน: อย่ารัน `get_refresh_token.py` พร่ำเพรื่อ** — รันเฉพาะตอนจำเป็นจริง เพราะทุกครั้งที่ออกใหม่เสี่ยงเบียด token เก่าให้ถูกถอน

**✅ ทำแล้ว (กันเชิงรุก, 2026-07-03): keep-alive ด้วย GitHub Actions** — ไฟล์ `.github/workflows/keep-alive.yml` ping `https://image-warehouse-mis.streamlit.app/healthz` ทุก ~10 นาที ช่วง 00:00-11:59 UTC (07:00-18:59 เวลาไทย) ทุกวัน → กันแอปหลับ (ลด cold start มือถือ + token ถูกใช้สม่ำเสมอ). ฟรีเพราะ repo public.
- ⚠️ ต้องใช้ `/healthz` (คืน 200 ตรง) — `/` และ `/_stcore/health` โดน 303 redirect ไป `share.streamlit.io/-/auth` (curl ตามไม่ได้เพราะไม่มีเบราว์เซอร์/JS)
- ⚠️ GitHub ปิด scheduled workflow อัตโนมัติถ้า repo เงียบ (ไม่มี commit) เกิน 60 วัน → เข้าแท็บ Actions กด "Enable workflow" อีกครั้งถ้าเงียบนาน
- ยืนยันแล้ว: กดรันเอง (workflow_dispatch) → `GET /healthz -> HTTP 200` สำเร็จ

**ยังพิจารณาทำต่อ:** ปรับแอปให้โชว์ข้อความสวยๆ บอกวิธีแก้แทนจอ error ดิบ ถ้า token พังอีก

---

## ✅ ทำเสร็จแล้ว

| เฟส | งาน | สถานะ |
|-----|-----|-------|
| C | OAuth + refresh token + verify เข้าถึง Sheet/Drive | ✅ |
| 1 | ติดตั้งไลบรารี (แก้ starlette ให้เข้ากับ Python 3.14) | ✅ |
| 2 | ระบบ login + ตัวเชื่อม Google + แก้หัวตาราง Sheet เป็น 8 คอลัมน์ | ✅ |
| 3 | หน้าส่งรูป (ย่อรูป → อัป Drive → บันทึก Sheet) — *ทดสอบ pipeline จริงผ่าน* | ✅ |
| 4 | หน้าคลังภาพ (filter + grid + ดาวน์โหลดเดี่ยว/zip + แบ่งหน้า) | ✅ |
| 5 | Dashboard + start.bat + README.md — *ทดสอบ AppTest ทั้ง 3 หน้าผ่าน* | ✅ |

โค้ดครบทุกไฟล์ compile ผ่านหมด

---

## ✅ Blocker ปลดล็อกแล้ว (2026-06-25)

**OAuth client เดิมกลับมาใช้ได้แล้ว** — Google เปิด client คืนหลังพ้นช่วง anti-abuse
- ทดสอบยิง refresh token **1 ครั้ง** → `REFRESH_OK` (ได้ access token)
- อ่าน Sheet `Image_Warehouse_DB` ได้ (998 rows)
- รัน AppTest: login ผ่าน, 3 แท็บเรนเดอร์, 0 error, sidebar ขึ้น "เชื่อม Google Sheet สำเร็จ"
- **ไม่ต้องสร้าง client ใหม่ / ไม่ต้องขอ refresh token ใหม่** — ค่าใน `.streamlit/secrets.toml` ใช้ได้เลย

---

## ▶️ เหลือทำต่อ

1. ✅ **เปลี่ยนรหัสผ่านแล้ว** (2026-06-25): `APP_PASSWORD` เปลี่ยนจาก `1234` เป็นรหัสใหม่ใน `.streamlit/secrets.toml` — ยืนยันด้วย AppTest ว่า login รหัสใหม่ผ่าน / รหัสเก่าใช้ไม่ได้แล้ว
2. ✅ **Deploy เสร็จสมบูรณ์ (2026-06-25)** 🎉
   - Live: **https://image-warehouse-mis.streamlit.app/**
   - โค้ด: https://github.com/Ben45-alt/image-warehouse (public, ไม่มีไฟล์ลับ)
   - Secrets วางใน Streamlit Cloud แล้ว
   - OAuth consent screen = **Production** → refresh token ไม่หมดอายุ 7 วัน

> หมายเหตุ: ตอนนี้พร้อมใช้งานเต็มรูปแบบบนเครื่องแล้ว — ดับเบิลคลิก `start.bat` → login ด้วยรหัสใหม่ → ใช้ได้ครบ 3 หน้า

---

## 🐛 บั๊กที่แก้แล้ว

**(2026-07-17) ใส่รหัสผิดแล้วขึ้น `TypeError` แทนที่จะบอกว่ารหัสผิด** (`auth.py`)
- อาการ: หน้า "🔐 เข้าสู่ระบบ" (admin/superuser) พิมพ์รหัสผิด → เด้ง `TypeError` ดิบใส่หน้าผู้ใช้ ไม่ขึ้นข้อความ "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"
- สาเหตุ: `_login_staff()` เทียบรหัส superuser ด้วย `hmac.compare_digest(p, su_pass)` **ตรงๆ กับสตริง** — ฟังก์ชันนี้รองรับเฉพาะ ASCII ถ้ามีอักขระ non-ASCII แม้ตัวเดียวจะ **โยน `TypeError: comparing strings with non-ASCII characters is not supported` แทนที่จะคืน False** → เจอง่ายมากเพราะ **ลืมสลับแป้นพิมพ์ ยังค้างภาษาไทยอยู่** แล้วพิมพ์รหัส (reproduce ยืนยันแล้ว)
- แก้: เพิ่ม `secrets_equal(a, b)` ใน `auth.py` — sha256 ทั้งสองฝั่งเป็น bytes ก่อนค่อย `compare_digest` → รับอักขระอะไรก็ได้ (ไทย/อีโมจิ) และยังกัน timing attack เหมือนเดิม ; `_login_staff` เปลี่ยนมาใช้ตัวนี้
- ℹ️ จุดอื่นปลอดภัยอยู่แล้ว: `verify_secret()` (admin จากชีต + รหัสกิจกรรม + รหัสดูอัลบั้ม) hash เป็น hex ASCII ก่อนเทียบเสมอ · `_login_general()` ใช้ `==` ธรรมดา ไม่ throw
- ✅ ทดสอบผ่าน: รหัสผิด 6 แบบ (ไทยล้วน/แป้นค้างไทย/ไทยปนอังกฤษ/อีโมจิ/ascii/ว่าง) → คืน False ไม่ throw · รหัสถูกยัง login ได้ · รหัสจริงที่เป็นภาษาไทยก็ใช้ได้
- ⚠️ **บทเรียน: ห้ามใช้ `hmac.compare_digest()` กับ str ที่รับจากผู้ใช้โดยตรง** — hash เป็น bytes ก่อนเสมอ

**(2026-06-25) รูปค้างหลังบันทึก → เสี่ยงบันทึกซ้ำ** (`page_upload.py`)
- อาการ: ช่องอัปรูปอยู่ "นอก" `st.form` แต่ `clear_on_submit` ล้างได้แค่ช่องในฟอร์ม → หลังกดบันทึก รูปเดิมยังค้าง พอเปลี่ยนแผนก/หมวด/หัวข้อแล้วกดบันทึกอีกครั้ง รูปเดิมถูกอัปซ้ำเป็นรายการใหม่ (reproduce ยืนยันแล้ว)
- แก้: ย้ายช่องอัปรูป (`file_uploader`/`camera_input`) เข้าไป "ใน" `st.form` (radio เลือกวิธียังอยู่นอกฟอร์มเพื่อสลับได้ทันที) → frontend ล้างรูปให้พร้อมช่องอื่นตอน submit
- เช็คเพิ่ม: ลำดับหัวตาราง Sheet ตรงกับลำดับที่โค้ดเขียน + ค่าที่เลือก (แผนก/หมวด) บันทึกถูกช่อง — ไม่มีบั๊กแมปคอลัมน์

**(2026-06-25) config.py ตกคอมม่า → แผนกรวมกัน** (`config.py`)
- string หลายตัววางติดกันไม่มีคอมม่า → Python ต่อเป็นก้อนเดียว (เช่น HR+HRSL, และก้อนยาว MTN→CARRICHS)
- แก้: เติมคอมม่าครบทุกบรรทัด (24 ฝ่ายแยกกัน) + คอมเมนต์เตือน
- เคลียร์ข้อมูลทดสอบเก่า 3 แถวใน Sheet + ลบรูปใน Drive (เคยใช้ชื่อแผนกเก่าที่ไม่มีใน config ใหม่ ทำให้ Dashboard/ฟิลเตอร์เพี้ยน)

**(2026-06-25) กด Enter ที่หน้า login ไม่ submit** (`app.py`)
- เดิมใช้ `text_input` + `button` แยกกัน → Enter ไม่กดปุ่ม
- แก้: ครอบด้วย `st.form` (ช่องเดียว) → Enter = submit ทันที

**(2026-06-25) พรีวิวรูปหน้าคลังภาพไม่ขึ้น** (`page_gallery.py`, `google_utils.py`)
- เดิมใช้ URL `drive.google.com/thumbnail?id=...` → รูปเพิ่งอัป Google ยังไม่สร้าง thumbnail / โดนบล็อก hotlink
- แก้: เพิ่ม `get_image_bytes()` (cache 10 นาที) โหลด bytes รูปจริงมาแสดงด้วย `st.image` — ยืนยัน round-trip ผ่าน

**(2026-06-25) บันทึกรูปล้มเหลว WinError 10053 (เน็ต/AV ตัดการเชื่อมต่อ)** (`google_utils.py`)
- อาการ: อัปรูปแล้วขึ้น `[WinError 10053] An established connection was aborted...` ระหว่างต่อ Google
- แก้: เพิ่ม `_retry()` ลองใหม่อัตโนมัติเมื่อเจอ ConnectionError/TimeoutError (1.5s,3s,4.5s) + ใส่ `num_retries=5` ใน Drive API
- ครอบ `upload_to_drive` (create + permission, seek buffer ก่อน retry กันอัปไฟล์เปล่า) และ `append_row`
- ยืนยัน: retry กู้คืนได้จริง / error ที่ไม่ใช่เน็ตไม่ถูก retry / อัปโหลดจริงยังทำงานปกติ

---

## ➕ ฟีเจอร์ที่เพิ่มหลัง deploy

**(2026-06-25) ปุ่มลบรูป ในหน้าคลังภาพ** (`page_gallery.py`, `google_utils.py`)
- แต่ละรูปมีปุ่ม **🗑️ ลบรูปนี้** → กดแล้วถามยืนยัน (✅ ลบเลย / ❌ ยกเลิก) กันกดพลาด
- ลบ = ลบไฟล์ใน Drive + ลบแถวใน Sheet (หาแถวจากคอลัมน์ "ลิงก์รูป" ที่ไม่ซ้ำกัน) ผ่าน `delete_photo()`
- ยืนยันแล้ว: round-trip ลบจริงครบ (Drive หาย + แถวหาย + ไม่มีตกค้าง) + กริดเรนเดอร์ปกติ

---

## 🚧 ระบบกิจกรรม + สิทธิ์ 3 ระดับ (กำลังทำ — local เท่านั้น ยังไม่ push)

สเปคเต็มอยู่ใน `Prompt_Activity_Feature.md` — ทำทีละเฟส

**✅ เฟส 1: โครงข้อมูล + hash (เสร็จ 2026-06-25)**
- เพิ่มไฟล์ `auth.py` — hash รหัสด้วย sha256 + salt (salt อยู่ใน secrets `HASH_SALT` ไม่ฮาร์ดโค้ด)
- `google_utils.py` เพิ่ม: `ensure_schema()`, แท็บ Activities/Users, คอลัมน์ `activity_id` (คอลัมน์ 9 ใน Sheet1), CRUD (add/load/set_status/find) + `get_storage_quota()` (โควตา Drive จริง)
- มุตเตชันล้าง cache เจาะจง (`load_activities.clear()`/`load_users.clear()`)
- secrets เพิ่ม: `HASH_SALT`, `SUPERUSER_USER`, `SUPERUSER_PASS` (local + .example)
- ✅ ทดสอบผ่าน: schema สร้างครบ / คอลัมน์เดิม 8 ไม่พัง / hash ถูก / CRUD round-trip / quota จริง (0.001/15 GB) / **ระบบเดิม 3 หน้ายังทำงานปกติ**
- ⚠️ Sheet จริงถูกเพิ่มคอลัมน์/แท็บแล้ว (backward-compatible — แอป production เดิมไม่กระทบ ยืนยันด้วย smoke test)

**✅ เฟส 2: Landing + login 4 ทาง (เสร็จ 2026-06-25)**
- `app.py` รื้อเป็น router: ดู `st.session_state["role"]` แล้วพาไปหน้าที่ถูก (คลังทั่วไปเดิมไม่แตะ logic)
- `auth.py` เพิ่ม: `ensure_session()`, `render_landing()` (3 การ์ด), ฟอร์ม login 4 แบบ, `logout()`, `render_topbar_logout()`, `find_open_activity()`
- login: general=APP_PASSWORD / user=รหัสกิจกรรม+ชื่อ (เช็คกิจกรรมเปิด+hash) / admin=Users tab / superuser=secrets
- หน้า user/admin/superuser ตอนนี้เป็น placeholder (รอเฟส 3-5)
- ✅ ทดสอบผ่าน (AppTest): landing 3 การ์ด / login ครบ 4 ทาง / รหัสผิดถูกปฏิเสธ / รหัสกิจกรรมผิดถูกปฏิเสธ / logout กลับ landing / คลังทั่วไปยังเข้าได้ 3 แท็บ

**✅ เฟส 3: หน้า user (เสร็จ 2026-06-25)**
- เพิ่มไฟล์ `page_activity_user.py` — 2 แท็บ: ส่งรูปเข้ากิจกรรม + ดูรูปในกิจกรรมนี้
- ส่งรูป: กรอกแค่รูป (ชื่อผู้ส่งมาจาก login) → reuse pipeline เดิม (บีบอัด/อัป Drive/retry) → บันทึกผ่าน `append_activity_row()` (แผนก/หมวด/ชื่อเรื่อง/แท็กว่าง + activity_id)
- ดูรูป: กรองด้วย activity_id เห็นเฉพาะกิจกรรมตัวเอง + ดาวน์โหลดเดี่ยว/zip (reuse `build_zip`)
- เชื่อมเข้า `app.py` (แทน placeholder user)
- ✅ ทดสอบผ่าน (AppTest + round-trip จริง): ส่งรูปสำเร็จ / แถวมี activity_id + ผู้ส่งถูก + คอลัมน์เดิมว่าง / แท็บดูรูปเห็น "พบ 1 รูป" / **isolation: รูปคลังทั่วไป+กิจกรรมอื่นไม่โผล่** (ลบข้อมูลทดสอบหมดแล้ว)

**✅ เฟส 4: หน้า admin (เสร็จ 2026-06-25)**
- เพิ่มไฟล์ `page_activity_admin.py` — 3 แท็บ: กิจกรรมของฉัน / คลังภาพกิจกรรม / ภาพรวม
- สร้างกิจกรรม: ตั้งรหัสเอง หรือเว้นว่าง=สุ่มให้ (6 ตัว ตัด 0/O/1/I) → เก็บ hash → โชว์รหัสจริงให้ก๊อปแจก + กันรหัสซ้ำกับกิจกรรมที่เปิดอยู่
- เปิด/ปิดกิจกรรม, คลังภาพ+ดาวน์โหลด+ลบรูป (เฉพาะกิจกรรมตัวเอง), ภาพรวม (การ์ด+กราฟ)
- ทุกอย่างกรองด้วย "คนสร้าง" = username → admin เห็นเฉพาะของตัวเอง
- เชื่อมเข้า `app.py`
- ✅ ทดสอบผ่าน (AppTest + round-trip): login / สร้าง(ตั้งเอง+สุ่ม) / กันรหัสซ้ำ / เปิด-ปิด / **isolation กิจกรรม+รูปของ admin อื่นไม่โผล่** (ลบข้อมูลทดสอบหมดแล้ว)

**✅ เฟส 5: หน้า superuser (เสร็จ 2026-06-26)**
- เพิ่มไฟล์ `page_activity_superuser.py` — 5 แท็บ:
  1) 📊 **Dashboard** — พื้นที่ Drive จริง (progress bar ใช้ไป/15GB + เหลือ + รูปทั้งหมด/ขนาดเฉลี่ยประมาณ), ภาพรวมกิจกรรม (การ์ด: เปิดอยู่/รูปเดือนนี้/admin ใช้งาน + ตารางกิจกรรมเรียงตามจำนวนรูป), แจ้งเตือน (เปิดแล้วยังไม่มีรูป / เปิดค้าง >30 วันไม่มีรูปใหม่)
  2) 🎯 **จัดการกิจกรรม** — superuser สร้างกิจกรรมเองได้ (reuse `_create_activity` จากหน้า admin, creator = username superuser) + เปิด/ปิดได้ "ทุก" กิจกรรมของทุก admin
  3) 🖼️ **คลังภาพทุกกิจกรรม** — เห็นรูปของทุก admin/กิจกรรม + กรองตามกิจกรรม + ZIP + ลบรูป (โชว์ชื่อกิจกรรม+คนสร้างใต้รูป)
  4) 👥 **จัดการบัญชี admin** — สร้างบัญชี (กันชื่อซ้ำ, รหัสเก็บ hash) + เปิด/ปิดบัญชี + **ลบบัญชีถาวร** (ขั้นยืนยัน, ใช้ `delete_user()` ใหม่ — ลบเฉพาะแถวใน Users กิจกรรม/รูปที่เคยสร้างไม่หาย)
  5) 📁 **คลังภาพทั่วไป (เดิม)** — reuse 3 หน้าเดิมเป็น sub-tabs (superuser เข้าคลังเก่าได้)
- `google_utils.py`: เพิ่ม `delete_user()` (ลบแถวใน Users, กันลบแถวหัวตาราง)
- `app.py`: เชื่อม superuser (แทน placeholder) + เปิดปุ่มรีเฟรชใน top bar (`show_refresh=True`)
- ✅ ทดสอบผ่าน (AppTest): login superuser → 5 แท็บเรนเดอร์ครบ / มีปุ่มสร้างกิจกรรม + ลบบัญชี admin / top bar มีรีเฟรช+ออกจากระบบ / การ์ด metric ขึ้น / คลังทั่วไปฝังในแท็บ 5 ทำงานปกติ / **0 exception**

**✅ ปรับปรุงหลังเฟส 5 (2026-06-26): หน้า "เข้าร่วมกิจกรรม" มี dropdown เลือกชื่อ**
- `auth.py` `_login_user()`: เปลี่ยนจาก "กรอกรหัสอย่างเดียว" → **เลือกชื่อกิจกรรมที่เปิดอยู่ (dropdown) + กรอกรหัส + ชื่อ** รองรับหลายกิจกรรมเปิดพร้อมกันชัดเจนขึ้น
- dropdown ใช้ `activity_id` เป็นค่าจริง (กันชื่อซ้ำ) โชว์เป็นชื่อด้วย `format_func` ; โชว์เฉพาะกิจกรรมสถานะ "เปิด" ; ถ้าไม่มีเปิดเลย → ข้อความแนะนำแทนฟอร์ม
- รหัสต้องตรงกับ "กิจกรรมที่เลือก" เท่านั้น (verify hash ของ activity_id ที่เลือก) — กันกรอกรหัสกิจกรรมอื่นแล้วหลุดเข้าผิดอัน
- ⚖️ trade-off ที่ผู้ใช้เลือก: ยอมโชว์รายชื่อกิจกรรมที่เปิดให้ทุกคนเห็น แต่ยังต้องมีรหัสถึงเข้าได้
- ✅ ทดสอบผ่าน (AppTest round-trip): dropdown โชว์กิจกรรมที่เปิด / รหัสผิดถูกปฏิเสธ / รหัสถูกเข้าได้+ผูก activity ถูกต้อง (ลบข้อมูลทดสอบแล้ว)

**✅ เฟส 6: ทดสอบรวมทุก role + แก้บั๊ก isolation (เสร็จ 2026-06-26)**
- 🐛 **เจอบั๊ก isolation:** หน้าคลังทั่วไปเดิม (`page_gallery` + `page_dashboard`) ใช้ `load_data()` ดึง "ทุกแถว" รวมรูปกิจกรรม (แผนก/หมวดว่าง) → รูปกิจกรรมหลุดมาโผล่ในคลังทั่วไปเมื่อเลือกแผนก "ทั้งหมด" + ทำตัวเลข Dashboard เพี้ยน
- 🔧 **แก้:** เพิ่ม `load_general_data()` ใน `google_utils.py` (กรองเฉพาะแถว `activity_id` ว่าง) → `page_gallery`/`page_dashboard` เปลี่ยนมาใช้ตัวนี้ (`page_upload` ไม่ต้องแก้ เพราะแค่เขียน บันทึก activity_id ว่างอยู่แล้ว)
- ✅ ชุดทดสอบรวม (AppTest) ผ่านครบ 6 ข้อ:
  1) Landing 3 การ์ด · 2) คลังทั่วไป login รหัสผิด/ถูก + 3 แท็บ · 3) **isolation** (เพิ่มแถวรูปกิจกรรม → คลังทั่วไปไม่เห็น, ลบทิ้งแล้ว) · 4) user dropdown + รหัสผิดปฏิเสธ · 5) admin สร้างบัญชีชั่วคราว→login รหัสผิด/ถูก + แท็บ (ลบบัญชีแล้ว) · 6) superuser รหัสผิด/ถูก + 5 แท็บ
- ℹ️ ข้อมูลจริงในชีตตอนนี้มี ~4 รูป (get_all_records) — เลข "998" ที่เคยเห็นคือ `row_count` (ขนาดกริดที่จองไว้) ไม่ใช่จำนวนรูป

**✅ เฟส 7: README + push ขึ้น production (เสร็จ 2026-06-26) 🎉**
- อัปเดต `README.md` (ระบบกิจกรรม + 3 ระดับสิทธิ์ + ตารางไฟล์ใหม่ + วิธีตั้ง secrets ใหม่) + `secrets.toml.example` มี `HASH_SALT`/`SUPERUSER_USER`/`SUPERUSER_PASS`
- ตรวจไม่มีไฟล์ลับ staged → commit `fa2e3f4` (12 ไฟล์, +1490 บรรทัด) → **push `main` สำเร็จ** → Streamlit Cloud redeploy
- ✅ ใส่ Streamlit Cloud Secrets (HASH_SALT/SUPERUSER_USER/SUPERUSER_PASS) ครบ → **ใช้งานจริง live ครบทุก role แล้ว** ที่ https://image-warehouse-mis.streamlit.app/

**🩹 ปัญหาตอน deploy ครั้งแรก + วิธีแก้ (บันทึกกัน ติดซ้ำ — 2026-06-26)**
1. **ImportError หลัง push** (`cannot import ... from google_utils`): Streamlit Cloud รีรัน `app.py` แต่ไม่รีสตาร์ท process → โมดูล `google_utils` ตัวเก่าค้างใน memory (sys.modules) ไม่มีฟังก์ชันใหม่ พอไฟล์ใหม่ไปเรียกเลยหาไม่เจอ
   → **แก้: Manage app → เมนู ⋮ → Reboot app** (บูต process ใหม่สะอาด) — โค้ดไม่ได้ผิด ยืนยันด้วย clean import ในเครื่องผ่าน
2. **superadmin login บอกรหัสผิด + error แวบเดียว**: วาง 3 บรรทัด secret ใหม่ผิดตำแหน่ง — ไปอยู่ "ใต้" `[google_oauth]` ใน Secrets → TOML นับเป็น `google_oauth.SUPERUSER_USER` → `st.secrets["SUPERUSER_USER"]` ระดับบนสุดหาไม่เจอ (ค่าว่าง) → login ไม่ผ่าน + `HASH_SALT` ก็หาไม่เจอเลยเด้ง error แวบนึง
   → **แก้: กฎ TOML — คีย์เดี่ยว (ไม่มี `[ ]`) ต้องอยู่บนสุดทั้งหมด ก่อนบรรทัด `[google_oauth]` เสมอ** (อะไรที่อยู่ใต้ table header จะถูกนับเป็นของกลุ่มนั้น) ใช้กับทั้งไฟล์ local และ Cloud Secrets

---

## ℹ️ ข้อควรรู้เรื่อง Hosting (Streamlit Community Cloud) — อ่านเมื่อสงสัย

**ฟรีไหม / ต้องต่ออายุไหม?**
- ✅ **ฟรีถาวร** สำหรับ public app — ไม่ต้องใส่บัตร ไม่มีค่ารายเดือน ไม่มีวันหมดอายุ ไม่ต้องต่ออายุ
- เงื่อนไข: repo บน GitHub ต้องยังอยู่และเป็น public (https://github.com/Ben45-alt/image-warehouse)

**แอป "หลับ" (sleep) — ปกติ ไม่ใช่แอปพัง**
- ถ้าไม่มีคนเข้าสักพัก แอปจะหลับเพื่อประหยัดทรัพยากร
- พอมีคนเปิดลิงก์อีกครั้ง จะ "ตื่น" เอง รอ ~30 วิ–1 นาที (บูตใหม่) แล้วใช้ได้ปกติ
- ถ้าทิ้งไว้นานมากๆ (เป็นเดือน) Streamlit อาจส่งอีเมลถามว่ายังใช้อยู่ไหม → กดยืนยัน/reboot ในเมลก็กลับมา

**ฝั่ง Google (สำคัญกว่า)**
- OAuth consent screen = **Production** แล้ว → refresh token ไม่หมดอายุตามเวลา
- ⚠️ Google จะเพิกถอน token ถ้า **ไม่ถูกใช้เลยเกิน 6 เดือน** → ถ้าใช้งานปกติไม่ต้องห่วง
- รูปเก็บใน Google Drive ของ `tfp.data.mis@gmail.com` (ฟรี 15 GB) → ถ้ารูปเยอะค่อยขยายทีหลัง

**อัปเดตแอปออนไลน์ยังไง?**
- แก้โค้ดในเครื่อง → `git add` → `git commit` → `git push` → Streamlit Cloud จะ redeploy ให้เองอัตโนมัติ

**ถ้าจะเปลี่ยน Secrets (เช่น เปลี่ยนรหัสผ่าน / refresh token ใหม่)**
- ต้องแก้ **2 ที่**: ไฟล์ `.streamlit/secrets.toml` ในเครื่อง + Secrets ใน Streamlit Cloud (App → Settings → Secrets)

---

## ➕ ฟีเจอร์เพิ่มหลัง production

**✅ ปุ่มลบกิจกรรม 2 ระดับ (เสร็จ 2026-06-30)** (`google_utils.py`, `page_activity_admin.py`, `page_activity_superuser.py`)
- เพิ่ม `delete_activity(activity_id)` ใน `google_utils.py` — ลบรูปทุกใบของกิจกรรม (ไฟล์ใน Drive + แถวใน Sheet1 ผ่าน `delete_photo` หาแถวจากลิงก์ใหม่ทุกครั้ง กันเลขแถวเลื่อน) แล้วลบแถวกิจกรรมในแท็บ Activities (กันลบแถวหัวตาราง) + ล้าง cache + คืนจำนวนรูปที่ลบ
- **superuser** (หน้า `page_activity_superuser` แท็บ "จัดการกิจกรรม"): ลบได้ "ทุก" กิจกรรม **พร้อมลบรูปทั้งหมด** — มีขั้นยืนยัน + เตือนว่ารูป N ใบจะถูกลบทั้ง Drive และ Sheet กู้คืนไม่ได้
- **admin** (หน้า `page_activity_admin` แท็บ "กิจกรรมของฉัน"): ลบได้ "เฉพาะกิจกรรมของตัวเอง" ที่ **0 รูป** (ไว้แก้ตอนสร้างผิด) — ถ้ามีรูปแล้วปุ่มถูก disable + บอก "แจ้ง superuser ให้ลบแทน"
  - 🔒 กัน race: ตอนกดลบจริง อ่านชีตสดอีกครั้ง ถ้าเจอว่ามีรูปเพิ่งถูกส่งเข้ามาพอดี → ปฏิเสธการลบ
- ✅ ทดสอบผ่าน (AppTest round-trip จริง 17/17): admin ลบกิจกรรมเปล่าได้/กิจกรรมมีรูปปุ่ม disable ลบไม่ได้ + กิจกรรมมีรูปยังอยู่ ; superuser ลบกิจกรรมมีรูปได้ → รูปหายทั้ง Drive+Sheet + แถวกิจกรรมหาย (ลบข้อมูลทดสอบหมดแล้ว ชีตกลับ 4 กิจกรรม/21 แถว)

---

## 🚀 อัปเกรดใหญ่ Round 1+2 (deploy แล้ว 2026-07-16, commit 6e707ab)

> ประชุมกับผู้ใช้ตกลง 5 ฟีเจอร์ แบ่งทำ 3 รอบ — Round 1+2 push ขึ้น production แล้ว, Round 3 (แชร์อัลบั้มเฉพาะคน) ยังไม่ทำ

**✅ Round 1 — metadata + ชื่อไฟล์ + phash** (`image_utils.py`, `google_utils.py`, `page_upload.py`, `page_activity_user.py`)
- รูป .jpg ฝัง metadata: เก็บ EXIF กล้องเดิม (DateTimeOriginal/GPS) **+** ฝังบริบท (ชื่อเรื่อง/กิจกรรม=ImageDescription, ผู้ส่ง=Artist, วันเวลา) — ใช้ `piexif` + ช่อง **XP (UTF-16)** ให้ Windows Explorer อ่านไทยถูก (ช่อง EXIF มาตรฐานเป็น ASCII ไทยจะ mojibake ในบางโปรแกรม แต่ XP ครอบให้แล้ว)
- ชื่อไฟล์: กิจกรรม = `ชื่อกิจกรรม_ลำดับ3หลัก_เวลา.jpg` / คลังทั่วไป = `แผนก_หมวด_เวลา.jpg` (มี `sanitize_name` ตัดอักขระอันตราย เฉพาะรูปใหม่)
- เก็บ `phash` (dHash 64 บิต เขียนเอง ไม่พึ่ง lib นอก) ตอนอัป ไว้ตรวจรูปซ้ำภายหลัง (UI เตือน/สแกนซ้ำ = งานรอบหลัง)
- ✅ ทดสอบในเครื่อง: metadata ฝังไทยอ่านกลับครบ + เก็บ DateTimeOriginal เดิมไม่ทับ / phash ซ้ำ=0 ต่าง=23 / ชื่อไฟล์ถูก

**✅ Round 2 — ถังขยะ + audit log + auto-close + ล็อก user** (`google_utils.py`, `auth.py`, ทุกหน้า gallery)
- **ถังขยะ 30 วัน:** ลบรูป = `trashed=True` ใน Drive (Google ล้างเอง 30 วัน) + ทำเครื่องหมายแถว (สถานะ/วันที่ลบ/ลบโดย) ไม่ลบแถวจริง → รูปหายจากคลังทันทีแต่กู้คืนได้. `trash_photo`/`restore_photo` + `load_active_data`/`load_trash_data`
  - แท็บถังขยะใหม่: **admin** เห็น/กู้เฉพาะกิจกรรมตัวเอง · **superuser** เห็นทั้งระบบ (ทุกกิจกรรม+คลังทั่วไป) กู้คืน+ลบถาวร · **user ไม่มีถังขยะ**
  - หลัง 30 วัน Drive ลบไฟล์เอง แถวในชีตค้าง → **ตั้งใจลบเอง** (ไม่ทำ cron)
- **Audit log:** แท็บ `Log` (เวลา/ผู้ทำ/role/การกระทำ/รายละเอียด/activity_id) — จดตอน อัป/ลบ/กู้คืน/ลบกิจกรรม/ลบถาวร. `log_action` ครอบ try/except ไม่ให้ log พังงานหลัก
- **auto-close 7 วัน:** กิจกรรมครบ 7 วันจากวันสร้าง → ผู้เข้าร่วม login ไม่ได้ (virtual ไม่แก้ชีต, `is_activity_open`/`open_activities`) + หน้า admin/superuser โชว์ "⏰ ปิดอัตโนมัติแล้ว"
- **ล็อก role=user ลบไม่ได้:** ยืนยันด้วย static test ว่า `page_activity_user` ไม่มีฟังก์ชันลบเลย
- ✅ ทดสอบในเครื่อง: auto-close ถูกทุกขอบเขต / compile+cross-import ครบ / user ปลอดฟังก์ชันลบ

**⚙️ schema (รันจริงกับชีต production แล้ว):** เพิ่ม Sheet1 คอลัมน์ J=`phash` K=`สถานะ` L=`วันที่ลบ` M=`ลบโดย` + สร้างแท็บ `Log` — ผ่านสคริปต์ `_migrate_schema.py` (ทำงานเทียบเท่า `ensure_schema` แต่รันนอก streamlit เพราะ local streamlit พังจาก starlette/Py3.14) แล้วลบสคริปต์ทิ้ง. ยืนยัน `get_all_records` อ่าน 21 แถวเดิมได้ปกติ คอลัมน์ใหม่ว่าง (backward-compatible)

**⏳ รอทดสอบบนเว็บจริง (หลัง redeploy เสร็จ):**
1. ถ้าเจอ ImportError → **Reboot app** (module เก่าค้าง sys.modules — บทเรียนเดิม) + piexif เป็น dep ใหม่ อาจต้อง reboot ให้ติดตั้ง
2. ทดสอบ: อัปรูป (เช็คชื่อไฟล์ใหม่ + เปิด properties รูปดู metadata ไทยบน Windows) · ลบรูป → ไปถังขยะ · กู้คืน · ดูแท็บ Log ว่าจดครบ

**✅ Round 3 — แชร์อัลบั้มเฉพาะคน (deploy แล้ว 2026-07-16, commit 460472f)** (`auth.py`, `google_utils.py`, `app.py`, `page_activity_user/viewer/admin/superuser.py`)
- **ถ่าย ≠ ดู:** หน้า user (role=user) เหลือ "ส่งรูป" อย่างเดียว ไม่เห็นอัลบั้มคนอื่นแล้ว (ตัดแท็บดูรูปออก)
- **role ใหม่ `viewer`** — ไฟล์ `page_activity_viewer.py` ดู+ดาวน์โหลด/zip อัลบั้ม ส่ง/ลบไม่ได้
- **แชร์อัลบั้มแบบไดรฟ์:** แต่ละกิจกรรมเจ้าของเลือก 🌐 ทุกคน (สาธารณะ) / 👤 เฉพาะคนที่แชร์ — โหมดเฉพาะคน admin กด "เพิ่มคนดู" → ระบบออก **รหัสดูส่วนตัวรายคน** (เก็บ hash ในแท็บ Shares) ก๊อปแจก + **ถอนสิทธิ์รายคนได้** · **default = เฉพาะคน** (กันส่อง)
- **landing เปลี่ยนเป็น 3 การ์ด:** คลังทั่วไป / กิจกรรม (เมนูย่อย: 📤 ส่งรูป | 🖼️ ดูอัลบั้ม) / เข้าสู่ระบบ
- **viewer เข้า 2 ทาง:** กดชื่ออัลบั้มสาธารณะ (ไม่ต้องรหัส) หรือกรอกรหัสดูส่วนตัว → เข้าอัลบั้มที่ถูกแชร์
- admin/superuser: กล่อง `render_share_panel` (reuse) ต่อกิจกรรม ตั้ง visibility + จัดการคนดู
- schema: Activities +คอลัมน์ `การมองเห็น` + แท็บ `Shares` (รัน `ensure_schema()` ตัวจริงผ่าน stub streamlit กับชีต production แล้ว — กิจกรรมเดิม 4 อัน default=เฉพาะคน)
- ✅ ทดสอบในเครื่อง: compile + stub-import ทุกโมดูลผ่าน + cross-import ครบ (end-to-end บนเว็บรอผู้ใช้เทสต์)

**🔑 หมายเหตุ deploy:** เจอบั๊ก NameError รอบแรก (commit 53477fd แก้แล้ว) — `is_activity_expired(days=AUTO_CLOSE_DAYS)` อ้างค่าคงที่เป็น default arg ที่ประเมินตอนโหลดโมดูล แต่ค่าคงที่อยู่ทีหลังในไฟล์. **บทเรียน: local streamlit พัง (starlette/Py3.14) compile อย่างเดียวไม่จับ NameError → ใช้วิธีสตับ streamlit ปลอมแล้ว import จริงทุกโมดูลก่อน push ทุกครั้ง** (จับ load-time error ได้). สตับ + ใส่ st.secrets จริง = รัน ensure_schema ตัวจริง migrate ชีตได้โดยไม่ต้องเขียน migration ซ้ำ

**⏳ รอทดสอบบนเว็บจริง (ทั้ง 3 รอบ):** ถ้า error → Reboot app. เทสต์: อัป(ชื่อไฟล์+metadata) · ลบ→ถังขยะ+กู้คืน · Log · แชร์อัลบั้มเฉพาะคน→เอารหัสไปเปิดหน้าดูอัลบั้ม · อัลบั้มสาธารณะ

**✅ landing 4 การ์ด (commit b3bcfbb):** คลังทั่วไป / ส่งรูปกิจกรรม / ดูอัลบั้ม / เข้าสู่ระบบ (แยกส่ง-ดู จบใน 1 คลิก)

**✅ ตรวจรูปซ้ำ pHash — ครบข้อ 5 (commit 67fb614)** (`google_utils.py`, `page_upload.py`, `page_activity_user.py`, `page_activity_admin/superuser.py`)
- **เตือนตอนอัป:** คำนวณ phash รูปใหม่ → เทียบในขอบเขตเดียวกัน (กิจกรรม=ในกิจกรรมนั้น / คลังทั่วไป=ในคลัง) Hamming<=5 = คล้าย → พักไว้ถามยืนยัน "จะส่งซ้ำไหม" (ใช้ session flash + pending confirm)
- **สแกนหาซ้ำ:** ปุ่ม "🔍 หารูปซ้ำ" ใน gallery admin/superuser → `group_duplicates` (union-find) จัดกลุ่มรูปคล้ายกัน → เลือกลบใบซ้ำ (ลงถังขยะ)
- `find_similar_photo` + `group_duplicates` ใน google_utils (import `hamming_distance` จาก image_utils) · ไม่ต้อง migrate schema (phash มีอยู่แล้ว)
- ✅ ทดสอบ logic ในเครื่อง: find_similar เจอ/ไม่เจอถูก · group_duplicates จัดกลุ่มถูก · stub-import ทุกโมดูลผ่าน

**🎉 แผนหลัก 5 ฟีเจอร์ครบแล้ว**

**✅ ของเสริมหลังครบแผนหลัก:**
- **QR Code + deep-link** (commit e062e92, 6a23b8a): `qr_utils.py` + `render_code_with_qr(code,key,kind)` โชว์รหัสตัวอักษร+ปุ่มเปิด/ปิด QR. QR เก็บ URL deep-link (view→`?viewcode=` เข้าอัลบั้มเลย / act→`?actcode=` เปิดส่งรูป+เติมรหัส). `auth.handle_deeplink()` อ่าน query param ตอนเปิดแอป auto-login/prefill. deps +qrcode
- **Audit Log viewer** (commit 373b409): `load_log()` + แท็บ '📋 Log' ในหน้า superuser (ตาราง ใคร/ทำอะไร/เมื่อไหร่ ใหม่สุดก่อน + กรอง action/role + ค้นหา + map activity_id→ชื่อ)
- **อัปหลายรูป + เอากล้องในเว็บออก**: หน้าส่งรูป (คลังทั่วไป+กิจกรรม) เปลี่ยน `st.camera_input` → `st.file_uploader(accept_multiple_files=True)` (บนมือถือช่องแนบไฟล์มีปุ่มถ่ายรูป fullscreen ให้อยู่แล้ว จอไม่เล็กเหมือน camera_input). อัปทีละหลายรูป มี progress bar + ตรวจรูปซ้ำแบบชุด (เจอซ้ำ→เลือก ส่งทั้งหมด/เฉพาะไม่ซ้ำ/ยกเลิก). กันชื่อไฟล์ซ้ำในชุด (กิจกรรม=seq เพิ่มต่อ / คลังทั่วไป=เติม _NN)

---

## 📁 เผยแพร่รูปกิจกรรม + คลังภาพแบบโฟลเดอร์ (2026-07-17)

> **โจทย์:** มีคนที่ไม่เกี่ยวกับกิจกรรมอยากดูรูป — ไม่อยากเปิดทั้งอัลบั้มให้ อยากให้ดูแค่ไม่กี่ใบ
> **ทางที่เลือก (ผู้ใช้เสนอเอง):** รูปที่เลือกไปโผล่ใน "คลังทั่วไป" เป็นโฟลเดอร์ของกิจกรรมนั้น → คนในบริษัทที่มีรหัสคลังทั่วไปอยู่แล้วเข้าดูเองได้ **ไม่ต้องออกรหัสใหม่/ส่ง QR ให้ใคร**

**🔑 หลักการสำคัญ: ไม่ได้ "ย้าย" รูป** — รูปมี `activity_id` ติดตัวอยู่แล้ว แค่ติดป้าย `เผยแพร่=ใช่` แล้วหน้าคลังทั่วไปหยิบมาโชว์เพิ่ม → รูปยังอยู่ในอัลบั้มกิจกรรมเหมือนเดิม เห็นได้ 2 ที่พร้อมกัน ปลดป้ายก็หายทันที **ไม่แตะ Drive / ชื่อไฟล์ / metadata เลย** (เลี่ยงปัญหาชื่อไฟล์+EXIF ที่ฝังชื่อกิจกรรมไว้)

**schema:** Sheet1 + คอลัมน์ **N = `เผยแพร่`** (`""` = ไม่เผยแพร่ / `ใช่`) — รัน `ensure_schema()` ตัวจริงผ่าน stub กับชีต production แล้ว (33 แถวเดิมอ่านได้ปกติ คอลัมน์ใหม่ว่าง = backward-compatible)

**ฝั่งกิจกรรม (คนติดป้าย):** `render_publish_toggle()` ใน `page_activity_admin.py` (reuse ทั้ง admin + superuser) — ปุ่ม 🌐 เผยแพร่เข้าคลังทั่วไป / ยกเลิกเผยแพร่ ใต้รูปแต่ละใบในคลังภาพกิจกรรม + จดลง Log · **role=user / viewer ไม่มีปุ่มนี้**

**หน้าคลังทั่วไป (`page_gallery.py`) รื้อเป็นโฟลเดอร์ 2 ชั้น:**
- ชั้นนอก: การ์ดโฟลเดอร์ 📁 **แผนก** (โชว์เฉพาะแผนกที่มีรูป + ปุ่ม "แสดงแผนกที่ยังไม่มีรูป") และ 🎉 **รูปจากกิจกรรม** (1 กิจกรรม = 1 โฟลเดอร์ เฉพาะที่มีรูปเผยแพร่)
- ชั้นใน: กริดรูปเดิม + breadcrumb + ZIP + แบ่งหน้า
  - โฟลเดอร์แผนก → กรองหมวด (ปุ่มแนวนอน ไม่ทำเป็นโฟลเดอร์อีกชั้น กันกดลึก) + คำค้น/ช่วงวันที่ใน expander + **ลบรูปได้** (ของคลังทั่วไปเอง)
  - โฟลเดอร์กิจกรรม → ดู+ดาวน์โหลดอย่างเดียว **ลบไม่ได้** (เจ้าของตัวจริงคือฝั่งกิจกรรม)
- ⚠️ **ตั้งใจไม่ใส่รูปปกในการ์ดโฟลเดอร์** — ทุกรูปต้องโหลด bytes จริงจาก Drive (hotlink thumbnail ใช้ไม่ได้) ถ้าใส่รูปปกต้องโหลดรูปเท่าจำนวนโฟลเดอร์ก่อนหน้าจะขึ้น = ยิ่งรูปเยอะยิ่งช้า (มติผู้ใช้: ไม่เอารูปปก)

**พฤติกรรมพ่วง:** รูปเผยแพร่ที่ถูกลบลงถังขยะ → หายจากคลังทั่วไปทันที (กู้คืนก็กลับมา) · กิจกรรมปิด/auto-close แล้ว รูปที่เผยแพร่ไว้ยังอยู่ · ลบกิจกรรมทั้งอัน → โฟลเดอร์หายเอง · **Dashboard ไม่นับรูปเผยแพร่** (ยังใช้ `load_general_data()` เดิม — ผู้ใช้บอกไม่ได้ใช้ส่งรายงานใคร)

**✅ ทดสอบในเครื่องผ่านครบ:** stub-import ทุกโมดูล · logic กรอง (เผยแพร่/ถังขยะ/ชีตเก่าไม่มีคอลัมน์) · **isolation: รูปกิจกรรมที่เผยแพร่ไม่หลุดเข้า `load_general_data()`** (บั๊กเฟส 6 ไม่กลับมา) · round-trip จริงกับชีต production (เปิด→เช็ค→ปิดคืน ไม่มีข้อมูลค้าง)

---

## 👀 ปัญหาที่เจอ — รอสังเกตเพิ่ม (ยังไม่แก้ 2026-06-30)

**ถ่ายรูปบนมือถือ (Safari) บางครั้งหมุนนาน บางครั้งไวมาก**
- อาการจริง (ผู้ใช้ยืนยัน): ใช้ต่อเนื่องในวันเดียวกัน = ปกติไว; แต่พอ **ปิดแอป Safari แล้วเปิดลิงก์ใหม่เพื่อถ่ายอีกรอบ** → ช้าบ้างไวบ้าง
- 🔍 วินิจฉัย: **ไม่ใช่บั๊กโค้ด** — เป็นพฤติกรรม Streamlit Community Cloud (ฟรี) ที่ "หลับ" แอปเมื่อไม่มีทราฟฟิกสักพัก พอเปิดลิงก์ใหม่ตอนแอปหลับต้องบูตใหม่ ~30วิ–1นาที (cold start); ถ้าแอปยังตื่นอยู่ก็ไว → อธิบาย "บางครั้งช้า บางครั้งไว" ได้พอดี
- ปัจจัยเสริม (เล็กน้อย): `st.camera_input` อัปรูป "ดิบ" เป็น PNG ขนาดใหญ่ขึ้น server ก่อนค่อยบีบอัดตอน save (บีบอัดเกิดทีหลัง เลยรู้สึกว่า "save ไว"); และ `st.tabs` รันทุกแท็บทุก rerun
- ✅ มติ (ผู้ใช้): **ปล่อยไว้ก่อน ยังไม่แก้** — ต้องให้คนใช้หลายๆ คนจริงก่อน ค่อยดูว่าคุ้มจะแก้ไหม
- 🛠️ ถ้าจะแก้ทีหลัง (เรียงตามผล):
  1. **keep-alive** ใช้บริการ uptime ฟรีภายนอก (UptimeRobot / cron-job.org) ping ลิงก์ทุก ~5 นาที (เฉพาะเวลาทำงานก็ได้) → แอปไม่หลับ เปิดมาไวตลอด *(แก้ตรงอาการที่สุด)*
  2. มือถือใช้ "แนบไฟล์ → ถ่ายจากกล้องเครื่อง" แทนกล้องในเว็บ (ได้ JPEG บีบแล้ว เล็กกว่า PNG ของ camera_input มาก)
  3. เปลี่ยน `st.tabs` → เมนูเลือกหน้า (รันเฉพาะหน้าที่เปิด) ลด overhead คงที่ทุก rerun
- ℹ️ `st.camera_input` ปรับความละเอียด/บีบอัดฝั่งเบราว์เซอร์ผ่าน Python ไม่ได้ — ต้องเลี่ยงด้วยข้อ 2 หรือทำ custom component (งานใหญ่)

---

## ⚠️ บทเรียน
อย่ารัน OAuth refresh token ถี่ๆ ในเวลาสั้น — Google จะ disable client ทดสอบกับ Google API เท่าที่จำเป็นพอ
