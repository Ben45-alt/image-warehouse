# Prompt สำหรับ Claude CLI — โปรเจกต์ "คลังภาพกลางของบริษัท"

> วิธีใช้: ก๊อปข้อความตั้งแต่เส้น `=====` ด้านล่างไปวางใน Claude CLI ได้เลย
> (ส่วนบนนี้เป็นโน้ตของเรา ไม่ต้องก๊อป)

**สิ่งที่เตรียมไว้แล้ว:** Google Cloud project, เปิด Drive API + Sheets API, สร้าง Google Sheet + โฟลเดอร์ Drive แล้ว
**ยังไม่ได้ทำ:** OAuth credentials (ให้ Claude CLI พาทำ)

=====================================================================

# โจทย์: สร้างเว็บแอป "คลังภาพกลางของบริษัท" ด้วย Python + Streamlit

ช่วยสร้างเว็บแอปภายในองค์กรด้วย **Python + Streamlit** ชื่อ "คลังภาพกลางของบริษัท"
เป็นที่เก็บรูปภาพของทุกแผนก ทุกประเภท ไว้ที่เดียว ให้พนักงานอัปโหลดรูปเข้ามา และให้ทุกคนค้นหา/ดึงรูปไปใช้งานได้เอง

**หมายเหตุสำคัญเกี่ยวกับผู้ใช้:** ผมเป็น IT Support กำลังเรียน Python ระดับ beginner
- ขอโค้ดที่ **อ่านง่าย เรียบง่าย** ใส่ **คอมเมนต์ภาษาไทย** อธิบายจุดสำคัญ
- อธิบายขั้นตอนการติดตั้งและรัน **ทีละ step**
- code block ต้อง copy-paste แล้วใช้ได้เลย

---

## 1. Stack และไลบรารีที่ใช้
- Frontend/Backend: **Streamlit**
- เก็บไฟล์รูป: **Google Drive**
- เก็บข้อมูลประวัติ (ฐานข้อมูล): **Google Sheets**
- บีบอัด/ย่อรูป: **Pillow (PIL)**
- เชื่อม Google: ใช้ **OAuth 2.0** (ดูข้อ 2)
- สร้างไฟล์ ZIP: ใช้ `zipfile` + `io.BytesIO` (in-memory ไม่เขียนลงดิสก์)

ทำไฟล์ `requirements.txt` ให้ครบด้วย

---

## 2. การเชื่อมต่อ Google — ใช้ OAuth (ห้ามใช้ Service Account)

**สำคัญมาก:** ผมใช้ Gmail ส่วนตัวแบบฟรี (ไม่มี Google Workspace)
- **ห้ามใช้ Service Account** เพราะ Service Account ไม่มีพื้นที่ Drive เป็นของตัวเอง → อัปโหลดไฟล์ขึ้น Drive จะ error `storageQuotaExceeded`
- **ให้ใช้ OAuth 2.0** = แอป login ด้วยบัญชี Google ของผมเอง → รูปจะไปเก็บใน Drive 15GB ของผมตามปกติ

กรุณา **พาผม setup OAuth ทีละขั้น** ดังนี้:
1. สอนสร้าง **OAuth consent screen** (External + ใส่อีเมลตัวเองเป็น test user)
2. สอนสร้าง **OAuth Client ID** (Desktop app) แล้วดาวน์โหลด `.json`
3. เขียน **สคริปต์เล็กๆ ให้รัน 1 ครั้ง** เพื่อ authorize แล้วได้ **refresh token**
4. เก็บ client_id / client_secret / refresh_token ไว้ใน **Streamlit Secrets** (`.streamlit/secrets.toml`) — ห้าม hard-code ไฟล์ credential ลงในโค้ดตรงๆ
5. ทำไฟล์ `secrets.toml.example` เป็นตัวอย่างให้ด้วย และเพิ่ม `.gitignore` กันไฟล์ลับหลุดขึ้น GitHub

ค่า config ที่ต้องอ่านจาก secrets: `SHEET_URL` (หรือ Sheet ID) และ `DRIVE_FOLDER_ID`

---

## 3. โครงสร้างฐานข้อมูล (Google Sheets)
หัวตาราง (คอลัมน์) มีดังนี้ — ปรับให้ตรงกับโค้ด:
```
วันเวลา | แผนก | หมวด | ชื่อเรื่อง | แท็ก | ผู้ส่ง | ลิงก์รูป | ชื่อไฟล์
```
- ตั้งค่า "แผนก" และ "หมวด" เป็น **list ในโค้ด** ที่แก้ไขเพิ่มได้ง่าย (ใส่ค่าตัวอย่างมาให้ก่อน เช่น แผนก: ฝ่ายผลิต/ฝ่ายบุคคล/ฝ่ายบัญชี/IT/คลังสินค้า ; หมวด: ถังดับเพลิง/อุปกรณ์/งานซ่อม/กิจกรรม/เอกสาร/อื่นๆ)

---

## 4. หน้าจอแอป — ทำเป็น 3 หน้า (ใช้ st.tabs หรือ multipage)

### หน้า 1: ส่งรูป (Upload Form)
- Dropdown เลือก **แผนก**
- Dropdown เลือก **หมวด**
- ช่องกรอก **ชื่อเรื่อง/คำอธิบาย**
- ช่องกรอก **แท็ก (tags)** — คั่นด้วยจุลภาค ไว้ค้นหาทีหลัง
- ช่องกรอก **ชื่อผู้ส่ง**
- อัปโหลดรูป: รองรับทั้ง `st.file_uploader` (แนบไฟล์) **และ** `st.camera_input` (ถ่ายจากกล้องมือถือ)
  - จำกัดเฉพาะไฟล์ **JPG, JPEG, PNG**
- ปุ่ม **"บันทึกข้อมูล"** เมื่อกดให้ทำตามลำดับ:
  1. **บีบอัด/ย่อรูปด้วย Pillow**: ความกว้างสูงสุด **1200px** (รักษาสัดส่วน), quality **75-80%**, แปลงเป็น JPEG → เป้าหมายไฟล์ ~100-300 KB
  2. อัปโหลดไฟล์ที่บีบแล้วขึ้นโฟลเดอร์ Google Drive
  3. ตั้งสิทธิ์ไฟล์ให้ "ใครมีลิงก์ดูได้" (anyone with link → reader) เพื่อให้แสดงรูปในแอปได้
  4. บันทึกข้อมูล 1 แถวต่อท้าย Google Sheets (วันเวลาปัจจุบัน + ทุกฟิลด์ + ลิงก์รูป + ชื่อไฟล์)
  5. แจ้งผลสำเร็จด้วย `st.success`

### หน้า 2: คลังภาพ (Gallery)
- แถบ Filter ด้านบน ค้นหาได้ **ทุกแบบรวมกัน**:
  - เลือก **แผนก** (รวมตัวเลือก "ทั้งหมด")
  - เลือก **หมวด** (รวม "ทั้งหมด")
  - เลือก **ช่วงวันที่** (date range)
  - ช่องพิมพ์ **คำค้น (keyword)** — ค้นจาก ชื่อเรื่อง + แท็ก
- แสดงผลเป็น **Grid Layout** (ใช้ `st.columns` เช่น 3-4 รูปต่อแถว) สวยงามสะอาดตา
  - แปลงลิงก์ Drive เป็น **thumbnail URL** (`https://drive.google.com/thumbnail?id=FILE_ID&sz=w400`) เพื่อให้โหลดเร็ว
- ใต้รูปแต่ละรูป: แสดง แผนก / หมวด / วันที่ / ชื่อเรื่อง + ปุ่ม **"ดาวน์โหลดรูปนี้"** (single download)
- ปุ่ม **"ดาวน์โหลดทั้งหมดที่ filter เป็น .zip"** (bulk ZIP download) — รวมทุกรูปที่ตรงเงื่อนไข filter เป็นไฟล์ zip เดียวให้โหลดในคลิกเดียว
- ทำ **pagination** (แบ่งหน้า เช่นหน้าละ 12-20 รูป) กันแอปอืดเมื่อรูปเยอะ

### หน้า 3: Dashboard (ภาพรวมสถิติ)
- การ์ดสรุปตัวเลข: จำนวนรูปทั้งหมด, จำนวนแผนก, รูปที่เพิ่มเดือนนี้
- กราฟแท่ง: จำนวนรูปแยกตาม **แผนก**
- กราฟแท่ง/วงกลม: จำนวนรูปแยกตาม **หมวด**
- กราฟเส้น: จำนวนรูปที่ส่งในแต่ละ **เดือน**
- ตาราง: รายการรูป **ล่าสุด** ที่เพิ่งส่งเข้ามา (เช่น 10 รายการ)
- ใช้ `st.bar_chart` / `st.line_chart` หรือ plotly ก็ได้ ขอแบบเรียบง่าย

---

## 5. เรื่องที่ต้องทำเพิ่ม (cross-cutting)
- **ระบบรหัสผ่านหน้าแอป**: เนื่องจาก deploy บน Streamlit Cloud ลิงก์เป็น public → ใส่ระบบ login ด้วยรหัสผ่านง่ายๆ (อ่านรหัสจาก `st.secrets`) กันคนนอก
- **Caching**: ใช้ `st.cache_data(ttl=...)` ตอนอ่าน Google Sheets เพื่อให้เร็วขึ้น และมีปุ่ม/วิธี refresh cache หลังอัปโหลดรูปใหม่
- **Validation**: เช็คชนิดไฟล์, เช็คว่ากรอกครบก่อนบันทึก, จัดการ error อย่างนุ่มนวล (try/except + ข้อความภาษาไทย)
- โครงสร้างโค้ด: แยกฟังก์ชันให้ชัด เช่น `compress_image()`, `upload_to_drive()`, `append_to_sheet()`, `load_data()` ฯลฯ

---

## 6. การ Deploy (ขอคำแนะนำด้วย)
- จะ deploy ฟรีบน **Streamlit Community Cloud** ผ่าน GitHub
- ช่วยอธิบายขั้นตอน: push ขึ้น GitHub → เชื่อม Streamlit Cloud → ใส่ค่า Secrets ยังไง
- เตือนเรื่องไฟล์ที่ห้าม commit (credential, secrets.toml)

---

## 7. สิ่งที่อยากได้เป็นผลลัพธ์ (Deliverables)
1. โค้ดแอปครบ (จัดไฟล์ให้เรียบร้อย)
2. `requirements.txt`
3. สคริปต์ generate refresh token (รันครั้งเดียว)
4. `secrets.toml.example` + `.gitignore`
5. `README.md` อธิบายวิธี setup OAuth + รัน local + deploy ทีละ step ภาษาไทย

**ขอให้เริ่มจากอธิบาย flow รวมและให้ผม setup OAuth (ข้อ 2) ให้เสร็จก่อน แล้วค่อยไล่เขียนโค้ดทีละหน้า อย่าเทโค้ดยาวๆ มาทีเดียว**
