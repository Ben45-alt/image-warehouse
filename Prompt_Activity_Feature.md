# Prompt (อัปเดต) สำหรับ Claude CLI — เพิ่มระบบ "กิจกรรม + สิทธิ์ 3 ระดับ" (ทำคู่กับระบบเดิม)

> วิธีใช้: รัน Claude CLI ใน **โฟลเดอร์โปรเจกต์เดิม** (image-warehouse) แล้วก๊อปตั้งแต่เส้น `=====` ไปวาง
> (ส่วนบนนี้เป็นโน้ตเรา ไม่ต้องก๊อป)

=====================================================================

# งาน: เพิ่มระบบ "กิจกรรม (Activity)" + ระบบสิทธิ์ 3 ระดับ — โดยให้ทำงาน "คู่กัน" กับระบบเดิม

นี่คือแอป Streamlit "คลังภาพกลางของบริษัท" ที่ deploy แล้ว (Streamlit Cloud + Google Sheets/Drive ผ่าน OAuth)
**กรุณาอ่านโค้ดเดิมทั้งหมดก่อน** (app.py, page_upload.py, page_gallery.py, page_dashboard.py, google_utils.py, config.py และไฟล์อื่นๆ) แล้วค่อยวางแผนแก้ อย่าเพิ่งเขียนโค้ดยาวรวดเดียว

ผมเป็น IT Support เรียน Python ระดับ beginner → ขอโค้ด **อ่านง่าย คอมเมนต์ไทย อธิบายทีละ step** และอย่าทำให้ของเดิมที่ deploy อยู่พัง

---

## หลักการสำคัญ: 2 ระบบอยู่คู่กัน (ไม่ใช่แทนที่)

1. **ระบบเดิม "คลังภาพทั่วไป"** — คงไว้เหมือนเดิมทั้งหมด (ส่งรูปตามแผนก/หมวด, คลังภาพ, dashboard, ข้อมูลเก่า 998 แถวยังใช้งานปกติ) **อย่าแก้ของเดิมโดยไม่จำเป็น**
2. **ระบบใหม่ "กิจกรรม"** — เพิ่มเข้ามาเป็นอีกส่วนหนึ่ง พร้อมสิทธิ์ 3 ระดับ

ทำ **หน้าแรก (landing)** ให้เลือกว่าจะเข้าส่วนไหน:
- **[คลังภาพทั่วไป]** → ใส่รหัสเดิม (`APP_PASSWORD`) → เข้า 3 หน้าเดิมตามปกติ
- **[เข้าร่วมกิจกรรม]** (user) → กรอกรหัสกิจกรรม + ชื่อ
- **[เข้าสู่ระบบ]** (admin / superuser) → username + password

---

## ระบบกิจกรรม — สิทธิ์ 3 ระดับ
- **superuser** (ผม คนเขียนโปรแกรม) — เห็น/จัดการทุกอย่าง รวมถึงเข้าคลังภาพทั่วไปและคลังเก่าได้ด้วย
- **admin** (หัวหน้า) — สร้าง/จัดการกิจกรรมของตัวเอง, เห็นรูปเฉพาะกิจกรรมที่ตัวเองสร้าง
- **user** (ลูกน้อง) — เข้ากิจกรรมด้วยรหัส, ถ่ายรูปส่ง, เห็นเฉพาะกิจกรรมที่ตัวเองเข้า

---

## 1. ระบบ Login
- **คลังภาพทั่วไป (เดิม):** ใช้ `APP_PASSWORD` เหมือนเดิม ไม่เปลี่ยน
- **user:** กรอก **รหัสกิจกรรม** + **ชื่อตัวเอง** → ถ้ารหัสถูกและกิจกรรม "เปิด" ก็เข้าได้ (ไม่ต้องมีบัญชี)
- **admin:** username + password → ตรวจกับแท็บ Users ใน Sheet
- **superuser:** username + password → ตรวจกับรหัสหลักใน `st.secrets` (เช่น `SUPERUSER_USER`, `SUPERUSER_PASS`)

เก็บ role + ตัวตนไว้ใน `st.session_state` และมีปุ่ม **ออกจากระบบ** กลับมาหน้าแรก

---

## 2. โครงสร้างข้อมูล (Google Sheet) — เพิ่ม 2 แท็บ + 1 คอลัมน์ (ของเดิมไม่ยุ่ง)

### แท็บใหม่ "Activities"
```
activity_id | ชื่อกิจกรรม | รหัสเข้า_hash | คนสร้าง | วันที่สร้าง | สถานะ
```
- `รหัสเข้า_hash`: **เก็บเป็น hash ไม่ใช่รหัสจริง** (ดูข้อ 4)
- `คนสร้าง`: username ของ admin/superuser ที่สร้าง
- `สถานะ`: เปิด / ปิด (ปิดแล้ว user เข้าไม่ได้)

### แท็บใหม่ "Users" (เก็บบัญชี admin)
```
username | password_hash | ชื่อ-นามสกุล | role | สถานะ
```
- superuser สร้างบัญชี admin ผ่านหน้าจอในแอป (ไม่ต้องแก้ชีตมือ)
- superuser ไม่ต้องอยู่ในแท็บนี้ (ใช้รหัสจาก secrets)

### แท็บรูป (ใช้ Sheet เดิม เพิ่ม 1 คอลัมน์)
- เพิ่มคอลัมน์ **`activity_id`** ต่อท้ายคอลัมน์เดิม
- รูปที่ส่งผ่าน **กิจกรรม**: เก็บ `วันเวลา`, `activity_id`, `ผู้ส่ง`, `ลิงก์รูป`, `ชื่อไฟล์` (แผนก/หมวด/แท็ก/ชื่อเรื่อง ปล่อยว่าง)
- รูปที่ส่งผ่าน **คลังภาพทั่วไป (เดิม)**: เก็บเหมือนเดิมทุกอย่าง + `activity_id` ปล่อยว่าง
- การแยกแสดง: ถ้า `activity_id` ว่าง = ของคลังทั่วไป, ถ้ามีค่า = ของกิจกรรมนั้น

---

## 3. หน้าจอแยกตาม role (เฉพาะส่วนกิจกรรม)

### user (หลังเข้ากิจกรรม)
- หน้าส่งรูป: แสดงชื่อกิจกรรมที่กำลังอยู่ + กรอก **แค่รูป** (ชื่อผู้ส่งมาจากตอน login) → บีบอัด → อัป Drive → บันทึก Sheet พร้อม activity_id (reuse pipeline เดิม)
- ดูรูปในกิจกรรมนี้ (grid + ดาวน์โหลดเดี่ยว/zip) — เห็นเฉพาะ activity_id นี้

### admin (หลัง login)
- **สร้างกิจกรรมใหม่:** กรอกชื่อ → ตั้ง/สุ่ม "รหัสเข้า" → บันทึก (เก็บ hash) → โชว์รหัสจริงให้ก๊อปไปแจกลูกน้อง (โชว์ตอนสร้าง)
- รายการกิจกรรมของตัวเอง + เปิด/ปิดกิจกรรม
- คลังภาพ + dashboard **เฉพาะกิจกรรมที่ตัวเองสร้าง** + ดาวน์โหลดเดี่ยว/zip + ลบรูป

### superuser (หลัง login รหัสหลัก)
- เห็นทุกกิจกรรม (ทุก admin) + คลังภาพของทั้งหมด + เข้าคลังภาพทั่วไป/คลังเก่าได้
- **จัดการบัญชี admin:** สร้าง/ปิดบัญชี admin (เขียนลงแท็บ Users)
- เห็นหน้า **Dashboard** (ดูข้อ 3.5)

---

## 3.5 หน้า Dashboard — เฉพาะ superuser เท่านั้น (admin/user ไม่เห็น)

**Layout:** ไม่มี sidebar — ย้ายปุ่ม **รีเฟรชข้อมูล** + **ออกจากระบบ** ไปไว้ที่ **top bar มุมบนขวา** ของหน้า (เนื้อหาเต็มความกว้าง สะอาดตา)

มี 3 ส่วนหลัก:

**1) พื้นที่ Google Drive** (สำคัญสุด — Drive ฟรี 15 GB)
- โชว์ ใช้ไปกี่ GB / 15 GB + % + พื้นที่เหลือ เป็นแถบ progress
- ดึงโควตาจริงจาก Drive API (endpoint `about.get` → `storageQuota`)
- เสริม: จำนวนรูปทั้งหมด + ขนาดเฉลี่ยต่อรูป

**2) ภาพรวมกิจกรรม**
- การ์ดสรุป: กิจกรรมที่เปิดอยู่, รูปในกิจกรรมเดือนนี้, จำนวน admin
- ตารางกิจกรรม: ชื่อกิจกรรม · คนสร้าง · จำนวนรูป (เรียงจากมากไปน้อย)

**3) กิจกรรมที่ต้องดูแล** (แจ้งเตือน)
- กิจกรรมที่ "เปิดแล้วยังไม่มีรูปเลย"
- กิจกรรมที่ "เปิดค้างนาน ไม่มีรูปใหม่เกิน 30 วัน"

> หน้าตาอ้างอิงตาม mockup ที่ทำไว้ (3 การ์ดหน้าแรก + หน้า user เรียบง่าย + dashboard มี top bar)

---

## 4. ความปลอดภัยของรหัส (สำคัญ)
- **ความเป็นส่วนตัวของกิจกรรม = ใช้รหัสอย่างเดียว (ระดับ A):** แต่ละกิจกรรมมีรหัสของตัวเอง คนไม่มีรหัสเข้าไม่ได้/ไม่เห็น ถ้าอยากให้เฉพาะบางแผนกเห็น = admin เอารหัสไปให้เฉพาะแผนกนั้น (ไม่ต้องทำระบบจำกัดแผนกเพิ่ม)
- **ห้ามเก็บรหัสกิจกรรม/รหัส admin เป็นตัวอักษรดิบในชีต**
- hash ด้วย `hashlib` (sha256) + salt ใน `st.secrets` (หรือ `passlib`/`bcrypt` ถ้าดีกว่า) — เลือกวิธีที่ติดตั้งง่ายบน Streamlit Cloud แล้วอธิบายให้ผมเข้าใจ
- ตรวจสอบ: hash ค่าที่กรอก แล้วเทียบกับ hash ในชีต

---

## 5. สิ่งที่ต้องระวัง / ทำเพิ่ม
- **อย่าทำระบบเดิมพัง** — reuse ฟังก์ชันเดิม (OAuth, บีบอัดรูป, upload_to_drive, append_row, retry, ลบรูป, cache) ให้มากที่สุด
- ทุกหน้าเช็คสิทธิ์ก่อนเสมอ (กัน user/admin มองข้ามขอบเขตตัวเอง)
- อัปเดต `secrets.toml.example` (เพิ่ม SUPERUSER_USER/PASS, SALT) + README + .gitignore
- ทดสอบ (AppTest หรือวิธีที่ใช้อยู่): เข้าคลังทั่วไปยังได้เหมือนเดิม, login ครบ 3 role, สร้างกิจกรรม, user เข้าด้วยรหัส, ส่งรูป, การมองเห็นถูกขอบเขต

---

## 6. ลำดับที่อยากให้ทำ
1. อ่านโค้ดเดิม + สรุปแผนการแก้ให้ผมดูก่อน (**ยังไม่ต้องเขียนโค้ด**)
2. เพิ่มโครงข้อมูล (Activities/Users/คอลัมน์ activity_id) + ฟังก์ชัน hash
3. ทำหน้าแรก (landing) + ระบบ login (คลังทั่วไปเดิม / user / admin / superuser)
4. ทำหน้า user (เข้ากิจกรรม → ส่งรูป → ดูรูปในกิจกรรม)
5. ทำหน้า admin (สร้าง/จัดการกิจกรรม + คลังภาพเฉพาะของตัวเอง)
6. ทำหน้า superuser (เห็นหมด + จัดการบัญชี admin)
7. ทดสอบว่าระบบเดิมยังปกติ + อัปเดต README + push ขึ้น GitHub

**เริ่มจากข้อ 1 ก่อน: อ่านโค้ดเดิมแล้วเสนอแผน อย่าเพิ่งเขียนโค้ด**
